name: Format, lock, and verify content-bound sidecar authority

on:
  push:
    branches:
      - agent/corpus-sidecar-path-authority-6885
    paths:
      - .github/workflows/diagnose-sidecar-authority-6938.yml

permissions:
  contents: write

jobs:
  format-lock-verify-publish:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Check out candidate
        uses: actions/checkout@v5
        with:
          ref: agent/corpus-sidecar-path-authority-6885
          fetch-depth: 0

      - name: Verify bounded base
        shell: bash
        run: |
          set -euo pipefail
          git merge-base --is-ancestor 8a50e945ced6c4d2a046527014365e9b14bdecbf HEAD
          test "$(git diff --name-only 8a50e945ced6c4d2a046527014365e9b14bdecbf HEAD | sort)" = $'.github/workflows/diagnose-sidecar-authority-6938.yml\n.github/workflows/verify-sidecar-authority-6938.yml'

      - name: Format and refresh the lockfile
        shell: bash
        run: |
          set -euo pipefail
          cargo fmt -p perl-corpus
          cargo check -p perl-corpus --all-targets
          python3 - <<'PY'
          import subprocess

          allowed = {
              'Cargo.lock',
              'crates/perl-corpus/src/fixture_expectations.rs',
              'crates/perl-corpus/src/sidecar.rs',
              'crates/perl-corpus/src/sidecar/context.rs',
              'crates/perl-corpus/src/sidecar/model.rs',
              'crates/perl-corpus/src/sidecar/parse.rs',
              'crates/perl-corpus/tests/fixture_sidecar_schema.rs',
              'crates/perl-corpus/tests/fixture_sidecars.rs',
              'crates/perl-corpus/tests/sidecar_path_authority.rs',
          }
          changed = set(subprocess.check_output(['git', 'diff', '--name-only'], text=True).splitlines())
          outside = changed - allowed
          if outside:
              raise SystemExit(f'format/lock refresh touched files outside the reviewed slice: {sorted(outside)}')
          if 'Cargo.lock' not in changed:
              raise SystemExit('expected Cargo.lock refresh for the sidecar hashing dependency')
          print('candidate-generated changes:', *sorted(changed), sep='\n  ')
          PY

      - name: Verify candidate with the refreshed lockfile
        shell: bash
        env:
          RUST_BACKTRACE: "1"
        run: |
          set -euo pipefail

          run_checked() {
            local label="$1"
            shift
            local log="/tmp/${label//[^a-zA-Z0-9]/_}.log"
            if "$@" >"$log" 2>&1; then
              cat "$log"
              return 0
            fi
            cat "$log"
            python3 - "$label" "$log" <<'PY'
          import sys
          from pathlib import Path

          title = sys.argv[1]
          lines = Path(sys.argv[2]).read_text(errors='replace').splitlines()
          message = '\n'.join(lines[-80:]) or 'command failed without output'
          message = message.replace('%', '%25').replace('\r', '%0D').replace('\n', '%0A')
          title = title.replace('%', '%25').replace('\r', ' ').replace('\n', ' ')
          print(f'::error title={title}::{message}')
          PY
            return 1
          }

          rustup show
          run_checked "corpus fmt check" cargo fmt -p perl-corpus -- --check
          run_checked "corpus locked all-target check" cargo check -p perl-corpus --all-targets --locked
          run_checked "corpus locked all-target tests" cargo test -p perl-corpus --all-targets --locked
          run_checked "corpus locked all-target clippy" cargo clippy -p perl-corpus --all-targets --locked -- -D warnings
          run_checked "diff check" git diff --check

      - name: Publish candidate-generated files and remove temporary verification surface
        shell: bash
        run: |
          set -euo pipefail
          rm .github/workflows/verify-sidecar-authority-6938.yml
          rm .github/workflows/diagnose-sidecar-authority-6938.yml
          git add Cargo.lock crates/perl-corpus \
            .github/workflows/verify-sidecar-authority-6938.yml \
            .github/workflows/diagnose-sidecar-authority-6938.yml
          python3 - <<'PY'
          import subprocess

          allowed = {
              '.github/workflows/diagnose-sidecar-authority-6938.yml',
              '.github/workflows/verify-sidecar-authority-6938.yml',
              'Cargo.lock',
              'crates/perl-corpus/src/fixture_expectations.rs',
              'crates/perl-corpus/src/sidecar.rs',
              'crates/perl-corpus/src/sidecar/context.rs',
              'crates/perl-corpus/src/sidecar/model.rs',
              'crates/perl-corpus/src/sidecar/parse.rs',
              'crates/perl-corpus/tests/fixture_sidecar_schema.rs',
              'crates/perl-corpus/tests/fixture_sidecars.rs',
              'crates/perl-corpus/tests/sidecar_path_authority.rs',
          }
          staged = set(subprocess.check_output(['git', 'diff', '--cached', '--name-only'], text=True).splitlines())
          outside = staged - allowed
          if outside:
              raise SystemExit(f'staged files outside the reviewed sidecar slice: {sorted(outside)}')
          required = {
              '.github/workflows/diagnose-sidecar-authority-6938.yml',
              '.github/workflows/verify-sidecar-authority-6938.yml',
              'Cargo.lock',
          }
          if not required <= staged:
              raise SystemExit(f'missing required staged files: {sorted(required - staged)}')
          PY
          git config user.name "EffortlessSteven"
          git config user.email "git@effortlesssteven.com"
          git commit -m "fix(corpus): lock and format sidecar authority"
          git push origin HEAD:agent/corpus-sidecar-path-authority-6885
