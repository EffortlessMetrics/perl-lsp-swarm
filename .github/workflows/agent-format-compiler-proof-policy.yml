name: Agent format compiler proof policy

on:
  push:
    branches:
      - agent/compiler-concepts-stack-ci
    paths:
      - .github/workflows/agent-format-compiler-proof-policy.yml

permissions:
  contents: read

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout exact candidate without write credentials
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - name: Install Rustfmt
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: 1.95.0
          components: rustfmt

      - name: Format proof policy source
        run: rustfmt --edition 2024 xtask/src/bin/compiler-proof-policy.rs

      - name: Verify formatted source
        run: rustfmt --edition 2024 --check xtask/src/bin/compiler-proof-policy.rs

      - name: Collect formatted production file
        run: |
          mkdir -p target/compiler-proof-policy-format/xtask/src/bin
          cp xtask/src/bin/compiler-proof-policy.rs target/compiler-proof-policy-format/xtask/src/bin/
          git diff -- xtask/src/bin/compiler-proof-policy.rs > target/compiler-proof-policy-format/rustfmt.patch

      - name: Upload formatted source
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: compiler-proof-policy-format-${{ github.sha }}
          path: target/compiler-proof-policy-format
          if-no-files-found: error
          retention-days: 7
