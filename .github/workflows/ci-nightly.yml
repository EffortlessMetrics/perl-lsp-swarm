name: CI (Nightly)

# Consolidated workflow for comprehensive nightly tests and label-gated jobs
# Combines: ci-expensive.yml, quality-checks.yml, fuzz.yml
# Cost: ~$0.15 per run (label-gated), ~$0.30 per nightly run
#
# Coverage is advisory: run it on the nightly/manual lane only. Normal PR and
# merge-queue validation use the required Rust Small and RIPR+ gates instead.

on:
  pull_request:
    branches: [ main, master ]
    # Keep label-gated jobs alive after the label is applied.
    types: [opened, synchronize, reopened, ready_for_review, labeled]
  schedule:
    # Run nightly at 3am UTC
    - cron: '0 3 * * *'
  workflow_dispatch:
    inputs:
      run_mutation:
        description: 'Run mutation testing'
        required: false
        type: boolean
        default: true
      run_benchmarks:
        description: 'Run performance benchmarks'
        required: false
        type: boolean
        default: true
      run_real_repo_latency:
        description: 'Run real-repo latency suite'
        required: false
        type: boolean
        default: true
      run_corpus_differential:
        description: 'Run full parser corpus differential suite'
        required: false
        type: boolean
        default: true
      run_coverage:
        description: 'Run test coverage'
        required: false
        type: boolean
        default: true
      run_fuzz:
        description: 'Run fuzz testing'
        required: false
        type: boolean
        default: true
      run_memory:
        description: 'Run LSP memory plateau workloads'
        required: false
        type: boolean
        default: true

# Cancel in-flight runs on new pushes (saves minutes)
concurrency:
  group: ci-nightly-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' && github.event.action == 'synchronize' }}

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: full

jobs:
  # ============================================================================
  # Label-Gated Jobs (run on PR with specific labels)
  # ============================================================================

  mutation:
    name: Mutation Testing
    if: |
      github.event_name == 'schedule' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.run_mutation != 'false') ||
      (github.event_name == 'pull_request' &&
       contains(github.event.pull_request.labels.*.name, 'ci:mutation'))
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772  # stable (master)
        with: { toolchain: 1.95.0 }
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6  # v2.9.2
        with:
          cache-on-failure: true
          key: ${{ runner.os }}-mutation-${{ hashFiles('Cargo.lock') }}
      - run: cargo install cargo-mutants --locked || true
      - run: cargo mutants --timeout 60 --no-shuffle || true
      - name: Upload mutation results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.0
        with:
          name: mutation-results-${{ github.sha }}
          path: mutants.out/mutants.json
          retention-days: 30

  benchmark:
    name: Performance Benchmarks
    timeout-minutes: 45
    if: |
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'schedule' ||
      contains(github.event.pull_request.labels.*.name, 'ci:bench')
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout repository
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      with:
        persist-credentials: false

    - name: Install Rust toolchain
      uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772  # stable (master)
      with:
        toolchain: stable

    - name: Cache cargo dependencies
      uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6  # v2.9.2
      with:
        key: ${{ runner.os }}-bench-${{ hashFiles('Cargo.lock') }}
        cache-on-failure: true

    - name: Install just
      uses: taiki-e/install-action@6c6fd71fe4fb72c3697d269963d0e15df8adedad  # v2.85.10
      with:
        tool: just

    - name: Run benchmark harness fixture tests
      # Fixture tests for the extraction/guard logic itself (both Criterion
      # on-disk layouts, stale/malformed-estimate rejection, zero-benchmark
      # and missing-expected-id fail-closed behavior). If these regress, the
      # steps below cannot be trusted to catch a vacuous benchmark run (#3979).
      run: |
        python3 benchmarks/scripts/test_extract_criterion.py -v
        python3 benchmarks/scripts/test_benchmark_guards.py -v

    - name: Build perl-lsp binary for real-repo UX perf lane
      run: cargo build -p perl-lsp-rs --bin perl-lsp --locked

    - name: Run real-repo first-diagnostics perf lane
      run: |
        mkdir -p benchmarks/results
        PERL_LSP_BIN="${{ github.workspace }}/target/debug/perl-lsp" \
          PERL_LSP_UX_REQUIRE_BINARY=1 \
          cargo test -p perl-lsp-ux-tests --features integration-test \
          --test ux_scenario_18_real_repo_perf -- --test-threads=1 --nocapture \
          2>&1 | tee benchmarks/results/real-repo-perf.txt

    - name: Run benchmarks (explicit Criterion targets)
      run: |
        set -euo pipefail

        # Delete any stale target/criterion/ before running so a leftover
        # estimate from a previous job/run can never satisfy the "did this
        # run produce results" check below (#3979).
        rm -rf target/criterion
        mkdir -p benchmarks/results

        # Invoke each *declared* Criterion bench target explicitly
        # (`cargo bench -p <crate> --bench <name>`) instead of a blanket
        # `cargo bench --workspace`. The blanket form forwards `-- --noplot`
        # (a Criterion-only flag) to every workspace bench target,
        # INCLUDING plain libtest unit-test/bench harnesses that cargo also
        # builds for a `--workspace` bench run (e.g. `perl-ast --lib`), and
        # libtest rejects it outright:
        #   error: Unrecognized option: 'noplot'
        #   error: bench failed, to rerun pass `-p perl-ast --lib`
        # That failure previously aborted the whole `cargo bench --workspace`
        # invocation before any Criterion target ran at all, and `|| true`
        # (removed here) swallowed it, producing "Total benchmarks: 0" (#3979).
        #
        # Entries are "crate:bench-target:required-feature" (feature may be empty).
        # This list must stay a superset of every real bench target
        # `cargo metadata --no-deps --format-version 1` reports (`.targets[]
        # | select(.kind == ["bench"])`) -- since this REPLACES the blanket
        # `cargo bench --workspace`, an omitted target here is silent
        # coverage loss the old command wouldn't have had. Verified against
        # the current metadata (14 bench targets, all 14 listed below); if a
        # new bench target is added to the workspace, add it here too.
        declare -a BENCH_TARGETS=(
          "perl-workspace:workspace_index_benchmark:workspace"
          "perl-token:token_scorecard:"
          "perl-symbol:symbol_benchmarks:"
          "perl-pragma:pragma_benchmarks:"
          "perl-parser:parser_benchmark:"
          "perl-parser:incremental_benchmark:incremental"
          "perl-parser:scope_benchmark:"
          "perl-parser:cpan_patterns_benchmark:"
          "perl-parser:positions_bench:"
          "perl-parser:substitution_performance:"
          "perl-lsp-rs:rope_performance_benchmark:"
          "perl-lexer:lexer_benchmarks:"
          "perl-dap:dap_benchmarks:"
          "perl-incremental-parsing:incremental_parsing_benchmarks:"
        )

        for entry in "${BENCH_TARGETS[@]}"; do
          IFS=':' read -r crate bench feature <<< "$entry"
          echo "=== cargo bench -p $crate --bench $bench ${feature:+(features: $feature)} ==="
          if [ -n "$feature" ]; then
            cargo bench -p "$crate" --locked --features "$feature" --bench "$bench" -- --noplot
          else
            cargo bench -p "$crate" --locked --bench "$bench" -- --noplot
          fi
        done 2>&1 | tee benchmarks/results/raw-output.txt

        # Extract structured results from Criterion. --strict fails closed
        # when target/criterion/ has no results at all, or when any
        # --expect-id benchmark is missing. Each --expect-id below is one
        # representative benchmark per declared target above, proving that
        # *specific* target actually executed rather than merely that
        # *something* did.
        #
        # Verified against a real run (29186609858): "cpan/moose_oo_class" is
        # a *direct* `c.bench_function("cpan/moose_oo_class", ...)` call, not
        # an explicit `benchmark_group`. Criterion sanitizes the literal "/"
        # in a direct id into "_" and writes a single flat directory
        # (target/criterion/cpan_moose_oo_class/new/...), unlike a real
        # group/name pair (e.g. "document_insertions/rope_insertion" below),
        # which genuinely nests. The id here must match what's actually on
        # disk, not the source-code string passed to bench_function.
        python3 ./benchmarks/scripts/extract-criterion.py \
          --output benchmarks/results/latest.json \
          --strict \
          --expect-id "incremental update single file" \
          --expect-id "token_scorecard_recorded" \
          --expect-id "cursor_extract_ascii" \
          --expect-id "build_small_file" \
          --expect-id "parse_simple_script" \
          --expect-id "incremental small edit" \
          --expect-id "scope_analysis_many_vars" \
          --expect-id "cpan_moose_oo_class" \
          --expect-id "document_insertions/rope_insertion" \
          --expect-id "simple_tokens" \
          --expect-id "configuration_validation/launch_config_validation" \
          --expect-id "offset_to_pos16_cached" \
          --expect-id "parse_basic_substitution" \
          --expect-id "undo_redo/edit_undo_redo"

    - name: Real-repo first-diagnostics perf gate (Catalyst 5000+ lines)
      run: |
        cargo test -p perl-lsp-rs --test real_project_latency \
          -- --ignored first_diagnostics_5000_line_catalyst --nocapture

    # Regression gating stays advisory (continue-on-error) because GitHub
    # Actions runners have high variance in absolute timings — a real
    # baseline (benchmarks/baselines/v0.10.0.json) exists and the
    # comparison is meaningful, but promoting it to a blocking gate would
    # produce false positives from runner speed differences. The nightly
    # report captures the trend; a sustained regression triggers a manual
    # investigation rather than an automatic merge block (#3979, #5282).
    # Integrity (did the harness actually run, see above) and regression
    # policy (did performance get worse) are deliberately separate.
    - name: Compare against baseline
      id: compare
      continue-on-error: true
      run: |
        LATEST_BASELINE=$(ls -t benchmarks/baselines/v*.json 2>/dev/null | head -1)
        if [ -n "$LATEST_BASELINE" ]; then
          ./benchmarks/scripts/compare.sh --fail-on-regression 2>&1 | tee comparison.txt
          echo "REGRESSION=$?" >> $GITHUB_OUTPUT
        else
          echo "No baseline found, skipping comparison"
          echo "REGRESSION=0" >> $GITHUB_OUTPUT
        fi

    - name: Generate benchmark receipt
      if: always()
      run: |
        python3 ./benchmarks/scripts/format-results.py benchmarks/results/latest.json --receipt > benchmark_receipt.txt
        cat benchmark_receipt.txt

    - name: Generate markdown report
      if: always()
      run: |
        python3 ./benchmarks/scripts/format-results.py benchmarks/results/latest.json --markdown > benchmark_report.md

    - name: Generate performance alerts
      id: alert
      continue-on-error: true
      run: |
        LATEST_BASELINE=$(ls -t benchmarks/baselines/v*.json 2>/dev/null | head -1)
        if [ -n "$LATEST_BASELINE" ]; then
          python3 ./benchmarks/scripts/alert.py --format markdown > alert.md
          python3 ./benchmarks/scripts/alert.py --check 2>&1
          echo "CRITICAL=$?" >> $GITHUB_OUTPUT
        else
          echo "No baseline found, skipping alerts"
          echo "CRITICAL=0" >> $GITHUB_OUTPUT
        fi

    - name: Summarize performance evidence
      if: always()
      run: |
        # This replaces the PR comment removed by #6028. That comment was the
        # only inline surface for these alerts, and it was the sole reason this
        # workflow held issue and pull-request write scopes while also executing
        # candidate code. A step summary restores the same visibility at zero
        # permission cost.
        #
        # Keyed off file presence rather than the `compare`/`alert` step
        # outputs: those are computed through a `tee` pipeline without
        # `pipefail`, so they do not carry the real exit status. That is a
        # pre-existing defect and is deliberately not depended on here.
        {
          echo "## Performance Benchmarks"
          echo
          if [ -s alert.md ]; then
            cat alert.md
            echo
          fi
          if [ -s comparison.txt ]; then
            echo "<details><summary>Baseline comparison</summary>"
            echo
            echo '```text'
            cat comparison.txt
            echo '```'
            echo
            echo "</details>"
            echo
          fi
          if [ ! -s alert.md ] && [ ! -s comparison.txt ]; then
            echo "No baseline comparison or alert output was produced."
          fi
          echo "Full benchmark artifacts are attached to this run."
        } >> "$GITHUB_STEP_SUMMARY"

    - name: Upload benchmark artifacts
      if: always()
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.0
      with:
        name: benchmark-results-${{ github.sha }}
        path: |
          benchmarks/results/latest.json
          benchmarks/results/raw-output.txt
          benchmarks/results/real-repo-perf.txt
          benchmark_receipt.txt
          benchmark_report.md
          comparison.txt
          alert.md
        if-no-files-found: warn
        retention-days: 30

  real-repo-latency:
    name: Real-Repo Latency (Catalyst 5K Diagnostics)
    timeout-minutes: 30
    if: |
      github.event_name == 'schedule' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.run_real_repo_latency != 'false') ||
      (github.event_name == 'pull_request' &&
       (contains(github.event.pull_request.labels.*.name, 'ci:bench') ||
        contains(github.event.pull_request.labels.*.name, 'ci:real-repo-latency')))
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout repository
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      with:
        persist-credentials: false

    - name: Install Rust toolchain
      uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772  # stable (master)
      with:
        toolchain: stable

    - name: Cache cargo dependencies
      uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6  # v2.9.2
      with:
        key: ${{ runner.os }}-real-repo-latency-${{ hashFiles('Cargo.lock') }}
        cache-on-failure: true

    - name: Run all ignored real-project latency tests
      env:
        PERL_LSP_FIRST_DIAGNOSTICS_BUDGET_MS: "5000"
      run: |
        cargo test -p perl-lsp-rs --test real_project_latency \
          -- --ignored --nocapture --test-threads=1

    - name: Upload real-repo latency artifacts
      if: always()
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.0
      with:
        name: real-project-latency-${{ github.sha }}
        path: |
          .ci/metrics/real_project_latency.json
        retention-days: 30

  corpus-differential:
    name: Corpus Differential (Full)
    timeout-minutes: 20
    if: |
      github.event_name == 'schedule' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.run_corpus_differential != 'false') ||
      (github.event_name == 'pull_request' &&
       contains(github.event.pull_request.labels.*.name, 'ci:corpus-differential'))
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout repository
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      with:
        persist-credentials: false

    - name: Install Rust toolchain
      uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772  # stable (master)
      with:
        toolchain: stable

    - name: Cache cargo dependencies
      uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6  # v2.9.2
      with:
        key: ${{ runner.os }}-corpus-differential-${{ hashFiles('Cargo.lock') }}
        cache-on-failure: true

    - name: Run full corpus differential suite
      run: |
        set -o pipefail
        cargo test -p perl-parser-comparison --test corpus_differential \
          -- --ignored corpus_differential_full --nocapture 2>&1 \
          | tee corpus-differential-report.txt

    - name: Upload corpus differential report
      if: always()
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.0
      with:
        name: corpus-differential-${{ github.sha }}
        path: corpus-differential-report.txt
        if-no-files-found: warn
        retention-days: 30

  lsp-memory-plateau:
    name: LSP Memory Plateau
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    if: |
      github.event_name == 'schedule' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.run_memory != 'false') ||
      (github.event_name == 'pull_request' &&
       contains(github.event.pull_request.labels.*.name, 'ci:memory'))
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772  # stable (master)
        with:
          toolchain: 1.95.0

      - name: Cache cargo dependencies
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6  # v2.9.2
        with:
          cache-on-failure: true
          cache-all-crates: true
          shared-key: nightly-lsp-memory-${{ hashFiles('Cargo.lock') }}

      - name: Build perl-lsp release binary
        run: cargo build --release -p perl-lsp-rs --bin perl-lsp --locked

      - name: Run document-churn plateau workload
        run: |
          mkdir -p target/memory
          N_FILES=500 \
          N_CHANGES=10 \
          DO_WORKSPACE_SYMBOL=0 \
          DELETE_AFTER_CLOSE=1 \
          BINARY=./target/release/perl-lsp \
          python3 scripts/repro_lsp_storm.py \
            --json-out target/memory/nightly-doc-churn.json \
            --csv-out target/memory/nightly-doc-churn.csv \
            --settle-seconds 3 \
            --server-stderr target/memory/nightly-doc-churn.server.log
          set +e
          python3 scripts/assert_rss_plateau.py \
            target/memory/nightly-doc-churn.json \
            --summary-out target/memory/nightly-doc-churn.plateau.json
          PLATEAU_STATUS=$?
          set -e
          case "$GITHUB_EVENT_NAME" in
            pull_request) RECEIPT_EVENT=pull_request ;;
            merge_group) RECEIPT_EVENT=merge_group ;;
            *) RECEIPT_EVENT=push ;;
          esac
          cargo xtask metrics memory \
            --workload-json target/memory/nightly-doc-churn.json \
            --plateau-json target/memory/nightly-doc-churn.plateau.json \
            --scenario lsp_doc_churn_delete \
            --receipt target/memory/receipts/nightly-doc-churn.receipt.json \
            --commit "$GITHUB_SHA" \
            --event "$RECEIPT_EVENT" \
            --markdown >> "$GITHUB_STEP_SUMMARY"
          cargo xtask gate-receipts validate target/memory/receipts/nightly-doc-churn.receipt.json
          exit "$PLATEAU_STATUS"

      - name: Run workspace-symbol plateau workload
        run: |
          N_FILES=300 \
          N_CHANGES=10 \
          DO_WORKSPACE_SYMBOL=1 \
          DELETE_AFTER_CLOSE=1 \
          BINARY=./target/release/perl-lsp \
          python3 scripts/repro_lsp_storm.py \
            --json-out target/memory/nightly-workspace-symbol.json \
            --csv-out target/memory/nightly-workspace-symbol.csv \
            --settle-seconds 3 \
            --server-stderr target/memory/nightly-workspace-symbol.server.log
          set +e
          python3 scripts/assert_rss_plateau.py \
            target/memory/nightly-workspace-symbol.json \
            --summary-out target/memory/nightly-workspace-symbol.plateau.json
          PLATEAU_STATUS=$?
          set -e
          case "$GITHUB_EVENT_NAME" in
            pull_request) RECEIPT_EVENT=pull_request ;;
            merge_group) RECEIPT_EVENT=merge_group ;;
            *) RECEIPT_EVENT=push ;;
          esac
          cargo xtask metrics memory \
            --workload-json target/memory/nightly-workspace-symbol.json \
            --plateau-json target/memory/nightly-workspace-symbol.plateau.json \
            --scenario lsp_workspace_symbol_churn_delete \
            --receipt target/memory/receipts/nightly-workspace-symbol.receipt.json \
            --commit "$GITHUB_SHA" \
            --event "$RECEIPT_EVENT" \
            --markdown >> "$GITHUB_STEP_SUMMARY"
          cargo xtask gate-receipts validate target/memory/receipts/nightly-workspace-symbol.receipt.json
          cargo xtask memory-trends render \
            --input-dir target/memory \
            --output target/memory/memory_plateau_trends.md
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          summary_path = os.environ.get("GITHUB_STEP_SUMMARY")
          if not summary_path:
              raise SystemExit(0)

          rows = [
              (
                  "doc churn + delete",
                  "target/memory/nightly-doc-churn.json",
                  "target/memory/nightly-doc-churn.plateau.json",
              ),
              (
                  "workspace-symbol churn + delete",
                  "target/memory/nightly-workspace-symbol.json",
                  "target/memory/nightly-workspace-symbol.plateau.json",
              ),
          ]

          with open(summary_path, "a", encoding="utf-8") as fh:
              fh.write("## LSP memory plateau\n\n")
              fh.write("| Workload | Files | Tail growth KB | Median tail slope KB/file | Result |\n")
              fh.write("| --- | ---: | ---: | ---: | --- |\n")
              for label, payload_path, plateau_path in rows:
                  payload = json.loads(Path(payload_path).read_text())
                  plateau = json.loads(Path(plateau_path).read_text())
                  result = "passed" if plateau["passed"] else "failed"
                  fh.write(
                      f"| {label} "
                      f"| {payload['n_files']} "
                      f"| {plateau['tail_growth_kb']} "
                      f"| {plateau['median_tail_slope_kb_per_file']} "
                      f"| {result} |\n"
                  )
          PY
          exit "$PLATEAU_STATUS"

      - name: Upload memory plateau artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.0
        with:
          name: lsp-memory-plateau-${{ github.sha }}
          path: |
            target/memory/nightly-doc-churn.json
            target/memory/nightly-doc-churn.csv
            target/memory/nightly-doc-churn.plateau.json
            target/memory/nightly-doc-churn.server.log
            target/memory/receipts/nightly-doc-churn.receipt.json
            target/memory/nightly-workspace-symbol.json
            target/memory/nightly-workspace-symbol.csv
            target/memory/nightly-workspace-symbol.plateau.json
            target/memory/nightly-workspace-symbol.server.log
            target/memory/receipts/nightly-workspace-symbol.receipt.json
            target/memory/memory_plateau_trends.md
          if-no-files-found: warn
          retention-days: 30

  test-coverage:
    name: Codecov / Patch 95
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    if: |
      github.event_name == 'schedule' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.run_coverage != 'false')
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Emit changed-file coverage route summary
        id: coverage_route
        env:
          BASE_REF: ${{ github.base_ref || github.event.repository.default_branch }}
        run: |
          set -euo pipefail
          BASE_REF="${BASE_REF#refs/heads/}"
          mkdir -p target/receipts/quality
          base_ref="$BASE_REF"
          git fetch --no-tags --depth=200 origin "$base_ref" || true
          python3 scripts/ci/route-codecov-packs.py \
            --base "origin/$base_ref" \
            --head HEAD \
            --manifest .ci/coverage-packs.toml \
            --receipt target/receipts/quality/ci-route.json \
            --summary target/receipts/quality/ci-route.md
          coverage_required="$(python3 -c 'import json; from pathlib import Path; route = json.loads(Path("target/receipts/quality/ci-route.json").read_text(encoding="utf-8")); print("true" if route.get("coverage_proof_packs") else "false")')"
          echo "coverage_required=$coverage_required" >> "$GITHUB_OUTPUT"
          if [ "$coverage_required" != "true" ]; then
            printf '# Patch Coverage Proof\n\n- decision: `skipped-by-policy`\n- reason: changed-file routing selected no LCOV coverage proof packs\n' \
              > target/receipts/quality/quality-gate-coverage.md
            printf '{"schema_version":"quality_gate.v1","mode":"enforce-patch-coverage","decision":"skipped-by-policy","reason":"changed-file routing selected no LCOV coverage proof packs"}\n' \
              > target/receipts/quality/quality-gate-coverage.json
          fi

      - name: Install Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772  # nightly (master)
        with:
          toolchain: nightly
          components: llvm-tools-preview

      - name: Cache cargo dependencies
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6  # v2.9.2
        with:
          key: ${{ runner.os }}-coverage-${{ hashFiles('Cargo.lock') }}
          cache-targets: false
          cache-on-failure: true

      - name: Install just
        uses: taiki-e/install-action@6c6fd71fe4fb72c3697d269963d0e15df8adedad  # v2.85.10
        with:
          tool: just

      - name: Install cargo-llvm-cov
        run: rustup run nightly cargo install cargo-llvm-cov --locked

      - name: Create legacy LSP fixtures (CI-only)
        run: |
          mkdir -p archive/legacy-tests/test_fixtures
          touch archive/legacy-tests/test_fixtures/main.pl
          touch archive/legacy-tests/test_fixtures/Module.pm
          touch archive/legacy-tests/test_fixtures/test.t

      - name: Run workspace coverage proof
        env:
          BASE_REF: ${{ github.base_ref || github.event.repository.default_branch }}
          RUSTFLAGS: "-Cdebuginfo=0"
          CARGO_BUILD_JOBS: 1
        run: |
          BASE_REF="${BASE_REF#refs/heads/}"
          base_ref="$BASE_REF"
          git fetch --no-tags --depth=200 origin "$base_ref" || true
          just coverage-proof "origin/$base_ref"

      - name: Append coverage proof summary
        if: always()
        shell: bash
        run: |
          if [ -f target/receipts/quality/ci-route.md ]; then
            cat target/receipts/quality/ci-route.md >> "$GITHUB_STEP_SUMMARY"
            printf '\n' >> "$GITHUB_STEP_SUMMARY"
          else
            echo "No changed-file coverage route summary was produced." >> "$GITHUB_STEP_SUMMARY"
          fi
          if [ -f target/receipts/quality/quality-gate-coverage.md ]; then
            cat target/receipts/quality/quality-gate-coverage.md >> "$GITHUB_STEP_SUMMARY"
          else
            echo "No coverage quality-gate summary was produced." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload coverage to Codecov
        id: codecov_upload
        # Non-fatal telemetry: the job verdict comes from the local quality-gate
        # receipt produced above (just coverage-proof / quality-gate step).
        # An upload failure (flake, rate-limit, empty token on re-run) must never
        # fail this required check when the gate passed.
        # Token wiring: CODECOV_TOKEN is set in env so the action can read it via
        # both the `with: token:` path AND the env-var fallback that Codecov v6
        # uses internally. The `env.CODECOV_TOKEN != ''` guard skips the upload
        # entirely when secrets are unavailable (fork PRs, certain re-run contexts).
        if: |
          env.CODECOV_TOKEN != ''
        continue-on-error: true
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f  # v7.0.0
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: target/lcov.info
          name: workspace-patch-coverage
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

      - name: Warn on Codecov upload failure
        if: always() && steps.codecov_upload.outcome == 'failure'
        shell: bash
        run: |
          printf '> [!WARNING]\n> Codecov telemetry upload failed. The job verdict is determined by the\n> local quality-gate receipt above, not by this upload. Upload failures\n> (flake, rate-limit, empty token on re-run) are non-fatal.\n' \
            >> "$GITHUB_STEP_SUMMARY"
          echo "::warning::Codecov telemetry upload failed. Verdict comes from local quality-gate receipt — this is non-fatal."

      - name: Upload coverage proof artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7
        with:
          name: coverage-proof-${{ github.sha }}
          path: |
            target/lcov.info
            target/receipts/quality/ci-route.json
            target/receipts/quality/ci-route.md
            target/receipts/quality/coverage-baseline.json
            target/receipts/quality/quality-gate-coverage.json
            target/receipts/quality/quality-gate-coverage.md
          retention-days: 14
          if-no-files-found: error

  tautology-check:
    name: Tautology Detection
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' &&
       contains(github.event.pull_request.labels.*.name, 'ci:strict'))
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Check for tautological assertions
        run: |
          echo "Checking for tautological test patterns..."

          if grep -r "assert!(.*\.is_some() || .*\.is_none())" tests/; then
            echo "❌ Found tautological assertions (is_some || is_none)"
            exit 1
          fi

          if grep -r "assert!(true)" tests/; then
            echo "❌ Found assert!(true)"
            exit 1
          fi

          if grep -r "assert!(.*\.len() >= 0)" tests/; then
            echo "❌ Found tautological length check (>= 0)"
            exit 1
          fi

          echo "✅ No tautological patterns found"

  semver-check:
    name: API Compatibility
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    if: |
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'schedule' ||
      (github.event_name == 'pull_request' &&
       contains(github.event.pull_request.labels.*.name, 'ci:semver'))
    continue-on-error: true
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Install Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772  # stable (master)
        with:
          toolchain: stable

      - name: Cache cargo dependencies
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6  # v2.9.2
        with:
          key: semver-${{ hashFiles('Cargo.lock') }}
          cache-on-failure: true

      - name: Install cargo-semver-checks
        run: cargo install cargo-semver-checks --version 0.47.0 --locked

      - name: Determine baseline tag
        id: baseline
        run: |
          BASELINE=$(git tag | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' | sort -V | tail -1)
          echo "baseline=$BASELINE" >> $GITHUB_OUTPUT
          echo "Using baseline: $BASELINE"

      - name: Check perl-parser API compatibility
        run: |
          cargo semver-checks check-release -p perl-parser \
            --baseline-rev ${{ steps.baseline.outputs.baseline }} \
            || echo "::warning::Breaking changes detected in perl-parser"

      - name: Check perl-lexer API compatibility
        run: |
          cargo semver-checks check-release -p perl-lexer \
            --baseline-rev ${{ steps.baseline.outputs.baseline }} \
            || echo "::warning::Breaking changes detected in perl-lexer"

      - name: Check perl-parser-core API compatibility
        run: |
          cargo semver-checks check-release -p perl-parser-core \
            --baseline-rev ${{ steps.baseline.outputs.baseline }} \
            || echo "::warning::Breaking changes detected in perl-parser-core"

      - name: Check perl-lsp-rs API compatibility
        run: |
          cargo semver-checks check-release -p perl-lsp-rs \
            --baseline-rev ${{ steps.baseline.outputs.baseline }} \
            || echo "::warning::Breaking changes detected in perl-lsp-rs"

      - name: Check perllsp API compatibility
        run: |
          cargo semver-checks check-release -p perllsp \
            --baseline-rev ${{ steps.baseline.outputs.baseline }} \
            || echo "::warning::Breaking changes detected in perllsp"

      - name: Generate breaking changes report
        if: always()
        run: |
          mkdir -p target/semver-reports
          cargo semver-checks check-release --workspace \
            --baseline-rev ${{ steps.baseline.outputs.baseline }} \
            --output-format json > target/semver-reports/breaking-changes.json || true

      - name: Upload breaking changes report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.0
        with:
          name: semver-breaking-changes-report
          path: target/semver-reports/breaking-changes.json
          retention-days: 30

  public-api-check:
    name: Public API Surface Check
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    if: |
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'schedule' ||
      (github.event_name == 'pull_request' &&
       contains(github.event.pull_request.labels.*.name, 'ci:public-api'))
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Install Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772  # stable (master)
        with:
          toolchain: 1.95.0

      - name: Cache cargo dependencies
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6  # v2.9.2
        with:
          key: public-api-${{ hashFiles('Cargo.lock') }}
          cache-on-failure: true

      - name: Install cargo-public-api
        run: cargo install cargo-public-api --locked --version 0.50.1

      - name: Install just
        uses: taiki-e/install-action@6c6fd71fe4fb72c3697d269963d0e15df8adedad  # v2.85.10
        with:
          tool: just

      - name: Check public API surface (perl-lsp-rs, perl-parser, perl-uri, perl-dap, perllsp)
        # Uses cargo public-api --simplified to omit blanket-impl noise; baselines in .ci/public-api-baselines/
        run: just public-api-check

  scorecard-ratchet-check:
    name: Scorecard Floor-Metric Ratchet Check
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    if: |
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'schedule' ||
      (github.event_name == 'pull_request' &&
       contains(github.event.pull_request.labels.*.name, 'ci:metrics-ratchet'))
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Install Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772  # stable (master)
        with:
          toolchain: 1.95.0

      - name: Cache cargo dependencies
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6  # v2.9.2
        with:
          key: scorecard-ratchet-${{ hashFiles('Cargo.lock') }}
          cache-on-failure: true

      - name: Install just
        uses: taiki-e/install-action@6c6fd71fe4fb72c3697d269963d0e15df8adedad  # v2.85.10
        with:
          tool: just

      - name: Check floor-metric ratchet (bootstrap-safe)
        # Passes if no receipt exists; compares against committed baselines when receipt is present.
        # See docs/project/metrics/RATCHET.md for the four-layer model and promotion workflow.
        run: just ci-metrics-ratchet

  clippy-strict:
    name: Clippy (Strict)
    timeout-minutes: 20
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' &&
       contains(github.event.pull_request.labels.*.name, 'ci:strict'))
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Install Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772  # stable (master)
        with:
          toolchain: stable

      - name: Cache cargo dependencies
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6  # v2.9.2
        with:
          key: ${{ runner.os }}-clippy-strict-${{ hashFiles('Cargo.lock') }}
          cache-on-failure: true

      - name: Run clippy (strict mode)
        run: |
          cargo clippy --workspace --locked -- -D warnings -D clippy::all -A missing_docs

  perl-kwalitee:
    name: Perl Kwalitee (advisory)
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    # Advisory-only for now: report never fails the process, and the job is
    # continue-on-error so a non-clean verdict does not block anything. Promote
    # to a blocking `check --profile pr --strict` once the receipt-backed
    # indicators are consistently green. See docs/reference/PERL_KWALITEE.md.
    continue-on-error: true
    if: |
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'schedule' ||
      (github.event_name == 'pull_request' &&
       contains(github.event.pull_request.labels.*.name, 'ci:kwalitee'))
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772  # stable (master)
        with:
          toolchain: 1.95.0

      - name: Cache cargo dependencies
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6  # v2.9.2
        with:
          key: ${{ runner.os }}-kwalitee-${{ hashFiles('Cargo.lock') }}
          cache-on-failure: true

      # The `perl-kwalitee` crate is pure — it never spawns a subprocess and
      # reads its evidence only from pre-generated JSON receipts. Without these
      # upstream receipts every nightly receipt-backed indicator resolves as
      # `unverified`. Generate them first (all native Rust, no perltidy/
      # perlcritic install needed) so the nightly report reflects real signal.
      # status/readiness aggregate the receipts above them, so they run last.
      # See docs/reference/PERL_KWALITEE.md.
      - name: Generate upstream Kwalitee receipts (native, no external tooling)
        run: |
          # Best-effort: this is an advisory report. `native-format check`/`corpus`
          # return a non-zero exit on a fixture/corpus regression, and Actions runs
          # `run:` under `set -e`. Without `set +e` the first failing generator would
          # abort the step, later receipts would never be written, and the report
          # below would revert those indicators to `unverified` — the exact symptom
          # this job exists to surface. Disable errexit so every generator runs and
          # a failing one shows up as a failing/unverified indicator in the report.
          set +e
          # --- native formatter receipts ---
          cargo xtask native-format check
          cargo xtask native-format corpus
          cargo xtask native-format config \
            --receipt target/receipts/format/native-format-config.json \
            --summary target/receipts/format/native-format-config.md
          cargo xtask native-format perltidy-compat --profile .ci/kwalitee/perltidyrc \
            --receipt target/receipts/format/native-format-perltidy-compat.json \
            --summary target/receipts/format/native-format-perltidy-compat.md

          # --- native critic receipts ---
          cargo xtask native-critic check
          cargo xtask native-critic check \
            --root xtask/tests/fixtures/native-critic/false-positive \
            --receipt target/receipts/native-tooling/native-critic-false-positive.json \
            --summary target/receipts/native-tooling/native-critic-false-positive.md
          cargo xtask native-tooling perlcritic-compat --profile .ci/kwalitee/perlcriticrc \
            --receipt target/receipts/native-tooling/perlcritic-compat.json \
            --summary target/receipts/native-tooling/perlcritic-compat.md

          # --- aggregation (reads all receipts above; must come last) ---
          cargo xtask native-tooling status \
            --receipt target/receipts/native-tooling/status.json
          cargo xtask native-tooling readiness \
            --status-receipt target/receipts/native-tooling/status.json \
            --receipt target/receipts/native-tooling/readiness.json

      - name: Evaluate Perl distribution Kwalitee (nightly profile)
        # Always run the report, even if a generator above exited non-zero, so the
        # advisory verdict reflects whatever receipts were produced rather than
        # being skipped entirely.
        if: always()
        run: |
          cargo xtask perl-kwalitee report --profile nightly \
            --json target/receipts/kwalitee/perl-kwalitee.json \
            --markdown target/receipts/kwalitee/perl-kwalitee.md
          cat target/receipts/kwalitee/perl-kwalitee.md >> "$GITHUB_STEP_SUMMARY"

      - name: Upload Kwalitee receipt
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.0
        with:
          name: perl-kwalitee-${{ github.sha }}
          # Upload the whole receipt directories rather than enumerating files:
          # the generation step writes JSON receipts plus their markdown summaries
          # (idempotence, parse-preservation, corpus-summary, per-command *.md), and
          # only this job writes under these paths — so a directory glob captures the
          # full evidence set for debugging without drifting out of sync on each new
          # receipt.
          path: |
            target/receipts/kwalitee/
            target/receipts/format/
            target/receipts/native-tooling/
          if-no-files-found: warn
          retention-days: 30

  # ============================================================================
  # Fuzz Testing (nightly only)
  # ============================================================================

  fuzz:
    name: Fuzz Target (${{ matrix.target }})
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    if: |
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'schedule'
    strategy:
      fail-fast: false
      matrix:
        target: [
          builtin_functions,
          config_surfaces,
          dap_eval_validator,
          dap_stack_parser,
          declaration_parsing,
          heredoc_parsing,
          incremental_edit_sequences,
          lexer_tokenization,
          lsp_cancellation_registry,
          lsp_navigation,
          module_surface,
          parser_integration,
          pod_extraction,
          quote_operators,
          regex_validation,
          semantic_model,
          structured_perl_programs,
          substitution_parsing,
          symbol_query_ranking,
          unicode_positions,
          utf16_roundtrip
        ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
      with:
        persist-credentials: false

    - name: Install Rust toolchain (nightly)
      uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772  # nightly (master)
      with:
        toolchain: nightly
        components: rust-src

    - name: Cache cargo dependencies
      uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6  # v2.9.2
      with:
        key: ${{ runner.os }}-fuzz-${{ matrix.target }}-${{ hashFiles('Cargo.lock') }}
        cache-on-failure: true

    - name: Install cargo-fuzz
      run: cargo install cargo-fuzz --locked

    - name: Run fuzzer
      run: |
        DURATION=${{ github.event.inputs.duration || '600' }}
        ./scripts/fuzz-bounded --duration "$DURATION" -- ${{ matrix.target }}

    - name: Upload crash artifacts
      if: failure()
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.0
      with:
        name: fuzz-artifacts-${{ matrix.target }}-${{ github.sha }}
        path: |
          fuzz/artifacts/
          fuzz/corpus/
        retention-days: 30
