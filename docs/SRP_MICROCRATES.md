# SRP microcrates — historical inventory pointer

The former generated microcrate inventory described an earlier workspace and must not be used as a current package or publishing plan.

For current membership and publish policy, use the [workspace manifest](https://github.com/EffortlessMetrics/perl-lsp-swarm/blob/main/Cargo.toml). For current ownership seams, use the [architecture overview](https://github.com/EffortlessMetrics/perl-lsp-swarm/blob/main/docs/reference/ARCHITECTURE.md) and the surviving package READMEs.

If a new inventory is generated, write it to an explicitly named report path with the source revision and generation command recorded. Do not overwrite this compatibility pointer.
