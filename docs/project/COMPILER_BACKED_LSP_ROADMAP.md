# Compiler-Backed Perl LSP Roadmap

This document defines the next semantic direction for `perl-lsp`.

The current product is still the language server. The near-term goal is not to
claim a general-purpose `rust-perl` replacement. The near-term goal is to grow a
Rust language engine that can answer compiler-grade questions for editor
features:

- What package, lexical scope, and pragma state are active here?
- Which symbols are declared by syntax, imported by compile-time rules, or
  generated by a framework?
- Which prototypes, constants, and inheritance facts affect this call?
- Which references are safe to rename, and which are behind dynamic boundaries?
- Which diagnostics are proven false positives by the semantic model?

Parsing stays the fast front lens. The path forward is to add the compiler
layers that turn syntax into durable semantic facts. Those same layers should
also be shaped so a future Rust-native Perl compiler/runtime path is a
continuation of the system, not a rewrite.

## Design Position

`perl-lsp` should be a compiler-backed LSP first, not a Perl runtime host in the
current lane. That is a product claim boundary, not an architectural ceiling.
The long-term platform direction is a Rust-native Perl compiler, runtime, and
tooling replacement path grown from the same fact substrate.

The primary engine is Rust:

```text
source
-> lexer
-> concrete syntax / recovery tree
-> AST
-> HIR
-> scope and pad model
-> package / stash model
-> pragma / feature model
-> import / export model
-> framework adapters
-> compile-time effect model
-> tooling IR
-> semantic fact graph
-> LSP providers
-> formatter / critic / refactor / determinism
-> bounded compiler/runtime targets
```

Real Perl still matters, but as a conformance oracle and validation instrument:

- differential tests against selected fixtures,
- framework behavior discovery,
- corpus conformance checks,
- validation of dynamic boundaries.

It must not become the normal editor hot path or a required runtime dependency
for baseline analysis. Public positioning that the parser does not call Perl is
still true for normal source analysis.

## Fast, Warm, Cold

The LSP should be organized as a tiered query engine.

### Fast Path

Runs on edits and must remain cheap:

- lexing,
- parsing,
- recovery,
- AST spans,
- local HIR refresh,
- local lexical facts,
- syntax-backed document symbols,
- fallback diagnostics and completions.

### Warm Path

Runs asynchronously and is cached by document or workspace hash:

- HIR indexing,
- lexical and package scope graphs,
- static import/export rules,
- simple inheritance,
- framework projections for stable patterns,
- workspace symbol and reference facts.

### Cold Path

Runs only when lower layers cannot honestly know the answer:

- compile-time effect modeling,
- module graph resolution,
- dynamic boundary classification,
- differential validation against sandboxed real Perl in CI or explicit proof
  lanes.

Cold-path results must be cached, provenance-tagged, and safe to ignore. A
failure in this layer must degrade answers; it must never break editing.

## Fact Doctrine

Every semantic fact should carry enough context for providers to be honest:

```text
entity id
kind
name
package / scope
range, if source-backed
provenance
confidence
source hash
dependency or module graph hash, when relevant
adapter or compiler-model version, when relevant
dynamic boundary, when unsupported behavior is observed
```

Providers should prefer exact facts but surface uncertainty instead of inventing
precision.

Suggested fact tiers:

```text
ExplicitSyntax
StaticDerived
FrameworkProjected
CompileModeled
RealPerlOracle
DynamicBoundary
```

`RealPerlOracle` is proof support, not the primary production source for editor
answers.

## GitHub Tracking

The build-out is tracked as issue-owned lanes. Do not open implementation PRs
from this document alone; pick the relevant issue, keep the PR one surface wide,
and link the verification receipt there.

| Workstream | Tracking issue | Next PR shape | Depends on | Evidence gate |
| --- | --- | --- | --- | --- |
| Compiler substrate umbrella | [#8191](https://github.com/EffortlessMetrics/perl-lsp/issues/8191) | Keep child issues current | [#7952](https://github.com/EffortlessMetrics/perl-lsp/issues/7952) | Child checklist and roadmap links stay current |
| Capability status | [#8205](https://github.com/EffortlessMetrics/perl-lsp/issues/8205), [#4063](https://github.com/EffortlessMetrics/perl-lsp/issues/4063), [#6484](https://github.com/EffortlessMetrics/perl-lsp/issues/6484) | Keep [compiler capability status](COMPILER_CAPABILITY_STATUS.md) current without copying generated parser metrics | Parser accuracy receipts | Parser metrics/check/status commands stay green |
| HIR | [#8224](https://github.com/EffortlessMetrics/perl-lsp/issues/8224) ([#8192](https://github.com/EffortlessMetrics/perl-lsp/issues/8192) baseline complete) | Keep HIR lowering coverage generated and current, no provider cutover | Parser AST anchors and first lowering fixtures | HIR snapshots, HIR coverage status, and local crate checks |
| Scope and pad | [#8193](https://github.com/EffortlessMetrics/perl-lsp/issues/8193) | Expand lexical resolution proof without provider cutover | HIR baseline | Lexical resolution fixtures |
| Package and stash | [#8194](https://github.com/EffortlessMetrics/perl-lsp/issues/8194) | Expand stash/typeglob/inheritance facts and workspace merge proof | HIR baseline, scope context | Stash/typeglob/inheritance fixtures |
| Compile environment and module resolution | [#8206](https://github.com/EffortlessMetrics/perl-lsp/issues/8206), [#8242](https://github.com/EffortlessMetrics/perl-lsp/issues/8242), [#8270](https://github.com/EffortlessMetrics/perl-lsp/issues/8270), [#8275](https://github.com/EffortlessMetrics/perl-lsp/issues/8275), [#8280](https://github.com/EffortlessMetrics/perl-lsp/issues/8280) | Current fixture-backed lane complete; keep facts available for downstream effects and provider proof | HIR, scope, stash, shared include-root builder, module-resolution cache-key proof | Compile-environment fixtures and [#8284](https://github.com/EffortlessMetrics/perl-lsp/pull/8284) receipt |
| Imports and exports | [#8244](https://github.com/EffortlessMetrics/perl-lsp/issues/8244), [#8264](https://github.com/EffortlessMetrics/perl-lsp/issues/8264) | Keep canonical `ImportSpec` / `ExportSet` / visible-symbol proof as provider input evidence | [#8252](https://github.com/EffortlessMetrics/perl-lsp/issues/8252), [#8253](https://github.com/EffortlessMetrics/perl-lsp/issues/8253), stash model, module resolution | Semantic scorecard and shadow compare |
| Framework adapters | [#8195](https://github.com/EffortlessMetrics/perl-lsp/issues/8195), [#8245](https://github.com/EffortlessMetrics/perl-lsp/issues/8245) | Exporter-family adapter registry slice complete; broader adapters stay issue-owned and parked | HIR, stash, imports | Framework fixtures with provenance and [#8287](https://github.com/EffortlessMetrics/perl-lsp/pull/8287) receipt |
| Compile-time effects | [#8207](https://github.com/EffortlessMetrics/perl-lsp/issues/8207), [#3394](https://github.com/EffortlessMetrics/perl-lsp/issues/3394), [#8293](https://github.com/EffortlessMetrics/perl-lsp/issues/8293), [#8294](https://github.com/EffortlessMetrics/perl-lsp/issues/8294) | HIR effect log, symbolic-ref boundaries, and selected differential proof complete | HIR, scope, stash, imports | Provider consumption remains gated by [#8197](https://github.com/EffortlessMetrics/perl-lsp/issues/8197) |
| Tooling IR / PIR | [#8196](https://github.com/EffortlessMetrics/perl-lsp/issues/8196) | PIR data model with context, no provider cutover | HIR, scope, stash | PIR lowering fixtures |
| Differential oracle | [#8199](https://github.com/EffortlessMetrics/perl-lsp/issues/8199) ([#4389](https://github.com/EffortlessMetrics/perl-lsp/issues/4389) background) | Real-Perl conformance runner for proof lanes only | Fact schema and sandbox policy | Structured agreement receipt |
| Provider cutover | [#8197](https://github.com/EffortlessMetrics/perl-lsp/issues/8197) | Fact-source tracing before behavior changes | Semantic scorecards, shadow compare | Provider-impact fixtures |

## Immediate PR Sequence

1. Keep [compiler capability status](COMPILER_CAPABILITY_STATUS.md) issue-owned
   and visible without copying generated parser metrics.
2. Add fact-source tracing before any provider consumes the new compiler facts,
   tracked in [#8197](https://github.com/EffortlessMetrics/perl-lsp/issues/8197).
3. Keep broader real-Perl conformance expansion issue-owned under
   [#8199](https://github.com/EffortlessMetrics/perl-lsp/issues/8199).

Recent module-resolution control points are complete:

- [#8270](https://github.com/EffortlessMetrics/perl-lsp/issues/8270):
  resolved/not-found outcomes over explicit provenance-labeled roots.
- [#8275](https://github.com/EffortlessMetrics/perl-lsp/issues/8275): cache
  key and invalidation proof for those outcomes.
- [#8280](https://github.com/EffortlessMetrics/perl-lsp/issues/8280):
  scoped `strict` / `warnings` / `feature` state facts with source anchors and
  dynamic pragma-argument boundaries.
- [#8245](https://github.com/EffortlessMetrics/perl-lsp/issues/8245):
  framework adapter registry and first Exporter-family fact projection without
  provider cutover.
- [#8291](https://github.com/EffortlessMetrics/perl-lsp/pull/8291):
  first HIR compile-effect log with model version, optional source hash,
  source/fact links, and fail-closed boundaries for unsupported phase blocks,
  dynamic `require`, and non-literal typeglob writes.
- [#8297](https://github.com/EffortlessMetrics/perl-lsp/pull/8297):
  symbolic-reference dynamic-boundary facts under disabled `strict refs`, with
  HIR and compile-effect proof and no provider cutover.
- [#8300](https://github.com/EffortlessMetrics/perl-lsp/pull/8300):
  selected compile-effect facts compared against a real-Perl oracle receipt for
  packages, subs, constants, prototypes, and `@ISA`, with zero disagreements
  and no provider cutover.

## Phase 0: Measurement Closeout To Capability Gaps

Parser-accuracy measurement rows are the control plane, not the finish line.
When measurement gaps are closed, status language should move to capability
gaps:

- syntax and recovery gaps,
- static semantic gaps,
- framework gaps,
- compile-time modeling gaps,
- provider behavior gaps.

Current denominators and failure packets belong in the generated parser status
files, not in this roadmap.

Relevant sources:

- [Compiler capability status](COMPILER_CAPABILITY_STATUS.md)
- [Parser status](status/parser.md)
- [Parser accuracy next](status/parser_accuracy_next.md)
- [Parsing Perl](PARSING_PERL.md)
- [Parser evolution](PARSER_EVOLUTION.md)

## Phase 1: HIR

Add a normalized high-level representation over the AST.

AST answers "what was written?" HIR answers "what language construct is this?"

Initial constructs:

- package declarations,
- subroutine and method declarations,
- `use` and `require`,
- lexical declarations,
- `our` and package declarations,
- calls and method calls,
- blocks,
- barewords,
- literals,
- dynamic boundaries.

The first HIR PR should be crate-local and fixture-backed. It should not cut LSP
providers over.

## Phase 2: Scope And Pad Model

Model Perl's lexical compilation state:

- `my`,
- `our`,
- `state`,
- `local`,
- package boundaries,
- block scopes,
- sub scopes,
- signature variables,
- pragma and feature visibility.

Outputs:

- scope frames,
- lexical bindings,
- package bindings,
- visible symbol sets,
- local reference facts.

This layer should unlock safer local rename, reference search, completions, and
diagnostics before broader compile-time modeling exists.

## Phase 3: Package And Stash Model

Model package symbol tables as first-class compiler state:

- package declarations,
- sub declarations,
- `our` variables,
- typeglob assignments,
- simple aliases,
- `@ISA`,
- constant subs,
- `AUTOLOAD` boundaries.

Outputs:

- package facts,
- stash facts,
- glob-slot facts,
- inheritance facts,
- alias facts,
- dynamic-boundary facts.

## Phase 4: Pragmas, Features, Imports, And Exports

Model common compile-time effects natively in Rust:

- `strict`,
- `warnings`,
- `feature`,
- `lib`,
- `@INC` and configured include paths,
- workspace module resolution,
- `parent`,
- `base`,
- `constant`,
- `vars`,
- `subs`,
- static `Exporter` cases,
- static `Exporter::Tiny` cases.

This phase should emit facts, not provider-specific behavior. Provider cutover
should happen only after scorecards and shadow comparisons show the facts are
stable.

This builds on the first-wave semantic substrate plan:

- [Semantic substrate first-wave plan](SEMANTIC_SUBSTRATE_FIRST_WAVE_PLAN.md)

## Phase 5: Framework Adapters

Frameworks are compile-time language extensions in practice. Stable patterns
should be modeled as explicit adapters over the fact graph:

- `Moo`,
- `Moose`,
- `Class::Tiny`,
- `Object::Pad`,
- `DBIx::Class`,
- `Catalyst`,
- `Dancer`,
- `Test::More`,
- `Try::Tiny`,
- `AUTOLOAD`-heavy modules.

Adapters emit generated-member facts, diagnostic policies, completions, and
rename blockers with provenance. They should not special-case LSP providers
directly.

## Phase 6: Compile-Time Effect Model

After HIR, scope, stash, and import/export facts exist, add a bounded model of
compile-time effects.

Start with representation and side-effect logging:

- `BEGIN`,
- `UNITCHECK`,
- `CHECK`,
- `use`,
- `no`,
- `require`,
- `import`,
- constant folding,
- prototype registration,
- symbol table mutation.

Then add safe evaluators for literal and modeled effects. Unsupported behavior
must produce a dynamic boundary, not a guessed fact.

Needed infrastructure:

- virtual filesystem and module resolver,
- modeled `@INC`,
- pragma stack,
- stash state,
- side-effect log,
- dynamic boundary packet.

## Phase 7: Tooling IR

Introduce a Perl intermediate representation oriented around editor tooling,
not bytecode execution.

It should model:

- scalar, list, and void context,
- lexical reads and writes,
- stash reads and writes,
- calls and method dispatch,
- assignment,
- branches,
- regex operations,
- dereference and glob access,
- dynamic boundaries.

The IR becomes the base for control-flow analysis, dead-code detection,
safe-delete, rename safety, and higher-confidence references.

## Sandbox And Real Perl Role

Sandboxed real Perl is still useful, but as proof infrastructure:

- differential tests,
- selected CPAN conformance checks,
- framework behavior discovery,
- validation of compiler-model disagreements,
- dynamic-boundary calibration.

Execution-bearing proof lanes must document:

- what runs,
- what it can read,
- what it can write,
- whether network is possible,
- timeout,
- memory/process caps,
- cache key,
- failure behavior.

No real-Perl execution should be hidden inside normal provider behavior.

## Metrics

Add compiler-substrate metrics as the layers appear:

- HIR lowering coverage,
- scope resolution precision,
- stash fact precision,
- import resolution precision,
- export resolution precision,
- pragma model coverage,
- prototype resolution precision,
- framework generated-member recall,
- compile-time effect model coverage,
- dynamic-boundary classification rate,
- IR lowering coverage.

Differential proof metrics should stay separate:

- Rust model vs real Perl package agreement,
- symbol table agreement,
- import/export agreement,
- `@ISA` agreement,
- prototype agreement,
- constant agreement.

## CI Shape

Fast PR lanes should stay Rust-only where possible:

```text
HIR fixtures
scope fixtures
stash fixtures
semantic scorecards
parser accuracy checks
provider shadow comparisons
```

Nightly or label-gated lanes can run differential checks against sandboxed real
Perl and selected corpus fixtures.

Any PR adding compile-time effect modeling should include:

- fact schema update,
- fixture,
- dynamic-boundary behavior,
- provider-impact statement, if any,
- differential proof or an explicit reason it is unavailable.

## Parallel Parser Work

Compiler layers do not replace parser hardening. Syntax and recovery gaps should
continue in narrow PRs:

```text
one corpus bucket
one reproducer fixture
one parser fix
one status update
one regression
```

Do not weaken gold expectations to reduce packet counts. Capability gaps should
be named and burned down, not hidden.

## Working Doctrine

```text
Fast path first.
Static facts are preferred when honest.
Framework projections promote stable dynamic patterns into cached Rust facts.
The Rust compiler substrate is the primary semantic engine.
Sandboxed real Perl is a conformance oracle, not the editor runtime.
Every fact has provenance, confidence, and freshness.
Unsupported compile-time behavior becomes a dynamic boundary.
Runtime execution is explicit, never ambient.
Provider cutover follows proof, not optimism.
```
