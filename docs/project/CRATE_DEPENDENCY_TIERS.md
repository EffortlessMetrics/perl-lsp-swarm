# Crate dependency tiers — current-source pointer

The former tier table was a snapshot of an earlier workspace and is not a current dependency contract.

Use the [workspace manifest](https://github.com/EffortlessMetrics/perl-lsp-swarm/blob/main/Cargo.toml) for exact membership, dependencies, exclusions, and publish policy. Use the [architecture overview](https://github.com/EffortlessMetrics/perl-lsp-swarm/blob/main/docs/reference/ARCHITECTURE.md) and package READMEs for current ownership seams. When a former crate has been absorbed, the manifest's absorption comment and surviving module path are authoritative.
