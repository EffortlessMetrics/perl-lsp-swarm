# Documentation source-of-truth stack

This tree separates artifact responsibilities:

- `proposals/` own **why**.
- `specs/` own **what behavior must be true**.
- `adr/` own durable architecture decisions.
- `status/` owns user-facing support claims.
- `handoffs/` owns closeout evidence.
- `templates/` owns document templates.
