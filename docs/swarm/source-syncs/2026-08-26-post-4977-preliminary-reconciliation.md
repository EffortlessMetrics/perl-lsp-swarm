# Post-#4977 preliminary reconciliation packet (2026-08-26)

Preliminary packet for #7971 under parent #6064. Generated by the landed
`sync-divergence` v2 tool (#7968 via #12457, #7969 via #12694). It is **not**
the final #4348 ledger and claims no freeze; swarm remains mutable.

## Exact subjects

| Role | Input | Resolved commit |
| --- | --- | --- |
| source (swarm) | current `origin/main` | `7eea107b67d56944e883103f4624ceb32198824d` |
| boundary (#4977) | pinned | `ed546227c1233d8171cc758dc30f38505544112f` |
| target (release head) | `upstream/master` | `287a4275794411040eda128c98c92e823cf9aba1` |

`merge-base(swarm, release)` equals the boundary exactly: the two lineages
diverge at #4977, so every target-side commit after it is genuinely post-boundary.
Population digest:
`c5dc7bd6c5711d5adbdc788447ccefc3c7d63684a407995faf9bee05d9c68d6c`.

## Comparison

```text
git cherry <source> <target> <boundary>   (via cargo xtask sync-divergence scaffold/check)
```

22 target-unique non-merge commits. Excluded merge ancestry is enumerated in the
receipt. Validator result on this ledger: **verdict = blocked, errors = 0**
(3 explicitly unresolved rows, 11 terminal rows, 8 release-lineage exclusions).
A nonzero exit with a clean receipt is the correct representation of a
preliminary packet that still owns open decisions; nothing here is greenwashed.

## Dispositions

| Commit | Subject (short) | Disposition |
| --- | --- | --- |
| `0375bf19a9` | action-gh-release bump | release_lineage_only |
| `239942e6d8` | ub-review action bump | release_lineage_only |
| `666981ab55` | docker/login-action bump | release_lineage_only |
| `f4b2033515` | taiki-e/install-action bump | release_lineage_only |
| `1517e39451` | dtolnay/rust-toolchain bump (25 workflows) | release_lineage_only |
| `2819142b0b` | docs(agent) decision shapes | release_lineage_only |
| `846603420b` | docs(semantic) Wave 2 plan copy | release_lineage_only |
| `c636be32cc` | CHANGELOG backfill 0.15.1-0.17.0 | release_lineage_only |
| `1148fcbfa2` | tokio lock bump | deliberately_abandoned (swarm already 1.53.1) |
| `cdd67d0b5b` | serde group lock bump | deliberately_abandoned (identical versions) |
| `ca493c617e` | 9-group lock bump | deliberately_abandoned (all current except two lags below) |
| `939e9b0ee1` | source-map dev bump | deliberately_abandoned (swarm already 0.8.0) |
| `e98e9c714b` | oxfmt/oxlint bumps | deliberately_abandoned (swarm newer: 0.64.0/1.79.0) |
| `0cf55ae9ba` | DAP legacy bridge deprecation | superseded_by_newer_architecture |
| `4f0de95797` | test-source panic ratchet | superseded_by_newer_architecture |
| `3334976917` | installer asset-name + curl-pipe fix | superseded_by_newer_architecture |
| `cd60f9289d` | goals: drop the retired wrapper proof-command dependency | superseded_by_newer_architecture |
| `a3e51152e4` | cache-warm workflow + policy lint registration | superseded_by_newer_architecture |
| `1fe56093a6` | vscode onboarding health-check routing | already_equivalent_in_swarm (`529678c2360e3c6b0c3c8eaf645d90d81d242994`) |

## Explicitly unresolved (drives follow-ups)

1. `71916684b1` **test(lsp): dependency roots reach providers (#3553)** — no
   coverage in `crates/perl-lsp-rs/tests` at source. Pending bounded port PR;
   planned-but-unmerged ports cannot satisfy `port_to_swarm`.
2. `07cf813f6b` / `c4e03ab736` **publication-sync contract (#10088)** — script,
   schema v2, and tests are absent from swarm; decide adoption versus declaring
   swarm's `validate_public_release_claims.py` / `verify-publication-facts.sh`
   stack the owner of the invariant. The changed sets include test paths, so a
   wholesale `release_lineage_only` classification is forbidden by the validator.
3. `blocking_decisions` are carried both per-row and in top-level `blockers`.

## Findings

- **True divergence (dependency currency):** swarm lock lags the release line on
  `regex-automata` (0.4.16 vs 0.4.18). Recorded as a swarm-side upgrade
  follow-up; not hidden behind lineage or abandonment. (`similar` initially
  looked lagged too, but swarm resolves it twice: 2.7.0 transitive via `insta`
  and 3.2.0 direct against xtask's `"3.1"` requirement, so no lag exists there.)
- **DAP:** the only DAP-code row deprecates a legacy bridge/server stack that has
  never existed in swarm's perl-dap (`debug_adapter/`, `backend/` architecture).
  No competing dispatcher implementation appears in this population; convergence
  follows #4348 promotion. Flagged for review rather than a new dap_dispatch ruling.
- **Tooling honesty control that fired:** the validator rejected an earlier draft
  of this ledger because one hand-transcribed entry omitted
  `.github/workflows/workflow-trigger-lint.yml` from its changed paths
  (`changed paths ... do not match Git`). The committed ledger passes with
  errors = 0.

## Non-goals honored

No complete-tree join, no freeze claim, no tag, no publication, no product code
change in this packet. Final transaction-bound comparison belongs to #7983;
publication-context translation to #6356; tree promotion to #4348.
