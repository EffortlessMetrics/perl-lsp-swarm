# Known limitations — compatibility pointer

This page retains compatibility with older links but no longer publishes a frozen limitation inventory or readiness claim. Current parser behavior is maintained in the [parser limitations page](https://github.com/EffortlessMetrics/perl-lsp-swarm/blob/main/docs/reference/PARSER_LIMITATIONS.md), [parser package README](https://github.com/EffortlessMetrics/perl-lsp-swarm/blob/main/crates/perl-parser/README.md), and [current status](https://github.com/EffortlessMetrics/perl-lsp-swarm/blob/main/docs/project/CURRENT_STATUS.md).

## 1. Source filters

See the current parser limitations page for the maintained explanation.

## 2. Runtime code generation

See the current parser limitations page for the maintained explanation.

## 3. Dynamic symbol table manipulation

See the current parser limitations page for the maintained explanation.

This file is copied into mdBook during deployment; repository-absolute links keep the compatibility page valid.
