# Workspace navigation — current-evidence pointer

This guide retires the former navigation narrative and its unsupported coverage and stability claims. Current package ownership and workspace boundaries are maintained in the [architecture overview](https://github.com/EffortlessMetrics/perl-lsp-swarm/blob/main/docs/reference/ARCHITECTURE.md), [workspace manifest](https://github.com/EffortlessMetrics/perl-lsp-swarm/blob/main/Cargo.toml), and relevant package READMEs.

The page is copied into mdBook during deployment, so repository-absolute links keep both source and published copies valid.
