---
version: "0.13.1"
tag: "v0.13.1"
release_date_utc: "2026-05-01"
previous_tag: "v0.13.0-rc1"
compare: "https://github.com/EffortlessMetrics/perl-lsp/compare/v0.13.0-rc1...v0.13.1"
notes_status: canonical
release_track: public-alpha
release_kind: patch
channels:
  crates_io: pending
  vscode_marketplace: "EffortlessMetrics.perl-lsp-rs"
  open_vsx: pending
  docker: pending
---

# v0.13.1

## Summary

Public-alpha patch release for release-channel hygiene, naming cleanup, and
package-manager correctness after the `v0.13.0-rc1` rehearsal.

No final `v0.13.0` tag was cut. The tagged release line moved directly from
`v0.13.0-rc1` to `v0.13.1`; use that range for source comparison and historical
accounting.

## Highlights

- **Release channels hardened** - Marketplace, Open VSX, and GitHub VSIX
  reporting stay independent and visible.
- **Homebrew tap aligned** - the owned tap path uses the `perllsp` formula name
  and validates release assets before opening a bump PR.
- **Release naming aligned** - package versions use normal SemVer while the
  product posture remains public alpha.
- **CI Gate diagnostics improved** - release-blocking timeouts are classified as
  gate pressure or hung tests instead of ambiguous runner cancellation.

## Release boundary

`v0.13.1` is the first final SemVer tag after the `0.13.0-rc1` release
candidate. References in older documentation to a completed `v0.13.0` launch
were planning shorthand, not a distinct tagged release.

## Install / upgrade

- **VS Code**: install or update the
  [Perl Language Server](https://marketplace.visualstudio.com/items?itemName=EffortlessMetrics.perl-lsp-rs)
  extension. The managed `perllsp` binary is part of the public-alpha line.
- **Homebrew tap**: install with `brew install effortlessmetrics/tap/perllsp`.
  This uses the owned EffortlessMetrics tap, not Homebrew/core.
- **Package managers**: Windows package-manager
  manifests describe public-alpha artifacts. Verify with `perllsp --version`
  after updating.
- **Source/Binary**: download release assets or use `cargo install perllsp`
  only for the published public-alpha line.

## Which file should I download?

Most Linux users should choose `gnu`. Use `musl` mainly for Alpine Linux or
musl-based containers. You do not need both GNU and musl archives.

| Your system | Download suffix |
|---|---|
| Linux x64 / AMD64, most distributions | `x86_64-unknown-linux-gnu` |
| Linux ARM64, most distributions | `aarch64-unknown-linux-gnu` |
| Linux x64 / AMD64, Alpine or musl containers | `x86_64-unknown-linux-musl` |
| Linux ARM64, Alpine or musl containers | `aarch64-unknown-linux-musl` |
| macOS Intel | `x86_64-apple-darwin` |
| macOS Apple Silicon | `aarch64-apple-darwin` |
| Windows x64 | `x86_64-pc-windows-msvc` |

## Links

- [Compare v0.13.0-rc1...v0.13.1](https://github.com/EffortlessMetrics/perl-lsp/compare/v0.13.0-rc1...v0.13.1)
- [Previous tagged release candidate](v0.13.0-rc1.md)
- [CHANGELOG](../../CHANGELOG.md)
- [Release ledger](../../RELEASE_HISTORY.md)
