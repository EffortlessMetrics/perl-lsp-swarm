---
version: "0.15.2"
tag: "v0.15.2"
tag_commit: "746edcb78fe0fa8f48d87386fd4f110502588a87"
release_date_utc: "2026-05-26"
previous_tag: "v0.15.1"
compare: "https://github.com/EffortlessMetrics/perl-lsp/compare/v0.15.1...v0.15.2"
github_release: "https://github.com/EffortlessMetrics/perl-lsp/releases/tag/v0.15.2"
notes_status: canonical
release_track: public-alpha
release_kind: patch
channels:
  github_release: "published 2026-05-26; see canonical release page and closeout receipt"
  crates_io: "0.15.2 packages visible; cargo install perllsp/perl-dap smoke passed"
  vscode_marketplace: "0.15.2 verified"
  open_vsx: "0.15.2 verified"
  docker: "Docker Hub amd64/arm64 verified; GHCR builder/runtime tags exist but are arm64-only"
---

# v0.15.2

## Summary

Hotfix release: restores `cargo install perllsp` / `cargo install perl-dap`
functionality that was broken in 0.15.1 due to a missing file in the
published `perl-lsp-rs-core` crate package.

## Highlights

### For users

- **`cargo install perllsp` and `cargo install perl-dap` work again.**
  The `0.15.1` crates.io publish omitted `build_catalog.rs` from the
  `perl-lsp-rs-core` package, causing `cargo install` to fail with a
  missing-file build error. This patch includes the file and adds a CI
  gate to prevent recurrence.

### For contributors

- **Package-content gate for `perl-lsp-rs-core`** — CI now checks the
  `.crate` archive file list, extracts it, and runs `cargo check --locked`
  on the unpacked crate before any publish step. (#9613)

## Release verification

- The [v0.15.2 GitHub Release](https://github.com/EffortlessMetrics/perl-lsp/releases/tag/v0.15.2) is published with
  platform binaries, checksums, SBOM, and VSIX artifact.
- crates.io reports `perllsp`, `perl-dap`, and `perl-lsp-rs-core` at `0.15.2`;
  fresh `cargo install` smokes for `perllsp` and `perl-dap` passed, and the
  installed `perllsp` binary passed the inline-completion stdio smoke.
- VS Code Marketplace and Open VSX report extension version `0.15.2`; both
  published-extension smokes passed after the GitHub Release artifact was
  available.
- Docker Hub builder and runtime tags expose both `linux/amd64` and
  `linux/arm64`. The corresponding GHCR tags exist, but authenticated manifest
  inspection shows only `linux/arm64`; `linux/amd64` is missing from both live
  indexes.

## Claim boundary

This release fixes the crates.io install surface only. No user-visible
behavior changes relative to 0.15.1.

## Related

- GitHub Release: [https://github.com/EffortlessMetrics/perl-lsp/releases/tag/v0.15.2](https://github.com/EffortlessMetrics/perl-lsp/releases/tag/v0.15.2)
- Previous release: [v0.15.1](v0.15.1.md)
- Hotfix commit: `746edcb78fe0fa8f48d87386fd4f110502588a87`
- Closeout receipt: [0.15.2 closeout audit](0.15.2-closeout-audit.md)
