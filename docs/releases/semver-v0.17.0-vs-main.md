# v0.18 SemVer waived-break inventory (v0.17.0 → current main)

Generated by `cargo semver-checks check-release` against the `v0.17.0` git tag for every crate listed in `.ci/public-api-baselines/ratchet-crates.txt`.

This inventory captures the major-level SemVer breaks that the 0.18 release is *absorbing* (not blocking). After 0.18 ships, any new major break reported against the `v0.18.x` baseline must either be reverted, versioned into a 0.19 minor bump, or added to a successor waived inventory — the regression gate that consumes this file (see #15269 follow-up) will fail the build otherwise.

## Summary

| Crate | Status | Checks (pass / fail / warn / skip) | Major-level breaks |
|---|---|---|---|
| `perl-lsp-rs` | ❌ 11 major break(s) | 185 / 11 / 0 / 49 | 11 |
| `perllsp` | ✅ pass | 196 / 0 / 0 / 49 | 0 |
| `perl-parser` | ❌ 3 major break(s) | 193 / 3 / 0 / 49 | 3 |
| `perl-dap` | ❌ 8 major break(s) | 188 / 8 / 0 / 49 | 8 |
| `perl-uri` | ✅ pass | 196 / 0 / 0 / 49 | 0 |
| `perl-lexer` | ❌ 7 major break(s) | 189 / 7 / 0 / 49 | 7 |
| `perl-parser-core` | ❌ 12 major break(s) | 184 / 12 / 0 / 49 | 12 |
| `perl-module` | ❌ 4 major break(s) | 192 / 4 / 0 / 49 | 4 |
| `perl-semantic-facts` | ✅ pass | 196 / 0 / 0 / 49 | 0 |
| `perl-source-identity` | ⚠️  tool error (exit 1) | 0 / 0 / 0 / 0 | 0 |
| `perl-symbol` | ✅ pass | 196 / 0 / 0 / 49 | 0 |
| `perl-lsp-rs-core` | ⚠️  tool error (exit 1) | 0 / 0 / 0 / 0 | 0 |

Totals: 6 crate(s) with major breaks, 4 passing, 2 with tool errors.
Total major-level breaks absorbed by 0.18: **45**.

## Per-crate waived breaks

### `perl-lsp-rs` — 11 waived break(s)

Raw tool output: `target/semver-reports/per-crate/perl-lsp-rs.txt`

1. **auto_trait_impl_removed** — auto trait no longer implemented
2. **constructible_struct_adds_field** — externally-constructible struct adds field
3. **constructible_struct_adds_private_field** — struct no longer constructible due to new private field
4. **enum_missing** — pub enum removed or renamed
5. **feature_missing** — package feature removed or renamed
6. **function_missing** — pub fn removed or renamed
7. **inherent_method_missing** — pub method removed or renamed
8. **method_requires_different_generic_type_params** — method now requires a different number of generic type parameters
9. **module_missing** — pub module removed or renamed
10. **struct_missing** — pub struct removed or renamed
11. **struct_pub_field_missing** — pub struct's pub field removed or renamed

### `perl-parser` — 3 waived break(s)

Raw tool output: `target/semver-reports/per-crate/perl-parser.txt`

1. **constructible_struct_adds_field** — externally-constructible struct adds field
2. **struct_marked_non_exhaustive** — struct marked #[non_exhaustive]
3. **struct_pub_field_missing** — pub struct's pub field removed or renamed

### `perl-dap` — 8 waived break(s)

Raw tool output: `target/semver-reports/per-crate/perl-dap.txt`

1. **constructible_struct_adds_field** — externally-constructible struct adds field
2. **enum_variant_added** — enum variant added on exhaustive enum
3. **enum_variant_missing** — pub enum variant removed or renamed
4. **feature_missing** — package feature removed or renamed
5. **inherent_method_missing** — pub method removed or renamed
6. **method_receiver_ref_became_mut** — method receiver changed from immutable to mutable reference
7. **module_missing** — pub module removed or renamed
8. **struct_missing** — pub struct removed or renamed

### `perl-lexer` — 7 waived break(s)

Raw tool output: `target/semver-reports/per-crate/perl-lexer.txt`

1. **constructible_struct_adds_private_field** — struct no longer constructible due to new private field
2. **enum_marked_non_exhaustive** — enum marked #[non_exhaustive]
3. **enum_unit_variant_changed_kind** — An enum unit variant changed kind
4. **enum_variant_added** — enum variant added on exhaustive enum
5. **struct_pub_field_missing** — pub struct's pub field removed or renamed
6. **trait_method_added** — pub trait method added
7. **trait_method_return_value_added** — pub trait method return value added

### `perl-parser-core` — 12 waived break(s)

Raw tool output: `target/semver-reports/per-crate/perl-parser-core.txt`

1. **constructible_struct_adds_field** — externally-constructible struct adds field
2. **enum_marked_non_exhaustive** — enum marked #[non_exhaustive]
3. **enum_no_repr_variant_discriminant_changed** — enum variant had its discriminant change value
4. **enum_struct_variant_field_added** — pub enum struct variant field added
5. **enum_variant_added** — enum variant added on exhaustive enum
6. **enum_variant_missing** — pub enum variant removed or renamed
7. **function_missing** — pub fn removed or renamed
8. **module_missing** — pub module removed or renamed
9. **struct_marked_non_exhaustive** — struct marked #[non_exhaustive]
10. **struct_missing** — pub struct removed or renamed
11. **struct_pub_field_missing** — pub struct's pub field removed or renamed
12. **type_mismatched_generic_lifetimes** — type now takes a different number of generic lifetimes

### `perl-module` — 4 waived break(s)

Raw tool output: `target/semver-reports/per-crate/perl-module.txt`

1. **enum_missing** — pub enum removed or renamed
2. **function_missing** — pub fn removed or renamed
3. **module_missing** — pub module removed or renamed
4. **struct_missing** — pub struct removed or renamed

## Tool errors

- `perl-source-identity`: cargo semver-checks exit 1; see `target/semver-reports/per-crate/perl-source-identity.txt` for raw output.
- `perl-lsp-rs-core`: cargo semver-checks exit 1; see `target/semver-reports/per-crate/perl-lsp-rs-core.txt` for raw output.

## Reproduction

```powershell
# Per-crate (mirrors `just semver-check-package`):
cargo semver-checks check-release -p <crate> --baseline-rev v0.17.0

# Full inventory regeneration (writes this file + per-crate raw output):
python tools/parse_semver_checks.py --out-dir docs/releases
```

## Provenance

- Baseline tag: `v0.17.0` (materialized by #15263).
- Current main SHA at regeneration: captured in the `head_sha` field of the companion JSON, or `git rev-parse HEAD` of the regenerating checkout.
- Tool: `cargo-semver-checks` v0.46.0 (0.47.0 when installed via `just _semver-check-install`).
- Ratchet crate list: `.ci/public-api-baselines/ratchet-crates.txt` (#14607).

## Follow-ups

- Wire this inventory into the release gate so post-0.18 drift is enforced (separate issue — does not belong in this PR per #15269).
- Reconcile `.cargo-semver-checks.toml [[packages]]` with `.ci/public-api-baselines/ratchet-crates.txt` (#14961).
