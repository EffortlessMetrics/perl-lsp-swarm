<!-- auto-generated by `cargo xtask compat-inventory`; do not edit -->

# `perl-tree-sitter-compat` consumer and behavior inventory

Generated from `policy/tree-sitter-compat-inventory.toml` reconciled against the crate's real surface. That file is
the authority; this page is a projection of it. Edit the ledger and run
`cargo xtask compat-inventory`.

Controlling issue #8880, train #8877 row PR-09. Migration is #8889; crate removal is #8890.
This inventory changes no compatibility behavior and removes no source.

## Source binding

The dispositions below were audited against this exact source. A change to any file
listed here invalidates the audit and the task fails until the rows are re-checked.

- Digest: `sha256:8808149a51e4a706dea655c3d7c4e015f3653746c3179d2777f62c68444d7590`
- Files (7):
  - `crates/perl-tree-sitter-compat/Cargo.toml`
  - `crates/perl-tree-sitter-compat/src/convert.rs`
  - `crates/perl-tree-sitter-compat/src/highlight.rs`
  - `crates/perl-tree-sitter-compat/src/lib.rs`
  - `crates/perl-tree-sitter-compat/src/node.rs`
  - `crates/perl-tree-sitter-compat/src/sexp.rs`
  - `crates/perl-tree-sitter-compat/tests/adapter.rs`

## Cargo dependent evidence

No workspace manifest declares a normal, dev, or build dependency on
`perl-tree-sitter-compat`. This is the evidence that permits an `unused` disposition; a text
search alone could not establish it.

## Disposition summary

| Disposition | Symbols | References |
| --- | ---: | ---: |
| `already_equivalent` | 2 | 0 |
| `migration_only_entry_point` | 0 | 25 |
| `unique_and_required` | 9 | 0 |
| `unique_but_invalid_or_obsolete` | 1 | 0 |
| `unused` | 2 | 3 |

## Symbols

Every `pub` and restricted-visibility item the crate's modules declare. Internal symbols
(0 here) are reachable only in-crate, so they retire with the crate rather than
migrating; they are recorded rather than skipped so the surface has no unexamined
remainder.

| Symbol | Kind | Visibility | Root re-export | Disposition | Canonical owner | Fixture | Migration | Removal condition | Note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `convert::TreeError` | `enum` | `public` | yes | `unique_and_required` | #8790 | crates/perl-tree-sitter-compat/tests/adapter.rs::parse_failures_at_different_offsets_differ_by_native_diagnostic | #8889 | Retires when #8790 projects parser-evidence-backed error/missing/recovery state and #8889 moves the offset/kind mapping onto it. | Maps native ParseError to a compat parse failure carrying offset and kind. No other crate projects native parse failure into a Tree-sitter-shaped error. Its Display and std::error::Error impls migrate with it: they are public behaviour (pinned by convert.rs::tree_error_displays_readably) that this inventory records here rather than as separate rows, because discovery covers declarations, not trait impls — see the boundary note at the head of this file. |
| `convert::parse_to_tree` | `function` | `public` | yes | `unique_and_required` | #8751 | crates/perl-tree-sitter-compat/tests/adapter.rs::sexp_round_trips_a_realistic_snippet | #8889 | Retires when #8751 constructs the immutable CompatTree from an exact parser snapshot and #8889 repoints callers at it. | Named by docs/explanation/TREE_SITTER_COMPATIBILITY.md as part of the current Tier-A surface. Entry point that turns Perl source into a Tree-sitter-shaped tree. |
| `convert::to_ts_node` | `function` | `public` | yes | `unique_and_required` | #8751 | crates/perl-tree-sitter-compat/tests/adapter.rs::nodes_carry_byte_and_point_ranges | #8889 | Retires when #8751 owns native-AST-to-compat-node construction and #8926 geometry supplies the byte/point ranges. | Named by docs/explanation/TREE_SITTER_COMPATIBILITY.md as part of the current Tier-A surface. Converts one native AST node, carrying byte offsets and UTF-8 points. The fixture exercises it transitively: adapter.rs never imports to_ts_node, but parse_to_tree reaches it for every node, and nodes_carry_byte_and_point_ranges asserts the ranges it produces. |
| `highlight::Highlight` | `struct` | `public` | yes | `unique_and_required` | #8883 | crates/perl-tree-sitter-compat/tests/adapter.rs::highlights_cover_keywords_variables_and_literals | #8889 | Retires when #8883 versions the canonical highlights query set and #8889 moves capture emission onto it. | Byte-range plus capture name. The only highlight capture record in the workspace; tree-sitter-perl-rs carries no highlight surface. |
| `highlight::capture_for` | `function` | `public` | yes | `unique_and_required` | #8883 | crates/perl-tree-sitter-compat/tests/adapter.rs::highlights_cover_keywords_variables_and_literals | #8889 | Retires when #8883 owns the node-kind-to-capture mapping as a query set rather than a match arm. | Node-kind to capture-name mapping, exercised transitively through highlights. Hand-written today; #8883 replaces it with a canonical query. |
| `highlight::highlights` | `function` | `public` | yes | `unique_and_required` | #8883 | crates/perl-tree-sitter-compat/tests/adapter.rs::highlights_cover_keywords_variables_and_literals | #8889 | Retires when #8883 executes canonical highlight captures over the compat tree. | Named by docs/explanation/TREE_SITTER_COMPATIBILITY.md as part of the current Tier-A surface. Walks the tree and emits node-granular captures. |
| `node::TsNode` | `struct` | `public` | yes | `unique_and_required` | #8803 | crates/perl-tree-sitter-compat/tests/adapter.rs::serializes_to_json | #8889 | Retires when #8803 lands the public Tree/Node/TreeCursor cutover and #8889 moves the serializable shape onto it. | The owned, serializable compat node. tree-sitter-perl-rs::Node borrows from a live tree and is not Serialize, so this shape is not already provided. |
| `node::TsNode::child` | `method` | `public` | no | `already_equivalent` | #8803 | — | — | Retires with TsNode; no behavior needs porting because tree-sitter-perl-rs::Node::child already exposes the same indexed child navigation. | tree-sitter-perl-rs/src/lib.rs:688 provides Node::child with the same semantics over the canonical tree representation. |
| `node::TsNode::child_count` | `method` | `public` | no | `already_equivalent` | #8803 | — | — | Retires with TsNode; tree-sitter-perl-rs::Node::child_count already provides it. | tree-sitter-perl-rs/src/lib.rs:683 provides Node::child_count with the same semantics. |
| `node::TsNode::descendant_count` | `method` | `public` | no | `unused` | — | — | — | — | Unique to this crate — tree-sitter-perl-rs has no descendant_count — but no consumer links against it and no documented Tier-A claim names it. It does have a crate-local unit test (node.rs::descendant_count_walks_the_subtree), which retires with the crate; what it lacks is a consumer and a product claim, not coverage. Incidental API. If #8803 later needs a subtree size it should derive one from the canonical tree rather than port this. |
| `node::TsPoint` | `struct` | `public` | yes | `unique_and_required` | #9526 | crates/perl-tree-sitter-compat/tests/adapter.rs::serializes_to_json | #8889 | Retires when #9526 becomes the single Tree-sitter byte/Point/range authority AND that authority carries a serializable point type. Deleting this before then breaks TsNode's derived Serialize. | The row/UTF-8-column semantics match tree-sitter-perl-rs::Point (src/lib.rs:83), but the shape is not already provided: that crate has no serde dependency at all, and its Point is #[non_exhaustive], so it is neither Serialize nor externally constructible. TsPoint is a field type of TsNode, whose derived Serialize — pinned by serializes_to_json — requires it. Same reasoning as the TsNode row; recording this as already_equivalent would let #8890 delete a serializable point type nothing replaces. |
| `node::pascal_to_snake` | `function` | `public` | yes | `unique_but_invalid_or_obsolete` | #8708 | crates/perl-tree-sitter-compat/tests/adapter.rs::sexp_round_trips_a_realistic_snippet | #8889 | The implementation must not be carried across — a name transform cannot be the node-kind authority — but the naming behaviour is load-bearing and must not be dropped with it. #8708's generated registry has to produce the kind names first; only then may #8889 repoint to_ts_node at it and #8890 delete this function. | Derives tree-sitter node-kind names from native NodeKind spelling. The crate's own CLAUDE.md flags this as the seam most likely to silently diverge from real grammars when upstream variants are added, which is precisely why #8708 supersedes the heuristic rather than adopting it. Obsolete as an authority, but not free-standing: convert.rs:92 sets TsNode.kind from it, so the snake_case names pinned by sexp_round_trips_a_realistic_snippet are this function's output. It therefore carries a migration dependency despite being obsolete — deleting it without the registry replacement would silently change required to_ts_node behaviour. |
| `sexp::to_sexp` | `function` | `public` | yes | `unique_and_required` | #9160 | crates/perl-tree-sitter-compat/tests/adapter.rs::deterministic_across_parses | #8889 | Retires when #9160 cuts the compatibility S-expression output over to the exact CompatTree produced by #8803. | Named by docs/explanation/TREE_SITTER_COMPATIBILITY.md as part of the current Tier-A surface. Not already equivalent: tree-sitter-perl-rs::Node::to_sexp documents itself at src/lib.rs:675 as a non-normative debug projection and explicitly defers compatibility serialization to this crate. |
| `sexp::to_sexp_pretty` | `function` | `public` | yes | `unused` | — | — | — | — | Indented variant of to_sexp. No consumer links against it and the documented Tier-A surface (TREE_SITTER_COMPATIBILITY.md) names only to_sexp. It has a crate-local unit test (sexp.rs::pretty_indents_children) that retires with the crate, and the crate README does advertise it alongside to_sexp — that README claim is inside the crate being removed and is not a Tier-A product claim, so it does not make the function required. Incidental debug convenience. |

## References

Every tracked file that mentions `perl-tree-sitter-compat` or `perl_tree_sitter_compat`, excluding this
inventory's own artifacts. An unexplained reference fails the task.

| Path | Reference kind | Disposition | Canonical owner | Removal condition | Note |
| --- | --- | --- | --- | --- | --- |
| `.ci/gate-policy.yaml` | `policy` | `migration_only_entry_point` | #8890 | The `-p perl-tree-sitter-compat` gate argument is removed when the crate is deleted. | Builds and tests the crate in a CI gate. This is an operational reference, not a code dependency: it names the package to cargo, nothing links against it. |
| `.ci/policies/agent-context-policy.toml` | `policy` | `migration_only_entry_point` | #8890 | The package name is dropped from the tracked-context comment when the crate is deleted. | Names the crate in a comment listing packages tracked for agent-context coverage. |
| `.spec/11246-clippy-map-err-ignore-boundaries/denominator.csv` | `documentation` | `unused` | — | — | Historical denominator row recorded by the #11246 clippy audit, noting that TreeError::ParseFailed drops parser error position. A record of a past measurement; it needs no edit when the crate is removed. |
| `Cargo.lock` | `lockfile` | `migration_only_entry_point` | #8890 | Regenerated when the crate is deleted from the workspace. | Lockfile entry for the workspace member. |
| `Cargo.toml` | `workspace_manifest` | `migration_only_entry_point` | #8890 | The workspace member entry and the workspace.dependencies row are removed when the crate is deleted. | Declares the crate as a workspace member and publishes a workspace dependency alias. The alias is unused: no member manifest references it. |
| `crates/perl-tree-sitter-compat/CLAUDE.md` | `own_crate` | `migration_only_entry_point` | #8890 | Deleted with the crate. | Package-local owner guidance for the crate itself. |
| `crates/perl-tree-sitter-compat/Cargo.toml` | `own_crate` | `migration_only_entry_point` | #8890 | Deleted with the crate. | The crate's own manifest. Not counted as a Cargo dependent: a package is not a consumer of itself. |
| `crates/perl-tree-sitter-compat/README.md` | `own_crate` | `migration_only_entry_point` | #8890 | Deleted with the crate. | The crate's own README. |
| `crates/perl-tree-sitter-compat/src/lib.rs` | `own_crate` | `migration_only_entry_point` | #8890 | Deleted with the crate. | Crate root declaring the four modules and the eleven root re-exports. |
| `crates/perl-tree-sitter-compat/tests/adapter.rs` | `own_crate` | `migration_only_entry_point` | #8889 | Migrates rather than being deleted outright: it is the named fixture for every unique_and_required row above, so #8889 must carry its assertions onto the canonical surface before #8890 removes the file. | The crate's only test file. Seven tests covering S-expression shape, byte/point ranges, highlight captures, JSON round-trip, determinism, parse-failure discrimination, and error recovery. |
| `crates/perl-workspace-core/CLAUDE.md` | `documentation` | `migration_only_entry_point` | #8890 | The downstream-consumer sentence is corrected when the crate is deleted. | Names this crate as perl-workspace-core's only current downstream consumer. That claim is upstream-facing: it describes who uses perl-workspace-core, not who uses this crate. |
| `crates/perl-workspace-core/README.md` | `documentation` | `migration_only_entry_point` | #8890 | The consumer reference is corrected when the crate is deleted. | Same upstream-facing claim as the neighbouring CLAUDE.md. |
| `crates/tree-sitter-perl-rs/src/lib.rs` | `documentation` | `migration_only_entry_point` | #8803 | The deferral comment is resolved when #8803 lands compatibility serialization on the canonical tree. | A doc comment on Node::to_sexp, not an import: it records that Node::to_sexp is a non-normative debug projection and defers compatibility serialization to this crate under issue 8047. This is the evidence that sexp::to_sexp is not already equivalent. |
| `docs/adr/PLSP-ADR-0006-perl-workspace-core-facts-substrate.md` | `documentation` | `migration_only_entry_point` | #8890 | The ADR records the crate's retirement when it is deleted. | The ADR that introduced this adapter as PR 9 of the facts-substrate programme. Normative history; it must record the outcome rather than be edited away. |
| `docs/architecture/repository-topology.md` | `documentation` | `migration_only_entry_point` | #8890 | Regenerated by `cargo xtask repo-topology` after the package row is removed. | Generated projection of policy/repository-topology.toml carrying the crate's ownership row. |
| `docs/explanation/TREE_SITTER_COMPATIBILITY.md` | `documentation` | `migration_only_entry_point` | #9171 | Reconciled by #9171 when public wording is cut over to the generated compatibility status. | The public claim surface. Names parse_to_tree, to_ts_node, highlights, and to_sexp as the current Tier-A surface, which is the evidence separating the required rows above from incidental API. |
| `docs/policy/NON_RUST_INVENTORY.md` | `documentation` | `migration_only_entry_point` | #8890 | Regenerated by `cargo xtask non-rust inventory --write` after the crate's files are deleted. | Generated tracked-file inventory listing the crate's non-Rust files. |
| `docs/project/status/quality.md` | `documentation` | `migration_only_entry_point` | #8890 | Regenerated after the crate is deleted. | Generated quality status table carrying a per-crate row for the package. |
| `docs/reference/CHANGE_SET_SHADOW_PARITY.md` | `documentation` | `unused` | — | — | A recorded parity comparison between two change-set classifiers. The package name appears inside captured reverse-dependency closures from a past observation, not as a live claim; the record needs no edit when the crate is removed. |
| `docs/reference/NATIVE_STACK_POLICY.md` | `documentation` | `migration_only_entry_point` | #9171 | Reconciled by #9171 with the rest of the public wording cutover. | Policy prose describing the native stack and where compatibility output lives. |
| `docs/reviews/3313-deep-review.md` | `documentation` | `unused` | — | — | Historical deep review of PR #3313, which introduced perl-workspace-core and this crate. A dated record of a past review; it needs no edit when the crate is removed. |
| `docs/specs/protocol-type-substrate-matrix.json` | `documentation` | `migration_only_entry_point` | #8890 | Regenerated by `cargo xtask generate-protocol-type-substrate-matrix` after the crate is deleted. | Generated substrate matrix carrying a row for the package. |
| `docs/specs/protocol-type-substrate-matrix.md` | `documentation` | `migration_only_entry_point` | #8890 | Regenerated alongside its JSON counterpart after the crate is deleted. | Markdown projection of the substrate matrix. |
| `policy/repository-topology.toml` | `policy` | `migration_only_entry_point` | #8890 | The package row is removed when the crate is deleted; #7400 owns the placement question until then. | Ownership authority row recording placement as pending with migration owner #7400 and a private workspace publish disposition. |
| `policy/ripr-suppressions.toml` | `policy` | `migration_only_entry_point` | #8890 | The suppression is deleted when the crate is deleted, or earlier if a production entrypoint consumes it. | Suppresses reachability analysis on the grounds that the crate is not wired into any production binary. That stated removal criterion is independent evidence for the zero-consumer finding. |
| `xtask/src/main.rs` | `tooling` | `migration_only_entry_point` | #8890 | The compat-inventory subcommand is removed when the crate it inventories is deleted. | Registers the `cargo xtask compat-inventory` subcommand that maintains this ledger. |
| `xtask/src/tasks/check_agent_context.rs` | `tooling` | `migration_only_entry_point` | #8890 | The package name is dropped from the tracked-context list when the crate is deleted. | Names the package in an agent-context coverage list. Operates on the package by name; it does not depend on the crate. |
| `xtask/src/tasks/compat_inventory.rs` | `tooling` | `migration_only_entry_point` | #8890 | Deleted with the crate it inventories, once #8889 and #8890 have consumed the ledger. | The inventory generator and validator itself. It names the package as data, not as a dependency. |
