project: vscode
component: Editor behavior
kind: Changed
body: Workspace status now separates the product name from runtime server state and reports the observed server identity only from a live version probe, preserved verbatim and single-line instead of asserting an executable the snapshot never established.
time: 2026-08-26T00:00:00Z
custom:
  PR: 6869
  Slug: vscode-observed-server-status
  Breaking: "no"
