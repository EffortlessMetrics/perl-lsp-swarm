project: product
component: Language intelligence
kind: Added
body: >-
  Write the lexer budget-stop recovery contract into the crate docs and pin
  every reachable over-budget token shape with five executable contract tests:
  empty-text UnknownRest over the degraded remainder through EOF for regex scan
  steps, regex bytes, and heredoc bytes. Document MAX_DELIM_NEST as a separate
  local quote-like recovery path and test that production reachability. Also
  pin the two bounded-payload exceptions of EOF-inside-budget heredoc recovery
  and the MAX_HEREDOC_DEPTH heredoc-header error.
time: 2026-08-30T00:00:00Z
custom:
  PR: "14362"
  Slug: budget-stop-recovery-contract
  Breaking: "no"
