Registration for #9769 (lands with PR #12289): `LspServer::session_warning_dedup` — typed bounded session-warning dedup store replacing the three unbounded `HashSet<String>` warning sets.

```
classification = provider_or_presentation / operational-session
semantic authority = none
lifecycle = server session + applicable root/config/backend subject
persistence = never
hard bound = explicit (32 fixed-size identities per family; saturation emits without retaining)
privacy = no raw secret/value/path payload (FNV-1a fingerprint identity only)
proof-domain disposition = pressure_only
```

Durable registration surface while the #9155 machine-readable registry is pending: `docs/large-workspaces/RETAINED_STATE_INVENTORY.md` (new row + memory budget entry). Pressure/bound counters for the #9183 row are exposed as `LspServer::session_warning_dedup_snapshot()` per family: `entries`, `high_water_entries`, `inserted`, `suppressed`, `emitted_without_retaining`, `cleared_by_lifecycle`.
