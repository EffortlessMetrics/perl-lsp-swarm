# Frozen native Rust CPANTS compatibility catalog v1 (#7170).
# Do not add indicator implementations, CLI, archive loading, or network lookup here.
# Source: Module::CPANTS::Analyse 1.03 and Module::CPANTS::SiteKwalitee (2025-01-18 tree).
# Per-fixture cascade expectations live in distribution_kwalitee_fixtures.v1.toml.

schema_version = 1
kind = "distribution_kwalitee.catalog"
catalog_version = "v1"
status = "frozen"
scoring_rule = "unweighted_applicable_offline_core"
cpants_analyse_version = "1.03"
cpants_site_kwalitee_ref = "github.com/cpants/Module-CPANTS-SiteKwalitee@2025-01-18"
production_runtime = "native_rust_offline"
oracle_role = "test_only_pinned_cpants"

[[metric]]
id = "cpants.has_readme"
alias = "has_readme"
title = "Distribution has a README"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Files.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "A README-like file is present at the distribution root."
fail_semantics = "No README-like file is present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 7178
fixture_ids = ["Acme-CatalogFreeze", "defect_has_readme"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.has_manifest"
alias = "has_manifest"
title = "Distribution has a MANIFEST"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Files.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "A MANIFEST file is present at the distribution root."
fail_semantics = "No MANIFEST file is present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 7178
fixture_ids = ["Acme-CatalogFreeze", "defect_has_manifest"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.has_meta_yml"
alias = "has_meta_yml"
title = "Distribution has META.yml"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Files.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "META.yml is present, or a local distribution exposes MYMETA.yml."
fail_semantics = "Neither META.yml nor a permitted local MYMETA.yml is present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 7178
fixture_ids = ["Acme-CatalogFreeze", "defect_has_meta_yml"]
known_differences = ["CPANTS treats MYMETA.yml as satisfying this metric only for local distributions."]
limitations = []

[[metric]]
id = "cpants.has_meta_json"
alias = "has_meta_json"
title = "Distribution has META.json"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Files.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "META.json is present, or a local distribution exposes MYMETA.json."
fail_semantics = "Neither META.json nor a permitted local MYMETA.json is present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 7178
fixture_ids = ["Acme-CatalogFreeze", "defect_has_meta_json"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.has_buildtool"
alias = "has_buildtool"
title = "Distribution has a recognised build tool"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Files.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "Makefile.PL or Build.PL is present."
fail_semantics = "Neither Makefile.PL nor Build.PL is present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 7178
fixture_ids = ["Acme-CatalogFreeze", "defect_has_buildtool"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.has_changelog"
alias = "has_changelog"
title = "Distribution has a changelog"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Files.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "A changelog-like filename matches m/^chang(es?|log)|history$/i."
fail_semantics = "No changelog-like file is present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 7178
fixture_ids = ["Acme-CatalogFreeze", "defect_has_changelog"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.no_files_to_be_skipped"
alias = "no_files_to_be_skipped"
title = "No MANIFEST.SKIP-ignored files are shipped"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Files.pm"
required_facts = ["staged_files", "manifest_skip"]
applicability = "all_distributions"
pass_semantics = "No shipped path matches MANIFEST.SKIP."
fail_semantics = "At least one shipped path matches MANIFEST.SKIP."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = ["cpants.has_manifest"]
remediation_owner = "distribution_author"
implementation_owner = 7178
fixture_ids = ["Acme-CatalogFreeze", "defect_no_files_to_be_skipped"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.no_symlinks"
alias = "no_symlinks"
title = "Distribution contains no symlinks"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Files.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "The staged tree contains no symbolic links."
fail_semantics = "At least one symbolic link is present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 7178
fixture_ids = ["Acme-CatalogFreeze", "defect_no_symlinks"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.has_tests"
alias = "has_tests"
title = "Distribution has tests"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Files.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "At least one test file is present (t/, test.pl, or similar)."
fail_semantics = "No test files are present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 7178
fixture_ids = ["Acme-CatalogFreeze", "defect_has_tests"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.has_tests_in_t_dir"
alias = "has_tests_in_t_dir"
title = "Tests live under t/"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Files.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "At least one test lives under t/."
fail_semantics = "Tests exist only outside t/."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = ["cpants.has_tests"]
remediation_owner = "distribution_author"
implementation_owner = 7178
fixture_ids = ["Acme-CatalogFreeze", "defect_has_tests_in_t_dir"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.no_stdin_for_prompting"
alias = "no_stdin_for_prompting"
title = "Build files do not read STDIN for prompts"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Files.pm"
required_facts = ["staged_files", "build_file_text"]
applicability = "all_distributions"
pass_semantics = "Makefile.PL/Build.PL do not prompt on STDIN."
fail_semantics = "A build file prompts on STDIN."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = ["cpants.has_buildtool"]
remediation_owner = "distribution_author"
implementation_owner = 7178
fixture_ids = ["Acme-CatalogFreeze", "defect_no_stdin_for_prompting"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.no_maniskip_error"
alias = "no_maniskip_error"
title = "MANIFEST.SKIP parses"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Files.pm"
required_facts = ["manifest_skip"]
applicability = "all_distributions"
pass_semantics = "MANIFEST.SKIP is absent or parses."
fail_semantics = "MANIFEST.SKIP is present and does not parse."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 7178
fixture_ids = ["Acme-CatalogFreeze", "defect_no_maniskip_error"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.manifest_matches_dist"
alias = "manifest_matches_dist"
title = "MANIFEST matches staged files"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Manifest"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Manifest.pm"
required_facts = ["staged_files", "manifest_text"]
applicability = "all_distributions"
pass_semantics = "MANIFEST paths match the staged tree under the declared policy."
fail_semantics = "MANIFEST and staged files disagree."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = ["cpants.has_manifest"]
remediation_owner = "distribution_author"
implementation_owner = 7178
fixture_ids = ["Acme-CatalogFreeze", "defect_manifest_matches_dist"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.meta_yml_is_parsable"
alias = "meta_yml_is_parsable"
title = "META.yml is parsable"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::MetaYML"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/MetaYML.pm"
required_facts = ["meta_yml"]
applicability = "all_distributions"
pass_semantics = "META.yml parses as YAML metadata."
fail_semantics = "META.yml is present but unparsable."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = ["cpants.has_meta_yml"]
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_meta_yml_is_parsable"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.meta_json_is_parsable"
alias = "meta_json_is_parsable"
title = "META.json is parsable"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::MetaYML"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/MetaYML.pm"
required_facts = ["meta_json"]
applicability = "all_distributions"
pass_semantics = "META.json parses as JSON metadata."
fail_semantics = "META.json is present but unparsable."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = ["cpants.has_meta_json"]
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_meta_json_is_parsable"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.meta_yml_has_provides"
alias = "meta_yml_has_provides"
title = "META provides mapping is present"
class = "cpants_offline_experimental"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::MetaYML"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/MetaYML.pm"
required_facts = ["meta_yml", "meta_json"]
applicability = "all_distributions"
pass_semantics = "Metadata declares a provides mapping."
fail_semantics = "No provides mapping is declared."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_meta_yml_has_provides"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.meta_yml_conforms_to_known_spec"
alias = "meta_yml_conforms_to_known_spec"
title = "META.yml conforms to a known spec"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::MetaYML"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/MetaYML.pm"
required_facts = ["meta_yml"]
applicability = "all_distributions"
pass_semantics = "Parsed META.yml conforms to CPAN::Meta spec 1.4 or 2."
fail_semantics = "Parsed META.yml fails the known spec."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = ["cpants.meta_yml_is_parsable"]
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_meta_yml_conforms_to_known_spec"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.meta_json_conforms_to_known_spec"
alias = "meta_json_conforms_to_known_spec"
title = "META.json conforms to a known spec"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::MetaYML"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/MetaYML.pm"
required_facts = ["meta_json"]
applicability = "all_distributions"
pass_semantics = "Parsed META.json conforms to CPAN::Meta spec 2."
fail_semantics = "Parsed META.json fails the known spec."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = ["cpants.meta_json_is_parsable"]
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_meta_json_conforms_to_known_spec"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.meta_yml_declares_perl_version"
alias = "meta_yml_declares_perl_version"
title = "Metadata declares a Perl version"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::MetaYML"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/MetaYML.pm"
required_facts = ["meta_yml", "meta_json"]
applicability = "all_distributions"
pass_semantics = "Metadata declares a minimum Perl version."
fail_semantics = "No Perl version is declared."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_meta_yml_declares_perl_version"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.meta_yml_has_repository_resource"
alias = "meta_yml_has_repository_resource"
title = "Metadata declares a repository resource"
class = "cpants_offline_experimental"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::MetaYML"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/MetaYML.pm"
required_facts = ["meta_yml", "meta_json"]
applicability = "all_distributions"
pass_semantics = "Metadata declares a repository resource URL."
fail_semantics = "No repository resource is declared."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_meta_yml_has_repository_resource"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.proper_libs"
alias = "proper_libs"
title = "Packages live under lib/ or the distribution root"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::FindModules"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/FindModules.pm"
required_facts = ["packages"]
applicability = "all_distributions"
pass_semantics = "Discovered packages use valid distribution paths."
fail_semantics = "A package lives outside permitted paths."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_proper_libs"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.no_missing_files_in_provides"
alias = "no_missing_files_in_provides"
title = "provides paths exist"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::FindModules"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/FindModules.pm"
required_facts = ["meta_provides", "staged_files"]
applicability = "all_distributions"
pass_semantics = "Every provides file exists in the staged tree."
fail_semantics = "A provides path is missing."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_no_missing_files_in_provides"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.meta_yml_has_license"
alias = "meta_yml_has_license"
title = "Metadata declares a license"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::License"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/License.pm"
required_facts = ["meta_yml", "meta_json"]
applicability = "all_distributions"
pass_semantics = "Metadata contains a license field."
fail_semantics = "No license field is declared."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_meta_yml_has_license"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.has_human_readable_license"
alias = "has_human_readable_license"
title = "A human-readable license is present"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::License"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/License.pm"
required_facts = ["staged_files", "pod"]
applicability = "all_distributions"
pass_semantics = "A LICENSE/Copying file or license POD is present."
fail_semantics = "No human-readable license text is present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_has_human_readable_license"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.has_separate_license_file"
alias = "has_separate_license_file"
title = "A separate license file is present"
class = "cpants_offline_experimental"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::License"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/License.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "A dedicated license file exists."
fail_semantics = "No dedicated license file exists."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_has_separate_license_file"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.has_license_in_source_file"
alias = "has_license_in_source_file"
title = "Source files contain a license"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::License"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/License.pm"
required_facts = ["packages", "pod"]
applicability = "all_distributions"
pass_semantics = "Primary source files contain license text."
fail_semantics = "Primary source files contain no license text."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_has_license_in_source_file"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.has_known_license_in_source_file"
alias = "has_known_license_in_source_file"
title = "Source license is a known license"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::License"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/License.pm"
required_facts = ["packages", "pod"]
applicability = "all_distributions"
pass_semantics = "Source license text matches a known license."
fail_semantics = "Source license text is absent or unrecognised."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = ["cpants.has_license_in_source_file"]
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_has_known_license_in_source_file"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.has_abstract_in_pod"
alias = "has_abstract_in_pod"
title = "POD contains an abstract"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Pod"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Pod.pm"
required_facts = ["pod"]
applicability = "all_distributions"
pass_semantics = "The primary module POD has a NAME abstract."
fail_semantics = "No NAME abstract is present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_has_abstract_in_pod"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.no_abstract_stub_in_pod"
alias = "no_abstract_stub_in_pod"
title = "POD abstract is not a stub"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Pod"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Pod.pm"
required_facts = ["pod"]
applicability = "all_distributions"
pass_semantics = "The NAME abstract is not a placeholder stub."
fail_semantics = "The NAME abstract is a stub."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = ["cpants.has_abstract_in_pod"]
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_no_abstract_stub_in_pod"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.no_broken_module_install"
alias = "no_broken_module_install"
title = "Module::Install is not a broken bundled copy"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::BrokenInstaller"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/BrokenInstaller.pm"
required_facts = ["staged_files", "inc"]
applicability = "all_distributions"
pass_semantics = "No broken bundled Module::Install is present."
fail_semantics = "A broken bundled Module::Install is present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_no_broken_module_install"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.no_broken_auto_install"
alias = "no_broken_auto_install"
title = "auto_install is not used in a broken way"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::BrokenInstaller"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/BrokenInstaller.pm"
required_facts = ["staged_files", "inc"]
applicability = "all_distributions"
pass_semantics = "auto_install is absent or not the broken form."
fail_semantics = "Broken auto_install usage is present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_no_broken_auto_install"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.use_strict"
alias = "use_strict"
title = "Packages use strict"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Uses"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Uses.pm"
required_facts = ["packages"]
applicability = "all_distributions"
pass_semantics = "Each package enables strict (including equivalent versions)."
fail_semantics = "A package does not enable strict."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_use_strict"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.use_warnings"
alias = "use_warnings"
title = "Packages use warnings"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::Kwalitee::Uses"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-Analyse/blob/master/lib/Module/CPANTS/Kwalitee/Uses.pm"
required_facts = ["packages"]
applicability = "all_distributions"
pass_semantics = "Each package enables warnings (including use v5.36+)."
fail_semantics = "A package does not enable warnings."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_use_warnings"]
known_differences = ["CPANTS treats `use v5.36` as satisfying use_warnings."]
limitations = []

[[metric]]
id = "cpants.buildtool_not_executable"
alias = "buildtool_not_executable"
title = "Build tool files are not executable"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Files.pm"
required_facts = ["staged_files", "permissions"]
applicability = "all_distributions"
pass_semantics = "Makefile.PL/Build.PL are not executable."
fail_semantics = "A build tool file is executable."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = ["cpants.has_buildtool"]
remediation_owner = "distribution_author"
implementation_owner = 7178
fixture_ids = ["Acme-CatalogFreeze", "defect_buildtool_not_executable"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.no_generated_files"
alias = "no_generated_files"
title = "No generated files are shipped"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Files.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "No Makefile, Build, or MYMETA generated artifacts are shipped."
fail_semantics = "A generated file is shipped."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_no_generated_files"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.portable_filenames"
alias = "portable_filenames"
title = "Filenames are portable"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Files.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "All filenames are portable across common filesystems."
fail_semantics = "A filename is not portable."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_portable_filenames"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.no_dot_underscore_files"
alias = "no_dot_underscore_files"
title = "No AppleDouble ._ files"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Files.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "No ._ AppleDouble files are present."
fail_semantics = "A ._ file is present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_no_dot_underscore_files"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.no_dot_dirs"
alias = "no_dot_dirs"
title = "No VCS/dot directories are shipped"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Files.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "No .git/.svn/etc directories are shipped."
fail_semantics = "A VCS or other dot directory is shipped."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_no_dot_dirs"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.no_local_dirs"
alias = "no_local_dirs"
title = "No local/ directories are shipped"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Files"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Files.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "No local/ directory is shipped."
fail_semantics = "A local/ directory is shipped."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_no_local_dirs"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.extractable"
alias = "extractable"
title = "Archive extracts without fatal error"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Extraction"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Extraction.pm"
required_facts = ["archive"]
applicability = "archive_input"
pass_semantics = "The archive extracts inside the sandbox."
fail_semantics = "The archive cannot be extracted."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4805
fixture_ids = ["Acme-CatalogFreeze", "defect_extractable"]
known_differences = []
limitations = ["Applies to archive input; directory input is not_applicable. Archive security failures are owned by #4805."]

[[metric]]
id = "cpants.extracts_nicely"
alias = "extracts_nicely"
title = "Archive extracts to a single distribution root"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Extraction"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Extraction.pm"
required_facts = ["archive"]
applicability = "archive_input"
pass_semantics = "Extraction yields one well-named top-level directory."
fail_semantics = "Extraction yields multiple roots or a mismatched name."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = ["cpants.extractable"]
remediation_owner = "distribution_author"
implementation_owner = 4805
fixture_ids = ["Acme-CatalogFreeze", "defect_extracts_nicely"]
known_differences = []
limitations = ["Applies to archive input; directory input is not_applicable."]

[[metric]]
id = "cpants.no_pax_headers"
alias = "no_pax_headers"
title = "Archive has no PAX headers"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Extraction"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Extraction.pm"
required_facts = ["archive"]
applicability = "archive_input"
pass_semantics = "The archive contains no PAX headers."
fail_semantics = "PAX headers are present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = ["cpants.extractable"]
remediation_owner = "distribution_author"
implementation_owner = 4805
fixture_ids = ["Acme-CatalogFreeze", "defect_no_pax_headers"]
known_differences = []
limitations = ["Applies to archive input; directory input is not_applicable."]

[[metric]]
id = "cpants.has_version"
alias = "has_version"
title = "Distribution declares a version"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::DistVersion"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/DistVersion.pm"
required_facts = ["meta_yml", "meta_json", "packages"]
applicability = "all_distributions"
pass_semantics = "A distribution version is declared."
fail_semantics = "No distribution version is declared."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_has_version"]
known_differences = []
limitations = ["Moved to SiteKwalitee because Test::Kwalitee runs on unpacked trees; the native claim is still local metadata/module version."]

[[metric]]
id = "cpants.has_proper_version"
alias = "has_proper_version"
title = "Distribution version is a proper version"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::DistVersion"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/DistVersion.pm"
required_facts = ["meta_yml", "meta_json"]
applicability = "all_distributions"
pass_semantics = "The declared version is a decimal-style proper version."
fail_semantics = "The declared version is missing or not a proper version."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = ["cpants.has_version"]
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_has_proper_version"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.distname_matches_name_in_meta"
alias = "distname_matches_name_in_meta"
title = "Archive/dir name matches metadata name"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Distname"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Distname.pm"
required_facts = ["input_identity", "meta_yml", "meta_json"]
applicability = "all_distributions"
pass_semantics = "Input identity name matches the metadata name."
fail_semantics = "Input identity name and metadata name disagree."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_distname_matches_name_in_meta"]
known_differences = []
limitations = ["Moved to SiteKwalitee because it originally inspected the archive filename; native claim uses staged input identity."]

[[metric]]
id = "cpants.no_invalid_versions"
alias = "no_invalid_versions"
title = "Package versions are valid"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Version"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Version.pm"
required_facts = ["packages"]
applicability = "all_distributions"
pass_semantics = "Discovered package versions parse."
fail_semantics = "A package version is invalid."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_no_invalid_versions"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.consistent_version"
alias = "consistent_version"
title = "Package versions are consistent"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Version"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Version.pm"
required_facts = ["packages"]
applicability = "all_distributions"
pass_semantics = "Discovered packages share one version."
fail_semantics = "Package versions disagree."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = ["cpants.no_invalid_versions"]
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_consistent_version"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.main_module_version_matches_dist_version"
alias = "main_module_version_matches_dist_version"
title = "Main module version matches dist version"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Version"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Version.pm"
required_facts = ["packages", "meta_yml", "meta_json"]
applicability = "all_distributions"
pass_semantics = "The main module version equals the distribution version."
fail_semantics = "Main module and distribution versions disagree."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = ["cpants.has_version"]
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_main_module_version_matches_dist_version"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.no_pod_errors"
alias = "no_pod_errors"
title = "POD has no checker errors"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Pod"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Pod.pm"
required_facts = ["pod"]
applicability = "all_distributions"
pass_semantics = "Public module POD parses without Pod::Checker errors."
fail_semantics = "POD checker errors are present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_no_pod_errors"]
known_differences = ["Native implementation uses ProjectModel POD facts, not a Perl Pod::Checker process."]
limitations = []

[[metric]]
id = "cpants.no_mymeta_files"
alias = "no_mymeta_files"
title = "MYMETA files are not shipped"
class = "cpants_offline_core"
participates_in_core_score = true
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::MyMeta"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/MyMeta.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "MYMETA.yml/json are absent from the staged tree."
fail_semantics = "A MYMETA file is shipped."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row stays in the compatible-core denominator and is reported as unverified."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_no_mymeta_files"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.valid_signature"
alias = "valid_signature"
title = "SIGNATURE is absent or locally verifiable"
class = "cpants_offline_extra"
participates_in_core_score = false
relationship = "adapted"
source_module = "Module::CPANTS::SiteKwalitee::Signature"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Signature.pm"
required_facts = ["signature"]
applicability = "all_distributions"
pass_semantics = "Unsigned distributions pass; a present SIGNATURE verifies with committed key material."
fail_semantics = "A SIGNATURE is present and fails local verification."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_valid_signature"]
known_differences = []
limitations = ["CPANTS may contact a keyserver. Native v1 never does; missing local key material is unverified, not a core-score fail."]

[[metric]]
id = "cpants.no_unauthorized_packages"
alias = "no_unauthorized_packages"
title = "PAUSE authorization for provided packages"
class = "unsupported_or_deferred"
participates_in_core_score = false
relationship = "deferred"
source_module = "Module::CPANTS::SiteKwalitee::Permissions"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Permissions.pm"
required_facts = ["cpan_permissions"]
applicability = "all_distributions"
pass_semantics = "Not evaluated in catalog v1."
fail_semantics = "Not evaluated in catalog v1."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 7170
fixture_ids = ["Acme-CatalogFreeze", "defect_no_unauthorized_packages"]
known_differences = []
limitations = ["Needs PAUSE permissions / CPANTS DB (`needs_db`). Deferred; not a site analogue with a local substitute."]

[[metric]]
id = "cpants.is_prereq"
alias = "is_prereq"
title = "Another author depends on this distribution"
class = "unsupported_or_deferred"
participates_in_core_score = false
relationship = "deferred"
source_module = "Module::CPANTS::SiteKwalitee::Prereq"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Prereq.pm"
required_facts = ["cpan_reverse_dependencies"]
applicability = "all_distributions"
pass_semantics = "Not evaluated in catalog v1."
fail_semantics = "Not evaluated in catalog v1."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 7170
fixture_ids = ["Acme-CatalogFreeze", "defect_is_prereq"]
known_differences = []
limitations = ["Needs the CPANTS database, is_extra, and is_disabled upstream. Popularity is not locally knowable."]

[[metric]]
id = "cpants.prereq_matches_use"
alias = "prereq_matches_use"
title = "Statically visible runtime imports are declared"
class = "cpants_site_analogue"
participates_in_core_score = false
relationship = "site_analogue"
source_module = "Module::CPANTS::SiteKwalitee::Prereq"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Prereq.pm"
required_facts = ["packages", "imports", "meta_prereqs"]
applicability = "all_distributions"
pass_semantics = "Every statically visible non-core runtime import has a declared prerequisite."
fail_semantics = "A statically visible non-core runtime import has no declared prerequisite."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_prereq_matches_use"]
known_differences = []
limitations = ["CPANTS core metric uses the CPANTS module-to-dist database (`needs_db`). Native v1 is a narrower ProjectModel claim and must not enter compatible_core_score."]

[[metric]]
id = "cpants.test_prereq_matches_use"
alias = "test_prereq_matches_use"
title = "Statically visible test imports are declared"
class = "cpants_site_analogue"
participates_in_core_score = false
relationship = "site_analogue"
source_module = "Module::CPANTS::SiteKwalitee::Prereq"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Prereq.pm"
required_facts = ["packages", "imports", "meta_prereqs"]
applicability = "all_distributions"
pass_semantics = "Every statically visible non-core test import has a declared test prerequisite."
fail_semantics = "A statically visible non-core test import has no declared test prerequisite."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_test_prereq_matches_use"]
known_differences = []
limitations = ["CPANTS extra metric uses the CPANTS database. Native v1 is a narrower local claim."]

[[metric]]
id = "cpants.configure_prereq_matches_use"
alias = "configure_prereq_matches_use"
title = "Statically visible configure imports are declared"
class = "cpants_site_analogue"
participates_in_core_score = false
relationship = "site_analogue"
source_module = "Module::CPANTS::SiteKwalitee::Prereq"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Prereq.pm"
required_facts = ["build_file_text", "meta_prereqs"]
applicability = "all_distributions"
pass_semantics = "Statically visible configure-time non-core imports are declared as configure prerequisites."
fail_semantics = "A configure-time import is undeclared."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = ["cpants.has_buildtool"]
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_configure_prereq_matches_use"]
known_differences = []
limitations = ["CPANTS experimental metric uses the CPANTS database and does not execute build scripts. Native v1 keeps that non-execution boundary."]

[[metric]]
id = "cpants.has_security_doc"
alias = "has_security_doc"
title = "A security policy document is present"
class = "cpants_offline_experimental"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Security"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Security.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "SECURITY.md or equivalent is present."
fail_semantics = "No security policy document is present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_has_security_doc"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.security_doc_contains_contact"
alias = "security_doc_contains_contact"
title = "Security document contains contact information"
class = "cpants_offline_experimental"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Security"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Security.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "The security document contains a contact."
fail_semantics = "The security document has no contact."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = ["cpants.has_security_doc"]
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_security_doc_contains_contact"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.has_contributing_doc"
alias = "has_contributing_doc"
title = "A contributing document is present"
class = "cpants_offline_experimental"
participates_in_core_score = false
relationship = "direct"
source_module = "Module::CPANTS::SiteKwalitee::Security"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Security.pm"
required_facts = ["staged_files"]
applicability = "all_distributions"
pass_semantics = "CONTRIBUTING.md or equivalent is present."
fail_semantics = "No contributing document is present."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 4806
fixture_ids = ["Acme-CatalogFreeze", "defect_has_contributing_doc"]
known_differences = []
limitations = []

[[metric]]
id = "cpants.easily_repackageable_by_debian"
alias = "easily_repackageable_by_debian"
title = "Debian packaging hints"
class = "unsupported_or_deferred"
participates_in_core_score = false
relationship = "deferred"
source_module = "Module::CPANTS::SiteKwalitee::Debian"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Debian.pm"
required_facts = ["debian_packaging_db"]
applicability = "all_distributions"
pass_semantics = "Not evaluated in catalog v1."
fail_semantics = "Not evaluated in catalog v1."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 7170
fixture_ids = ["Acme-CatalogFreeze", "defect_easily_repackageable_by_debian"]
known_differences = []
limitations = ["Downstream packaging database metric; not a local distribution fact."]

[[metric]]
id = "cpants.easily_repackageable_by_fedora"
alias = "easily_repackageable_by_fedora"
title = "Fedora packaging hints"
class = "unsupported_or_deferred"
participates_in_core_score = false
relationship = "deferred"
source_module = "Module::CPANTS::SiteKwalitee::Fedora"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Fedora.pm"
required_facts = ["fedora_packaging_db"]
applicability = "all_distributions"
pass_semantics = "Not evaluated in catalog v1."
fail_semantics = "Not evaluated in catalog v1."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 7170
fixture_ids = ["Acme-CatalogFreeze", "defect_easily_repackageable_by_fedora"]
known_differences = []
limitations = ["Downstream packaging database metric; not a local distribution fact."]

[[metric]]
id = "cpants.fits_fedora_license"
alias = "fits_fedora_license"
title = "License fits Fedora packaging policy"
class = "unsupported_or_deferred"
participates_in_core_score = false
relationship = "deferred"
source_module = "Module::CPANTS::SiteKwalitee::Fedora"
source_version = "1.03"
behavior_ref = "https://github.com/cpants/Module-CPANTS-SiteKwalitee/blob/master/lib/Module/CPANTS/SiteKwalitee/Fedora.pm"
required_facts = ["fedora_license_db"]
applicability = "all_distributions"
pass_semantics = "Not evaluated in catalog v1."
fail_semantics = "Not evaluated in catalog v1."
not_applicable_semantics = "Catalog v1 freezes only the input-role not-applicable gate (for example archive-only metrics on a directory input); per-metric content-driven not-applicable conditions are not frozen here and are authored with the indicator."
unverified_semantics = "Required facts were not produced; this row is reported as unverified and never enters the compatible-core denominator."
depends_on = []
remediation_owner = "distribution_author"
implementation_owner = 7170
fixture_ids = ["Acme-CatalogFreeze", "defect_fits_fedora_license"]
known_differences = []
limitations = ["Downstream license-policy metric; not a local distribution fact."]
