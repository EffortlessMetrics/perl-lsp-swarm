[package]
name = "perl-diagnostics"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
license.workspace = true
description = "Unified diagnostic codes, types, and catalog for Perl LSP."
documentation = "https://docs.rs/perl-diagnostics"
readme = "README.md"
keywords = ["perl", "lsp", "diagnostics", "linting", "language-server"]
categories = ["development-tools", "data-structures", "text-editors"]

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[dependencies]
serde = { version = "1.0", optional = true, features = ["derive"] }
serde_json.workspace = true
# sha2: collision-resistant digest for ParseDiagnosticAnchor stale-source detection.
# Domain-separated; the workspace pins sha2 0.11.
sha2 = { workspace = true }

[features]
default = []
serde = ["dep:serde"]

[dev-dependencies]
perl-test-must = { path = "../perl-test-must" }

[lints]
workspace = true
