[package]
name = "perl-test-must"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
description = "Dependency-free panic-on-failure Result and Option helpers for Rust tests"
readme = "README.md"
license = "MIT OR Apache-2.0"
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/perl-test-must"
keywords = ["perl", "testing", "test-helpers", "must", "assertions"]
categories = ["development-tools::testing"]

# Zero dependencies — that is the entire point.
[dependencies]

[package.metadata.docs.rs]
rustdoc-args = ["--cfg", "docsrs"]

[lints]
workspace = true
