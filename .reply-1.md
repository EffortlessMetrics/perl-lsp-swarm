Confirmed real against the live tree and repaired in 451a190f6.

`PullDiagnosticsOrchestrator::warnings_sent` was a fourth unbounded raw-string warning store (`execution-failed:{e}` error bodies, `missing-profile:{path}` keys) beside the governed critic family. The orchestrator now routes all three of its warning shapes through `server.session_warning_dedup` (Critic family), sharing the same typed identities, 32-entry hard cap, and fingerprint-only retention as the push path; its `warnings_sent` field is gone. The didChangeConfiguration critic transition already clears that shared family immediately before calling `orchestrator.reset()`, so the analyzer/warning lifecycle alignment is preserved without a second store. The xtask negative control (`session_warning_dedup_is_not_a_raw_string_set`) now also fails if `warnings_sent` reappears in `runtime/diagnostics.rs`, and the inventory row states the store covers the pull path.
