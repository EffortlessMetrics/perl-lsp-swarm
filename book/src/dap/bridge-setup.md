# DAP Compatibility Setup

The supported first-mile debugger path is the native `perl-dap` CLI documented
in [DAP User Guide](./user-guide.md).

Historical compatibility setup lives in the repository reference doc:

```text
docs/reference/DAP_LEGACY_BRIDGE_COMPAT.md
```

Use that reference only when maintaining or migrating an older debugger setup.
