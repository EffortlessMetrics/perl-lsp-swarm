# Architecture Overview — compatibility pointer

The former checked-in book page mixed retired crate topology, obsolete performance figures, and historical documentation-policy claims. It is retained as a compatibility entry point, not a current authority.

Use the [current architecture reference](https://github.com/EffortlessMetrics/perl-lsp-swarm/blob/main/docs/reference/ARCHITECTURE.md), the [workspace manifest](https://github.com/EffortlessMetrics/perl-lsp-swarm/blob/main/Cargo.toml), and package-local READMEs for current ownership and implementation details.
