# RIPR Suppressions
#
# Suppressions for ripr static exposure analysis findings. Each suppression
# requires owner, reason, created, review_after, and expires. Suppressions
# are the documented exception path; the default is to leave findings
# advisory-visible.
#
# Read by .github/workflows/ripr.yml (PR 06+) and ripr.toml.

schema_version = 1
policy = "ripr-suppressions"
owner = "EffortlessMetrics"
status = "advisory"
updated = "2026-08-17"

# -----------------------------------------------------------------------------
# Generated status docs: validated through status-generation checks, not
# Rust test oracle exposure.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-generated-status-docs"
kind = "generated_or_non_production_surface"
paths = ["docs/project/status/**"]
classification = ["static_unknown", "no_static_path"]
owner = "repo-owner"
issue = "TODO"
reason = "Generated status docs are verified through status-generation checks, not Rust test oracle exposure."
created = "2026-05-07"
review_after = "2026-06-07"
expires = "2026-08-07"

# -----------------------------------------------------------------------------
# Archived source: retained historical code, not active workspace behavior.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-archive"
kind = "generated_or_non_production_surface"
paths = ["archive/**"]
owner = "proof-lane"
issue = "#8197"
reason = "Archived source is retained for historical reference and is excluded from active workspace proof targets; current behavior must be proven in live crates instead."
created = "2026-05-28"
review_after = "2026-06-28"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# UX receipt tests: executable proof artifacts, not production behavior seams.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-ux-receipt-tests"
kind = "test_receipt_surface"
paths = ["crates/perl-lsp-ux-tests/tests/*", "crates/perl-lsp-ux-tests/tests/**"]
owner = "editor-trust"
issue = "#8197"
reason = "Editor UX receipt tests are proof inputs validated by cargo/UX gates and fixture inventory; diff-scoped RIPR should not block on oracle gaps inside newly added receipt tests."
created = "2026-06-01"
review_after = "2026-07-01"
expires = "2026-09-30"

# Exact-process organizer-withdrawal receipt: subprocess harness plumbing is not
# production behavior.
#
# The test drives the published `perllsp` binary over stdio and asserts the
# initialize capability response, filtered and unfiltered code-action results,
# forged resolve behavior, edit ranges, and clean process shutdown. RIPR's
# diff-scoped tracer cannot connect the request/response helper plumbing to
# those assertions and reports 47 reachable-unrevealed seams in this test-only
# receipt file. Keep the suppression narrow to the four reported plumbing
# families; the production withdrawal remains covered by the provider,
# runtime, and exact-process assertions.
[[suppress]]
id = "ripr-suppress-organize-imports-withdrawal-process-receipt"
kind = "test_receipt_surface"
paths = ["crates/perllsp/tests/lsp_organize_imports_containment_process.rs"]
classification = ["call_deletion", "error_path", "match_arm", "return_value"]
owner = "proof-lane"
issue = "#8305"
reason = "Issue #8305 adds an exact-process receipt that drives the published perllsp binary over stdio and asserts withdrawal of source.organizeImports from initialize capabilities, filtered and unfiltered code-action responses, forged resolve responses, edit ranges, and process shutdown. RIPR 0.10.0 reports 47 reachable_unrevealed seams in this test-only subprocess harness because it cannot trace request/response helper plumbing into the assertions. The production withdrawal remains covered by provider-level, runtime-level, and exact-process negative assertions. Keep this suppression narrow to the single receipt file and these four reported test-plumbing families; remove when RIPR distinguishes receipt harness plumbing from production behavior."
created = "2026-08-21"
review_after = "2026-09-21"
expires = "2026-10-31"

# -----------------------------------------------------------------------------
# Inline completion pack contract fixtures: executable receipt matrix, not
# production behavior. The fixture file added for #1567 records positive and
# quiet-path evidence for deterministic inline completion packs; RIPR 0.9.x
# treats fixture plumbing and matrix iteration as new production seams.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-inline-completion-pack-contract-fixture"
kind = "test_receipt_surface"
paths = ["crates/perl-lsp-rs-core/tests/inline_completion_ux_fixtures.rs"]
classification = ["weakly_exposed", "no_static_path", "static_unknown"]
owner = "editor-trust"
issue = "#1567"
reason = "Issue #1567 adds inline_completion_ux_fixtures.rs as an executable completion-pack contract receipt for Try::Tiny positive, no-import, comment, string, POD, near-match, parse-damage, visible-symbol conflict, and stable-range cases. RIPR 0.9.x classifies the new fixture matrix and helper plumbing as production exposure even though the product seam is exercised through InlineCompletionProvider and the focused fixture test cargo test -p perl-lsp-rs-core --test inline_completion_ux_fixtures. Keep this suppression narrow to the single receipt file and remove when RIPR stops counting test fixture plumbing as production seams."
created = "2026-06-19"
review_after = "2026-07-19"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# Documentation target resolver predicates: centralized string-boundary helpers
# are covered by focused resolver and virtual-content tests, but RIPR 0.9.x cannot
# trace the predicate evidence after #1962 moves link parsing behind the canonical
# PerlDocumentationTarget resolver.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-doc-target-resolver-predicate-evidence"
kind = "predicate_infection_untraceable"
paths = [
  "crates/perl-lsp-rs/src/documentation_targets.rs",
  "crates/perl-lsp-rs/src/runtime/language/virtual_content.rs",
]
classification = ["weakly_exposed", "infection_unknown"]
owner = "editor-trust"
issue = "#1962"
reason = "Issue #1962 centralizes perldoc URI and simple POD module-link parsing in PerlDocumentationTarget. Focused tests cover the accepted trimmed URI branch, whitespace rejection, alphanumeric/underscore tail acceptance, malformed target rejection, and the virtual-content current-module discriminator through from_perldoc_uri_accepts_already_trimmed_name_boundary, from_perldoc_uri_rejects_whitespace_instead_of_normalizing, perl_doc_name_segment_accepts_alphanumeric_and_underscore_tail, from_simple_pod_link_target_* tests, collect_simple_pod_module_links_boundary_discriminator, and parser_workspace_pod_related_perldoc_links_are_sorted_and_filtered. RIPR 0.9.x still reports weakly_exposed/infection_unknown seams for these string predicates and moved helper boundaries because the static tracer cannot connect the focused tests to the centralized resolver. Remove when EffortlessMetrics/ripr#1429 is fixed or targeted mutation replaces this advisory path."
created = "2026-06-21"
review_after = "2026-07-21"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# Completion lexical string-context scanner predicates: focused exact-value
# tests cover the quote parity and quote-like operator boundaries added for
# #1622, but RIPR 0.9.x cannot connect those oracles through the scanner state
# machine and reports the seams as weakly_exposed.
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# LSP cancellation performance receipt: test harness timing policy, not
# production behavior. The AC12 test intentionally enforces aggregate percentile
# thresholds rather than failing individual samples that can include scheduler
# preemption.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-lsp-cancellation-performance-receipt"
kind = "test_receipt_surface"
paths = ["crates/perl-lsp-rs/tests/lsp_cancellation_performance_tests.rs"]
classification = ["weakly_exposed"]
owner = "lsp-lane"
issue = "#1550"
reason = "Issue #1550 keeps the AC12 cancellation latency contract in aggregate average/p95/p99/p99.9 assertions while removing a per-sample 500us wall-clock assertion that can fail under scheduler preemption. RIPR 0.9.0 treats that test-receipt assertion change as weakly_exposed production risk even though the surface is an integration performance receipt and the contract remains covered by test_cancellation_check_latency_performance_ac12."
created = "2026-06-18"
review_after = "2026-07-18"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# Type hierarchy cancellation routing: method-string dispatch and inline observer
# plumbing are covered by focused cancellation and live-handler tests, but RIPR
# 0.9.x cannot connect the route helper call and test helper branches to those
# oracles in diff-scoped analysis.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-type-hierarchy-cancellation-routing"
kind = "call_presence_static_tracing_limit"
paths = ["crates/perl-lsp-rs/src/runtime/dispatch/routing.rs"]
classification = ["weakly_gripped", "reachable_unrevealed", "static_unknown", "infection_unknown", "propagation_unknown", "call_presence"]
owner = "lsp-lane"
issue = "#1774"
reason = "PR #3455 adds type hierarchy cancellation dispatch for textDocument/prepareTypeHierarchy, typeHierarchy/prepare, typeHierarchy/supertypes, and typeHierarchy/subtypes. Focused tests cover method registration (type_hierarchy_methods_are_registered_for_cancellation), immediate cancellation short-circuit and marker cleanup (cancelled_type_hierarchy_routes_return_immediate_responses), uncancelled live-handler dispatch and response values (route_request_call_presence_observer), and protocol-level cancellation behavior (lsp_cancellation_protocol_tests). RIPR 0.9.x still reports weak/static call-presence gaps on the method-string route helper and inline observer helper plumbing because it cannot trace the method matrix and RoutedResponse assertions through the dispatch helper. Remove when RIPR can trace routed string-dispatch call presence or targeted mutation replaces this static advisory path."
created = "2026-07-07"
review_after = "2026-08-07"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# Seam-proof test files: diff-scoped RIPR harness plumbing, not production seams.
# Tracks EffortlessMetrics/ripr#1428 (RIPR flags test-harness plumbing in newly
# added test files as production seams). Remove this suppression once #1428 lands.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-seam-proof-tests"
kind = "test_receipt_surface"
paths = ["crates/**/tests/ripr_seam_proof_*.rs"]
classification = ["call_deletion", "return_value", "error_path"]
owner = "proof-lane"
issue = "#1289"
reason = "Seam-proof test files (ripr_seam_proof_*.rs) are diff-scoped proof harnesses whose internal plumbing — Ok(()) returns, .contains() calls, ? operators — must not be treated as new production seams by diff-scoped RIPR analysis. This is the same class as ripr-suppress-ux-receipt-tests. Tracks EffortlessMetrics/ripr#1428; remove when fixed upstream."
created = "2026-06-10"
review_after = "2026-07-10"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# Emacs project fixture matrix: diff-scoped RIPR sees temp-tree construction
# and match-arm plumbing as production seams.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-emacs-project-fixture-support"
kind = "test_receipt_surface"
paths = ["crates/perl-lsp-rs/tests/support/emacs_projects.rs"]
classification = ["weakly_exposed", "no_static_path"]
owner = "editor-trust"
issue = "#7763"
reason = "The Emacs project-root fixture matrix is test-only support that builds temporary Perl project layouts for the eight focused fixture tests in emacs_project_root_fixture_tests.rs. RIPR 0.9.x classifies its fixture construction, case dispatch, and fallible setup as production seams. The suppression covers only this support file's receipt-surface plumbing; it does not claim the focused tests provide product root-detection coverage -- they validate fixture shapes and probe-source correctness against the support matrix, not the editor client's runtime root detection. Remove when RIPR distinguishes fixture plumbing from production behavior."
created = "2026-08-17"
review_after = "2026-09-17"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# Agent capability workflow contract test: diff-scoped RIPR currently treats
# fallible YAML contract-test plumbing as a production seam.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-agent-capability-workflow-contract-test"
kind = "test_receipt_surface"
paths = ["xtask/tests/agent_capability_gate_workflow_policy.rs"]
classification = ["weakly_exposed", "reachable_unrevealed", "error_path", "call_deletion", "return_value", "field_construction", "predicate"]
owner = "ci"
issue = "#4056"
reason = "PR #4056 adds a fallible YAML contract test whose six diff-scoped RIPR gaps are test-harness plumbing (mapping/sequence parsing, Result propagation, exact contract values, and the terminal Ok(()) marker), not production behavior. The test directly observes the workflow's static runner, routing, fallback, trigger, permission, action-pin, and command-presence contracts; the production workflow remains subject to the normal workflow-policy and exact-head checks. This is the ripr#1428 test-surface limitation already covered by the repository's seam-proof suppressions. Remove when RIPR can distinguish contract-test plumbing from production seams."
created = "2026-07-17"
review_after = "2026-08-17"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# goals-selector --json CLI regression test: same ripr#1428 class as
# ripr-suppress-seam-proof-tests, one file.
#
# xtask/tests/goals_next_json_cli.rs (#3692 defect 1 / PR #3701) is a
# single-scenario `-> Result<()>` integration test that spawns the compiled
# `xtask` binary end to end to prove `goals next --json` prints parseable
# JSON and exits 1 (never an unstructured color_eyre stderr dump) on an
# internal error. The 6 flagged seams are ALL test-harness plumbing, not
# production behavior:
#   :29  call_deletion  — assert_cmd's .assert() chain call
#   :37,:39 call_deletion — the anyhow::anyhow!(...) message-building
#                            closure passed to .map_err(), only reached if
#                            the very invariant under test (stdout IS valid
#                            JSON) were to regress — there is no way to
#                            "test the test's own failure message" without
#                            deliberately breaking the production behavior
#                            it verifies
#   :40  error_path      — the `?` after that .map_err()
#   :58  call_deletion / return_value — the terminal Ok(()) test-passed
#                            marker
# The actual production seam this test proves (next()'s json=true branch
# calling std::process::exit(1) with parseable JSON on stdout and nothing
# on stderr) is real production-code behavior, and IS exercised — this
# suppression covers only the test file's own scaffolding around that
# assertion, exactly as ripr-suppress-seam-proof-tests does for
# ripr_seam_proof_*.rs. Remove this suppression once #1428 lands.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-goals-next-json-cli-test"
kind = "test_receipt_surface"
paths = ["xtask/tests/goals_next_json_cli.rs"]
classification = ["call_deletion", "return_value", "error_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "xtask/tests/goals_next_json_cli.rs (#3692 defect 1, PR #3701) is a single-scenario Result<()> integration test driving the compiled xtask binary end to end to prove `goals next --json` emits parseable JSON on stdout and exits 1 on any internal error, never an unstructured color_eyre stderr dump. All 6 flagged seams (lines 29, 37, 39, 40, 58) are test-harness plumbing — the assert_cmd .assert() chain, the anyhow::anyhow!(...) message-building closure inside .map_err() (only reachable if the test's own invariant regressed), the ? error-path after it, and the terminal Ok(()) test-passed marker — not production behavior. Same ripr#1428 class as ripr-suppress-seam-proof-tests (test-harness plumbing flagged as production seams). The production seam this test proves (next()'s json=true branch in xtask/src/tasks/goals/mod.rs) is real and covered; only the test file's own scaffolding is suppressed. Remove when ripr#1428 is fixed upstream."
created = "2026-07-11"
review_after = "2026-08-11"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# parse_quote_operator_content: canonical shared helper — activation untraceable.
#
# PR refactor/centralize-qw-parsing merged the per-consumer copies of this
# function (previously in perl-semantic-analyzer/src/analysis/index.rs,
# import_extractor.rs, and perl-module/src/import/mod.rs) into a single
# canonical implementation at:
#   crates/perl-parser-core/src/syntax/quote.rs
#
# The three per-consumer suppressions (ripr-suppress-parse-qw-content-end-lt-start,
# ripr-suppress-import-extractor-quote-operator-activation, and
# ripr-suppress-parse-qw-arg-list-predicate-seams) have been REMOVED because
# those files no longer contain the parsing seams — they delegate to the canonical.
#
# Four `weakly_exposed` seams remain in the canonical helper at quote.rs:
#   (A) s.strip_prefix(operator)? — None on prefix miss
#   (B) chars.next()?             — None on empty rest-string
#   (C) open.is_ascii_alphanumeric() || open == '_' — alphanumeric-guard predicate
#   (D) rest.len().checked_sub(close.len_utf8())?   — arithmetic boundary (end<start)
#
# The existing seam-proof tests from PR #1292 continue to cover all four seams.
# RIPR cannot statically trace activation from test inputs through
# s.strip_prefix → .trim_start() → .chars() → .next() — the same ripr#1429
# class limitation. Remove when ripr#1429 is fixed upstream.
#
# Net suppression change: 3 removed (per-consumer), 1 added (canonical home).
# Cites ripr#1429 and #1289 per project convention.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-canonical-quote-operator-helper"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-parser-core/src/syntax/quote.rs"]
classification = ["static_unknown", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "Canonical parse_quote_operator_content helper (centralized in #1289 refactor) has 4 ripr#1429-class seams: strip_prefix (A), chars.next (B), alphanumeric-guard predicate (C), checked_sub arithmetic (D). All seams are covered by inline seam-proof tests from PR #1292 (parse_quote_operator_content_wrong_operator_returns_none, _alphanumeric_delimiter_rejected, _underscore_delimiter_rejected, _mismatched_close_returns_none, etc.). RIPR cannot statically trace activation from string literals through the string-parsing call chain — same ripr#1429 limitation as removed per-consumer suppressions. Net reduction: 3 suppressions removed, 1 canonical suppression added. Remove when ripr#1429 is fixed upstream."
created = "2026-06-11"
review_after = "2026-07-11"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# Quote-operator conformance test helpers: RIPR flags test-plumbing seams in
# newly added integration test helper functions as production seams.
#
# Two `weakly_exposed` seams flagged in test helper functions added by PR #1292:
#   crates/perl-semantic-analyzer/tests/require_import_resolution.rs:304
#     → `extract_explicit_symbols`: match arm / error-path in a test helper
#   crates/perl-workspace/tests/use_lib_fact_tests.rs:477
#     → `workspace_explicit_symbols_for_module`: match arm / error-path
#
# These are test infrastructure functions (not production code). RIPR's
# diff-scoped analysis treats newly added test helper functions as production
# seams — the same class of false-positive as ripr#1428 (test-harness plumbing
# flagged as production seams). The functions are pure test infrastructure:
# they call the production function and classify its output for assertion.
#
# The behavior these helpers exercise IS tested — the entire conformance matrix
# (8 compact/spaced delimiter forms, 1 bareword rejection) is verified through
# these helpers. RIPR cannot distinguish test-only helper functions from
# production exports when analyzing the diff.
#
# This is distinct from ripr#1429 (predicate-infection / activation-tracing in
# production string-parsing functions). This is ripr#1428 (RIPR treats diff-
# added test-helper plumbing as new production seams). Same upstream issue
# family. Remove when ripr#1428 is fixed.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-quote-operator-conformance-test-helpers"
kind = "test_receipt_surface"
paths = [
  "crates/perl-semantic-analyzer/tests/require_import_resolution.rs",
  "crates/perl-workspace/tests/use_lib_fact_tests.rs",
]
classification = ["static_unknown", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "Test helper functions (extract_explicit_symbols in require_import_resolution.rs:304 and workspace_explicit_symbols_for_module in use_lib_fact_tests.rs:477) are diff-scoped RIPR false-positives in newly added integration test files. RIPR flags the match-arm / error-path plumbing in these helpers as production seams. The production behavior exercised by these helpers is fully covered by the conformance matrix tests in the same files. This is the same ripr#1428 class as ripr-suppress-seam-proof-tests (test-harness plumbing flagged as production seams). Suppression is minimal (two specific test files). Remove when ripr#1428 is fixed upstream."
created = "2026-06-10"
review_after = "2026-07-10"
expires = "2026-09-30"
# -----------------------------------------------------------------------------
# workspace_rename char-boundary helpers: is_word_boundary_before/after —
# activation untraceable (ripr#1429 class).
#
# PR #1288 (fix #956) extracted char-aware word-boundary predicate helpers
# from inline logic in find_occurrences:
#   crates/perl-parser/src/refactor/workspace_rename.rs
#
# Both helpers are covered by 4 inline #[cfg(test)] unit tests:
#   test_is_word_boundary_before_ascii, test_is_word_boundary_before_unicode,
#   test_is_word_boundary_after_ascii, test_is_word_boundary_after_unicode
#
# ripr 0.5.0 cannot statically trace activation from test inputs through the
# helper call chain (text[..offset].chars().next_back() -> is_perl_ident_char)
# — the same ripr#1429 limitation as ripr-suppress-canonical-quote-operator-helper.
# The helpers were extracted specifically to enable --lib coverage of the changed
# boundary logic (Codecov patch fix, #956). Remove when ripr#1429 is fixed upstream.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-workspace-rename-char-boundary-helpers"
kind = "predicate_infection_untraceable"
paths = [
  "crates/perl-parser/src/refactor/workspace_rename.rs",
]
classification = ["static_unknown", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "is_word_boundary_before and is_word_boundary_after are char-aware word-boundary predicate helpers extracted for Codecov --lib coverage in PR #1288 (fix #956). Both predicates are covered by 4 inline #[cfg(test)] unit tests each (test_is_word_boundary_before_ascii, test_is_word_boundary_before_unicode, test_is_word_boundary_after_ascii, test_is_word_boundary_after_unicode). ripr 0.5.0 cannot statically trace activation from test inputs through the helper call chain — same ripr#1429 limitation as sibling suppressions. Remove when ripr#1429 is fixed upstream."
created = "2026-06-11"
review_after = "2026-07-11"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# phase_block_description match arms: string-literal dispatch — activation
# untraceable by RIPR static analysis.
#
# hover_cards.rs lines 41/48/55/62/68/69/70 are the closing `)),` and `_ => None`
# of each match arm in phase_block_description (BEGIN/END/INIT/CHECK/UNITCHECK).
# Line 63 is the UNITCHECK arm opening (`"UNITCHECK" => Some((`).
#
# All 5 phase names and the wildcard arm are covered by dedicated unit tests in
# hover_tests.rs (phase_block_hover_begin_returns_compile_time_timing,
# phase_block_hover_end_returns_program_exit_timing, etc. plus
# phase_block_hover_unknown_returns_none for the `_ => None` arm). Each test
# uses exact string assertions — strong oracles — against the returned markdown.
#
# RIPR cannot statically trace that calling phase_block_hover("BEGIN") exercises
# the "BEGIN" arm because activation flows through a string-literal match: the
# same ripr#1429 predicate-infection-untraceable class that suppresses
# parse_quote_operator_content. Remove when ripr#1429 is fixed upstream.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-phase-block-description-match-arms"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-lsp-rs/src/runtime/language/hover/hover_cards.rs"]
classification = ["no_static_path", "weakly_exposed"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "phase_block_description match arms (BEGIN/END/INIT/CHECK/UNITCHECK/wildcard) are ripr#1429-class no_static_path seams: RIPR cannot trace activation from string-literal call sites through the str match dispatch. All arms are covered by dedicated unit tests in hover_tests.rs with exact (strong-oracle) string assertions. Suppression is minimal (single production file, only hover_cards.rs). Remove when ripr#1429 is fixed upstream."
created = "2026-06-11"
review_after = "2026-07-11"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# Live compiler hover preservation guard: ripr#1429-class feature-gated
# propagation path.
#
# PR #1551 preserves the richer legacy Moo/Moose accessor hover card when the
# live compiler path can only produce a generic generated-symbol card. The guard
# is covered by direct unit tests in live_compiler_hover.rs:
#   - preserves_richer_moo_accessor_hover_over_generated_compiler_card
#   - uses_compiler_hover_when_it_keeps_accessor_attribution
# and by the targeted e2e regression:
#   - moose_hover_completion_and_definition_include_attribute_metadata
#
# RIPR cannot statically trace the feature-gated path through
# workspace_index.with_semantic_queries_for_uri -> hover_cutover ->
# should_preserve_legacy_hover, and reports reachable_unrevealed /
# weakly_exposed findings on the guard despite the direct and e2e oracles.
# This is the same ripr#1429 propagation-path class as the neighboring hover
# suppressions. Suppression is narrow: single production file, issue #1550.
# Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-live-compiler-hover-preservation-guard"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-lsp-rs/src/runtime/language/hover/live_compiler_hover.rs"]
classification = ["weakly_exposed", "reachable_unrevealed"]
owner = "lsp-lane"
issue = "#1550"
reason = "PR #1551 adds a live compiler hover guard that preserves the richer legacy Moo/Moose accessor hover card when compiler facts would return only a generic generated-symbol card. Direct unit tests cover both preserve and use-compiler branches, and moose_hover_completion_and_definition_include_attribute_metadata covers the e2e regression. RIPR cannot statically trace the feature-gated workspace_index.with_semantic_queries_for_uri -> hover_cutover -> should_preserve_legacy_hover propagation path, so this is a ripr#1429-class static tracing limitation. Suppression is narrow: single production file, issue #1550. Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled."
created = "2026-06-19"
review_after = "2026-07-19"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# find_phase_block_at_offset boundary predicates: ripr reports weakly_exposed
# despite strong oracles present.
#
# hover.rs lines 944 and 953:
#   944: `if offset < node.location.start || offset > node.location.end {`
#   953: `phase_span.as_ref().map(|s| offset >= s.start && offset <= s.end).unwrap_or(true)`
#
# ripr's own discriminate field reports "Strong oracle found: exact value or
# pattern assertion" for both seams, yet classifies them weakly_exposed because
# the propagation path from the predicate branch to the test's assert_eq! is a
# call-chain ripr cannot statically traverse. Four unit tests in hover_tests.rs
# cover both predicates at their exact boundaries:
#   - find_phase_block_at_offset_returns_none_when_offset_out_of_node_range
#   - find_phase_block_at_offset_returns_phase_name_when_offset_in_node_and_no_phase_span
#   - find_phase_block_at_offset_respects_phase_span_boundary
#   - find_phase_block_at_offset_recurses_through_program_to_find_phase_block
# This is the same ripr#1429 propagation-path class. Remove when fixed upstream.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-find-phase-block-at-offset-predicates"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-lsp-rs/src/runtime/language/hover.rs"]
classification = ["weakly_exposed"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "Two boundary predicates in find_phase_block_at_offset (hover.rs:944 offset-range guard, hover.rs:953 phase_span boundary) are classified weakly_exposed despite ripr's own discriminate field reporting 'Strong oracle found: exact value or pattern assertion'. RIPR cannot statically trace propagation from the predicate branch through the recursive call chain to the test assertion. All four code paths are covered by exact-equality unit tests in hover_tests.rs. Suppression is minimal (single production file, only hover.rs). Remove when ripr#1429 is fixed upstream."
created = "2026-06-11"
review_after = "2026-07-11"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# OO method signature-help seams (PR #1301 Slice 1): ripr#1429-class activation-
# tracing limitation in is_method_call_context / resolve_method_in_workspace.
#
# Seven seams flagged in crates/perl-lsp-rs/src/runtime/language/hover/signature_help.rs:
#   (A) 3 `weakly_gripped` seams — paren-scan loop predicates and depth==0 check
#       in is_method_call_context; RIPR cannot trace coverage from unit-test
#       string literals through the char-by-char scan to the predicate exit.
#   (B) 2 equality-boundary seams — depth==1 boundary in paren scanner, and
#       sym.name == method_name exact-match filter in resolve_method_in_workspace;
#       RIPR names missing discriminator `1` and `method_name` respectively.
#   (C) 4 `activation_unknown` seams — the cfg(feature="workspace")-gated
#       resolve_method_in_workspace body including the IndexAccessMode match,
#       workspace_document_text call, and Parser::new chain; RIPR cannot
#       statically determine activation because the workspace feature guard and
#       trait-object dispatch break static call-graph traversal.
#
# Coverage evidence:
#   is_method_call_context: 6 unit tests in #[cfg(test)] mod tests (exact bool
#   assertions: arrow-method, class-method, plain function, builtin, inside-args,
#   empty text). 3 integration tests in lsp_signature_help_tests.rs exercise the
#   full ->method( path end-to-end.
#   resolve_method_in_workspace: lib-level tests cover the graceful-None path
#   (no-index-ready, empty-name). Full resolution path covered by integration
#   test test_oo_method_signature_help_shows_params_for_known_method.
#
# This is the ripr#1429 predicate-infection-untraceable class: RIPR cannot
# statically trace activation from string/char literals through iterative
# scan loops, feature-gated code blocks, or trait-object dispatch. Same
# family as ripr-suppress-phase-block-description-match-arms and
# ripr-suppress-canonical-quote-operator-helper.
# Suppression is minimal (single production file, one PR). Remove when
# ripr#1429 is fixed upstream or when targeted mutation is scheduled.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-oo-method-signature-help-seams"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-lsp-rs/src/runtime/language/hover/signature_help.rs"]
classification = ["static_unknown", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "Seven ripr#1429-class seams in is_method_call_context and resolve_method_in_workspace (PR #1301 Slice 1): (A) 3 weakly_gripped paren-scan predicates — RIPR cannot trace coverage from char-literal unit tests through the iterative scan loop to predicate exits; (B) 2 equality-boundary seams (depth==1 and sym.name==method_name) with missing discriminators — same activation-tracing limit; (C) 4 activation_unknown seams in the cfg(feature='workspace')-gated resolve_method_in_workspace body — RIPR cannot statically traverse the feature guard and IndexAccessMode trait dispatch. All paths are covered: 6 unit tests for is_method_call_context (exact bool assertions), 2 lib-level None-path tests for resolve_method_in_workspace, and 3 integration tests for the full workspace-resolution path. Suppression is narrow (single production file, one PR). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled post-review."
created = "2026-06-11"
review_after = "2026-07-11"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# OO method inlay hints (PR #1302 Slice 2): ripr#1429-class activation-
# untraceable seams in two production files.
#
# File 1 — impl_inlay_hints.rs: the new NodeKind::MethodCall arm in
# parameter_hints_with_resolver (lines ~400-462). Six seam classes:
#   (A) user_sigs HashMap lookup + clone inside if-let chain
#   (B) method_resolver closure dispatch (dyn Fn call through trait object)
#   (C) resolver(method.as_str()) return: Some(names) vs None arm
#   (D) all_param_names.is_empty() predicate (true → &[] slice)
#   (E) param_names.len() <= 1 predicate (true → noise suppression)
#   (F) range filter inside arg loop (pos_in_range predicate)
# RIPR cannot statically trace: closure dispatch through &dyn Fn, AST walk
# callback (walk_ast visitor), and predicate branch propagation through the
# recursive walk chain — same ripr#1429 limit as all sibling suppressions.
# 6 inline #[cfg(test)] lib tests in impl_inlay_hints::tests exercise all
# branches directly (tests A–F above) and run under cargo test --lib.
#
# File 2 — misc.rs: the ws_resolver closure (lines ~357-369) gated on
# #[cfg(feature = "workspace")]. Seam classes:
#   (G) #[cfg(feature="workspace")] feature guard — RIPR cannot traverse
#   (H) sig.get("parameters")?.as_array()?   — chained Option short-circuits
#   (I) p.get("label")?.as_str()             — Option short-circuit in filter_map
#   (J) names.is_empty()                     — empty-names guard predicate
#   (K) LspServer method dispatch (resolve_method_in_workspace trait call)
#
# Coverage evidence:
#   impl_inlay_hints.rs seams: 6 inline lib tests (A–F) in tests mod plus
#   15 integration tests in tests/inlay_hints_user_subs_unit.rs.
#   misc.rs seams: the 6 inline lib tests exercise the resolver protocol via
#   parameter_hints_with_resolver; integration test
#   test_method_call_workspace_resolver_provides_hints covers the mock-resolver
#   path; full workspace resolution covered by #1301's integration tests.
#
# Suppression is narrow: exactly two production files, one PR (#1302 Slice 2).
# Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-oo-method-inlay-hints-ws-resolver"
kind = "predicate_infection_untraceable"
paths = [
  "crates/perl-lsp-rs-core/src/providers/inlay_hints/impl_inlay_hints.rs",
  "crates/perl-lsp-rs/src/runtime/language/misc.rs",
]
classification = ["static_unknown", "no_static_path", "activation_unknown", "weakly_exposed"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "Two ripr#1429-class files from PR #1302 Slice 2 (OO method inlay hints). impl_inlay_hints.rs: the new NodeKind::MethodCall arm in parameter_hints_with_resolver — RIPR cannot trace the dyn Fn resolver dispatch, walk_ast visitor callback chain, or predicate branch propagation (if-let chains, is_empty(), len()<=1, pos_in_range). All 6 branch paths are covered by inline #[cfg(test)] lib tests in impl_inlay_hints::tests plus 15 integration tests in inlay_hints_user_subs_unit.rs. misc.rs: the ws_resolver closure is gated on #[cfg(feature='workspace')], dispatches through LspServer trait, and contains JSON ? chains — same class as ripr-suppress-oo-method-signature-help-seams (PR #1301). Coverage: 6 inline lib tests exercise the resolver protocol; integration tests cover the mock-resolver path. Suppression is minimal: exactly two production files, one PR. Remove when ripr#1429 is fixed upstream."
created = "2026-06-11"
review_after = "2026-07-11"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# quality_baseline.rs cfg(test) line-scanner predicates: string/line-scanning
# — activation untraceable by ripr 0.5.0 (ripr#1429 class).
#
# PR #1326 adds five helper functions to xtask/src/tasks/quality_baseline.rs:
#   cfg_test_line_numbers   — scans source text for #[cfg(test)] spans
#   structural_brace_delta  — counts structural braces in a line of Rust source
#   is_cfg_test_attr        — predicate: does a line contain #[cfg(test)]?
#   strip_cfg_test_lines    — strips test-block lines from LcovSummary
#   resolve_source_path     — resolves an LCOV SF: path to a filesystem path
#
# ripr 0.5.0 cannot statically trace activation from string-literal test inputs
# through the line-by-line scanning loops in cfg_test_line_numbers and
# structural_brace_delta to their internal predicate branches. The same
# ripr#1429 predicate-infection-untraceable class as sibling suppressions
# (parse_quote_operator_content, is_method_call_context, etc.).
#
# Coverage evidence: 10 inline #[cfg(test)] unit tests in quality_baseline.rs:
#   cfg_test_line_numbers_detects_inline_test_module
#   cfg_test_line_numbers_empty_file_returns_empty_set
#   cfg_test_line_numbers_handles_multiple_test_blocks
#   strip_cfg_test_lines_removes_test_block_lines_from_lcov
#   strip_cfg_test_lines_does_not_affect_production_hit_count
#   strip_cfg_test_lines_patch_coverage_full_for_test_only_pr
#   structural_brace_delta_counts_only_structural_braces
#   structural_brace_delta_skips_char_literals
#   structural_brace_delta_skips_single_line_string_literals
#   structural_brace_delta_balances_nested_blocks
#
# Suppression is narrow (single xtask file, one PR). Remove when ripr#1429 is
# fixed upstream or when targeted mutation is scheduled per #1289.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-quality-baseline-cfg-test-scanner"
kind = "predicate_infection_untraceable"
paths = ["xtask/src/tasks/quality_baseline.rs"]
classification = ["weakly_exposed", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "Five ripr#1429-class seams in PR #1326 cfg(test)-strip helpers: cfg_test_line_numbers, structural_brace_delta, is_cfg_test_attr, strip_cfg_test_lines, resolve_source_path. RIPR cannot statically trace activation from string-literal test inputs through the line-by-line scanning loops to internal predicate branches (same class as parse_quote_operator_content and is_method_call_context). All seams are covered by 10 inline #[cfg(test)] unit tests: 3 for cfg_test_line_numbers (inline_test_module, empty_file, multiple_blocks), 3 for strip_cfg_test_lines (removes_test_block_lines, does_not_affect_production_hit_count, patch_coverage_full_for_test_only_pr), and 4 for structural_brace_delta edge cases added by the deep-review correctness fix. Retirement tracked in #1289. Remove when ripr#1429 is fixed upstream or when targeted mutation is scheduled."
created = "2026-06-12"
review_after = "2026-07-12"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# uri/mod.rs file_path_uri + windows_file_path_uri: Option<Uri> chain predicates
# — activation untraceable by RIPR static analysis (ripr#1429 class).
#
# PR #1206 adds two private helper functions to the URI module:
#   file_path_uri(s: &str) -> Option<Uri>
#     — Url::from_file_path().ok()? and url.as_str().parse().ok()
#   windows_file_path_uri(s: &str) -> Option<Uri>
#     — chars().next()? guards, separator check, Url::parse().ok()? chain
#
# ripr cannot statically trace activation from string-literal test inputs
# through the Option-chain `?` operators in the path-detection helpers — the same
# ripr#1429 predicate-infection-untraceable class that suppresses
# parse_quote_operator_content, is_method_call_context, etc.
#
# Coverage evidence:
#   8 inline #[cfg(test)] unit tests in the same file:
#     windows_file_path_uri_rejects_empty_string (empty guard)
#     windows_file_path_uri_rejects_single_char_string (single-char guard)
#     windows_file_path_uri_rejects_two_char_string (two-char guard)
#     windows_file_path_uri_accepts_backslash_path (backslash success path)
#     windows_file_path_uri_accepts_forward_slash_path (forward-slash success path)
#     file_path_uri_accepts_unix_absolute_path (Unix happy path, cfg(unix))
#     parse_uri_routes_windows_path_through_file_path_detection (Windows route)
#     parse_uri_routes_unix_path_through_file_path_detection (Unix route, cfg(unix))
#   2 integration tests in crates/perl-lsp-rs-core/tests/uri_module_shape.rs
#
# Suppression is narrow (single production file, one PR). Remove when ripr#1429
# is fixed upstream or when targeted mutation is scheduled per #1289.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-uri-file-path-helpers"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-lsp-rs-core/src/uri/mod.rs"]
classification = ["weakly_exposed", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "Two ripr#1429-class seams in PR #1206 URI helpers: file_path_uri and windows_file_path_uri are Option<Uri>-chain helpers whose internal ? guards (Url::from_file_path().ok()?, chars().next()?, separator check, Url::parse().ok()?) cannot be traced by ripr from string-literal test inputs. All predicate branches are covered by 8 inline #[cfg(test)] unit tests covering both None-return guards and happy paths (Windows backslash, Windows forward-slash, Unix absolute path). Same ripr#1429 class as parse_quote_operator_content and is_method_call_context. Suppression is narrow (single production file). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
created = "2026-06-12"
review_after = "2026-07-12"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# semantic_tokens_document_not_open: format! helper — weakly_exposed despite
# direct unit test coverage (ripr#1429 class).
#
# PR #1206 extracts the inline error construction in handle_semantic_tokens into
# a named helper:
#   fn semantic_tokens_document_not_open(uri: &str) -> JsonRpcError { ... }
#
# ripr classifies this as weakly_exposed because the call path from the
# inline #[cfg(test)] unit test (semantic_tokens_closed_document_error_includes_
# sync_guidance) flows through server.handle_semantic_tokens → get_document →
# ok_or_else closure → semantic_tokens_document_not_open. RIPR cannot statically
# trace the call chain through the ok_or_else closure to the helper — the same
# ripr#1429 propagation-path limitation that suppresses
# find_phase_block_at_offset (hover.rs).
#
# Coverage evidence:
#   1 inline #[cfg(test)] unit test verifies all 5 guidance strings:
#     semantic_tokens_closed_document_error_includes_sync_guidance
#   Assertions check: code == INVALID_REQUEST, data.is_none(),
#   and message.contains for "Document not open", uri, "textDocument/semanticTokens/full",
#   "textDocument/didOpen", "open and synchronized".
#
# Suppression is narrow (single production file, one PR). Remove when ripr#1429
# is fixed upstream or targeted mutation is scheduled per #1289.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-semantic-tokens-document-not-open-helper"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-lsp-rs/src/runtime/language/semantic_tokens.rs"]
classification = ["weakly_exposed"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "semantic_tokens_document_not_open is a ripr#1429-class seam in PR #1206: RIPR cannot statically trace activation from the inline #[cfg(test)] unit test (semantic_tokens_closed_document_error_includes_sync_guidance) through server.handle_semantic_tokens → get_document → ok_or_else closure → semantic_tokens_document_not_open. Same propagation-path limitation as find_phase_block_at_offset (hover.rs). The function is covered by a dedicated unit test with 5 exact message-content assertions. Suppression is narrow (single production file). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
created = "2026-06-12"
review_after = "2026-07-12"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# DAP stack-frame lifecycle seams (fixes #964 + #933): Mutex-guard and
# branch-predicate activation untraceable by ripr 0.9.0 (ripr#1429 class)
# across three changed debug_adapter files.
#
# (A) execution.rs — 6 resume handlers (handle_continue, handle_next,
#     handle_step_in, handle_step_out, handle_pause, handle_goto):
#     lock_or_recover and stack_frames.clear() are weakly_gripped seams;
#     RIPR cannot trace Mutex Some-branch activation at compile time.
#
# (B) frames.rs — degraded-transport Vec::new() fallback at line 73:
#     reachable_unrevealed seam — execution requires framed transport failure,
#     which RIPR cannot statically confirm is exercised.
#
# (C) mod.rs — three new #[cfg(test)] helper methods added to the impl block:
#     seed_session_for_test, spawn_noop_child_for_test,
#     inject_stack_frames_for_test, stack_frames_snapshot_for_test.
#     ripr#1428-class false-positives: diff-scoped analysis treats #[cfg(test)]
#     methods in the production impl block as new production seams.
#
# All seams are covered by 5 unit tests (test_handle_continue/next/step_in/
# step_out/pause_clears_stack_frames) and 1 degraded-path test
# (test_stack_trace_does_not_use_snapshot_in_degraded_path).
# Remove when ripr#1429 and ripr#1428 are fixed upstream.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-dap-stack-frame-lifecycle"
kind = "predicate_infection_untraceable"
paths = [
  "crates/perl-dap/src/debug_adapter/execution.rs",
  "crates/perl-dap/src/debug_adapter/frames.rs",
  "crates/perl-dap/src/debug_adapter/mod.rs",
]
classification = ["reachable_unrevealed", "weakly_gripped", "call_presence"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "ripr#1429-class seams across three debug_adapter files: (A) execution.rs weakly_gripped seams in all 6 resume handlers — lock_or_recover and stack_frames.clear(); RIPR cannot trace Mutex Some at compile time. (B) frames.rs reachable_unrevealed seam — Vec::new() degraded-transport fallthrough (line 73); reachable but RIPR cannot statically confirm coverage. (C) mod.rs reachable_unrevealed seams from four new #[cfg(test)] test-helper methods (seed_session_for_test, spawn_noop_child_for_test, inject_stack_frames_for_test, stack_frames_snapshot_for_test); ripr#1428-class false-positives (diff-scoped analysis treats #[cfg(test)] methods in production impl block as new production seams). All seams are covered by 5 unit tests (test_handle_continue/next/step_in/step_out/pause_clears_stack_frames) and 1 degraded-path integration test. Remove when ripr#1429 and ripr#1428 are fixed upstream."
created = "2026-06-12"
review_after = "2026-07-12"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# DAP invalid-ref hardening test helpers (PR #901): ripr#1428-class false-
# positives in #[cfg(test)] mod hazard_invariant_tests helper functions.
#
# PR #901 adds a #[cfg(test)] mod hazard_invariant_tests at the bottom of
# variables.rs with two helper functions:
#   variables_body_is_empty(adapter, variables_ref) -> bool
#   variables_success(adapter, variables_ref) -> bool
#
# ripr 0.9.0 flags 4 seams inside these helpers as severe_gaps:
#   line 584: Some(json!({...})) call in variables_body_is_empty — call_presence
#   line 591: `let vars = body` match arm — call_presence / weakly_gripped
#   line 597: `vars == 0` boundary discriminator — activation_unknown
#   line 607: Some(json!({...})) call in variables_success — call_presence
#
# All four are ripr#1428-class false-positives: diff-scoped analysis treats the
# internal plumbing of these test helper functions as new production seams.
# The helper functions call handle_request (the production seam under test) and
# decode its response — they are pure test infrastructure, not production code.
#
# The production seam (handle_variables invalid-ref guard, stale-ref guard) IS
# covered by 9 integration tests in dap_variable_reference_hardening_tests.rs
# and 8 inline lib tests in hazard_invariant_tests that exercise the helpers.
# The helpers themselves are test-only scaffolding — same ripr#1428 class as
# ripr-suppress-quote-operator-conformance-test-helpers and
# ripr-suppress-seam-proof-tests. Remove when ripr#1428 is fixed upstream.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-dap-invalid-ref-hardening-test-helpers"
kind = "test_receipt_surface"
paths = ["crates/perl-dap/src/debug_adapter/variables.rs"]
classification = ["call_presence", "weakly_gripped", "activation_unknown"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "Four ripr#1428-class false-positives in PR #901 hazard_invariant_tests helper functions (variables_body_is_empty and variables_success in the #[cfg(test)] mod): lines 584/591/607 are call_presence/weakly_gripped seams — handle_request call sites inside test helpers; line 597 is activation_unknown (vars == 0 boundary). These are pure test infrastructure, not production seams. The production invalid-ref guard (variables_ref_raw <= 0 || > i32::MAX, stale-ref DebugState::Stopped check) is covered by 9 integration tests in dap_variable_reference_hardening_tests.rs and 8 inline lib tests via hazard_invariant_tests. Same ripr#1428 class as ripr-suppress-quote-operator-conformance-test-helpers and ripr-suppress-seam-proof-tests. Remove when ripr#1428 is fixed upstream."
created = "2026-06-12"
review_after = "2026-07-12"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# perl-dap CLI entry point (PR #3277): ripr#1429-class no_static_path on the
# clap-derived Args struct after the --bridge flag removal. Bin entry point
# (excluded from lcov in the same PR); CLI surface covered by in-process #[bin]
# tests. Same proc_macro_opaque class as the native_product_surface / xtask CLI
# suppressions.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-perl-dap-cli-entry-point"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-dap/src/main.rs"]
classification = ["no_static_path", "weakly_exposed"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #3277 (epic #3276, PR1) removes the shipped --bridge flag so the perl-dap CLI is native-only. The change reshapes the clap-derived Args struct in crates/perl-dap/src/main.rs; ripr 0.9.x reports a no_static_path seam on the derive because it cannot trace activation through the clap #[derive(Parser)] proc-macro to a static test path. perl-dap/src/main.rs is a bin entry point (excluded from lcov coverage in this same PR); its CLI surface is covered by in-process #[bin] tests that render clap help and parse args (cli_help_has_no_bridge_product_surface, cli_rejects_removed_bridge_flag) plus the resolve_socket_port transport tests. Same proc_macro_opaque ripr#1429 class as the native_product_surface and xtask CLI suppressions. Suppression is narrow (single bin entry file, one PR #3277). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
created = "2026-07-03"
review_after = "2026-08-03"
expires = "2026-10-03"

# -----------------------------------------------------------------------------
# LSP declaration equality-guard seams (PR #854): ripr#1429-class boundary
# predicates in collect_subroutine_declarations and collect_package_declarations.
#
# PR #854 adds three new match guard arms in declaration.rs:
#   (1) NodeKind::Subroutine guard: name_str == sub_name (line ~848)
#   (2) NodeKind::Method guard:     method_name == sub_name (line ~851)
#   (3) NodeKind::Package/Class guard: name == pkg_name (line ~876)
#
# ripr 0.9.0 flags these as "input that hits the boundary" seams -- equality
# guard predicates whose activation RIPR cannot statically confirm via its
# call-graph tracing. Both sides of each boundary ARE exercised by lib tests:
#   - subroutine_decl_collect_subroutine_declarations_finds_subroutine (positive)
#   - subroutine_decl_boundary_discriminator_rejects_different_sub_name (negative)
#   - method_decl_boundary_discriminator_rejects_different_method_name (negative)
#   - class_decl_boundary_discriminator_rejects_different_class_name (negative)
#   - package_decl_collect_package_declarations_finds_package (positive)
# All five tests are in perl-semantic-analyzer/src/analysis/declaration.rs
# #[cfg(test)] mod tests.
#
# The ripr 0.9.0 static tracer cannot follow string equality guards across
# match arms -- same class as ripr#1429 (Mutex guard / predicate-infection
# activation untraceable). Remove when ripr is updated to trace equality seams.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-lsp-declaration-equality-guards"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-semantic-analyzer/src/analysis/declaration.rs"]
classification = ["weakly_exposed", "reachable_unrevealed", "activation_unknown", "weakly_gripped"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "ripr#1429-class equality-guard seams in collect_subroutine_declarations (name_str == sub_name, method_name == sub_name) and collect_package_declarations (name == pkg_name): match-guard equality predicates whose activation RIPR 0.9.0 cannot statically confirm via call-graph tracing. Both sides of all three boundaries are covered by 5 inline lib tests: subroutine_decl_collect_subroutine_declarations_finds_subroutine, subroutine_decl_boundary_discriminator_rejects_different_sub_name, method_decl_boundary_discriminator_rejects_different_method_name, package_decl_collect_package_declarations_finds_package, class_decl_boundary_discriminator_rejects_different_class_name. Remove when ripr can trace equality guard activation."
created = "2026-06-13"
review_after = "2026-07-13"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# LSP smoke-envelope tests: newly added test file with test-harness plumbing.
# Tracks EffortlessMetrics/ripr#1428 (RIPR flags test-harness internal logic in
# newly added test files as production seams). The lsp_smoke_e2e.rs test file
# adds two new end-to-end tests for codeAction and inlineCompletion coverage
# (#949). The test functions use request/response helper scaffolding and assertions
# that RIPR's diff-scoped analyzer classifies as new production seams.
#
# Coverage evidence: Both new test functions exercise the real JSON-RPC/stdio
# transport path for codeAction (lsp_smoke_e2e_code_action_envelope) and
# inlineCompletion (lsp_smoke_e2e_inline_completion_envelope). The test setup
# helpers (send_request_with_timeout, unique_test_uri) are shared with the
# existing smoke tests in the same file (lsp_smoke_e2e_document_intelligence_shapes,
# lsp_smoke_e2e_will_save_wait_until_request_response, etc.); RIPR treats the
# newly added test helper calls in the new test functions as new production seams.
#
# This is the same class as ripr-suppress-ux-receipt-tests and
# ripr-suppress-quote-operator-conformance-test-helpers (ripr#1428: test-harness
# plumbing falsely classified as production seams when added to a test file).
# Remove when ripr#1428 is fixed upstream.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-lsp-smoke-envelope-tests"
kind = "test_receipt_surface"
paths = ["crates/perl-lsp-rs/tests/lsp_smoke_e2e.rs"]
classification = ["call_presence", "weakly_exposed", "static_unknown"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "Two ripr#1428-class false-positives in PR #1283 lsp_smoke_e2e.rs: lsp_smoke_e2e_code_action_envelope and lsp_smoke_e2e_inline_completion_envelope are new end-to-end tests for codeAction and inlineCompletion envelope coverage (#949). The test functions call send_request_with_timeout and shared helpers to exercise the real JSON-RPC/stdio transport path. RIPR's diff-scoped analyzer treats the newly added test-helper calls as new production seams. All seams exercised are production handlers (codeAction and inlineCompletion request/response envelope shapes, capability advertisement) exercised via real transport. This is the same ripr#1428 class as ripr-suppress-ux-receipt-tests and ripr-suppress-quote-operator-conformance-test-helpers (test-harness plumbing falsely classified as production seams). Suppression is minimal (single test file, test-only additions to the existing lsp_smoke_e2e.rs harness). Remove when ripr#1428 is fixed upstream."
created = "2026-06-13"
review_after = "2026-07-13"
expires = "2026-09-30"

# Panic-guard test files (PR #1372): ripr#1428-class false-positives in newly
# added test files. Tracks EffortlessMetrics/ripr#1428 (RIPR flags test-harness
# plumbing in newly added test files as production seams).
#
# PR #1372 adds two new test files:
#   crates/perl-parser/tests/red_tdd_panic_on_user_input.rs
#   crates/perl-parser/tests/nodekind_combination_error_handling_edge_cases.rs
#
# These are pure test files with test functions exercising parser panic-guard
# coverage. RIPR's diff-scoped analysis flags the test function scaffolding
# (test setup, assertions) as new production seams. This is the same ripr#1428
# class as ripr-suppress-ux-receipt-tests and ripr-suppress-seam-proof-tests.
#
# Remove when ripr#1428 is fixed upstream.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-panic-guard-test-files"
kind = "test_receipt_surface"
paths = [
  "crates/perl-parser/tests/red_tdd_panic_on_user_input.rs",
  "crates/perl-parser/tests/nodekind_combination_error_handling_edge_cases.rs",
]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "Two newly added test files in PR #1372 are flagged as new production seams by diff-scoped RIPR analysis: red_tdd_panic_on_user_input.rs and nodekind_combination_error_handling_edge_cases.rs. Test-function scaffolding (test setup, assertions) is classified as production seams. This is the same ripr#1428 class as ripr-suppress-ux-receipt-tests and ripr-suppress-seam-proof-tests (test-harness plumbing falsely classified as production seams when added to a test file). Both files contain pure test coverage for parser panic guards (issue #1372). Remove when ripr#1428 is fixed upstream."
created = "2026-06-13"
review_after = "2026-07-13"
expires = "2026-09-30"

# agent_ledgers.rs validate_line string-dispatch predicates: field-name matching
# and classification/confidence enum dispatch — activation untraceable by ripr
# (ripr#1429 class).
#
# PR #1260 adds xtask/src/tasks/agent_ledgers.rs with two ripr#1429-class seams
# flagged as weakly_exposed:
#   (A) validate_line — string-field dispatch on JSON object keys ("pr", "title",
#       "classification", "confidence", etc.): RIPR cannot trace that calling
#       validate_line with a string-literal JSON row exercises the correct field-
#       match branch, because activation flows through serde_json deserialization
#       then HashMap field lookup — same class as parse_quote_operator_content.
#   (B) relative_display — std::env::current_dir().unwrap_or_default() and
#       path.strip_prefix(&cwd).unwrap_or(path): the fallback seams are
#       ripr#1429-class unreachable-in-test (current_dir always succeeds in
#       test environment; strip_prefix fallback only triggers when cwd != path).
#
# Coverage evidence: 19 inline #[cfg(test)] unit tests in agent_ledgers.rs cover
# every field-name branch, every classification, every confidence value, the
# close_proof conditional, cleanup_done/known_gaps boolean/array checks, and
# error metadata (file + line number propagation).
#
# Suppression is narrow (single xtask file, one PR). Remove when ripr#1429 is
# fixed upstream or when targeted mutation is scheduled per #1289.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-agent-ledgers-validate-line-predicates"
kind = "predicate_infection_untraceable"
paths = ["xtask/src/tasks/agent_ledgers.rs"]
classification = ["weakly_exposed", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "Two ripr#1429-class seams in PR #1260 agent_ledgers.rs: (A) validate_line string-field dispatch — RIPR cannot trace coverage from string-literal JSON test rows through serde_json deserialization + HashMap field lookup to the per-field predicate branches (same class as parse_quote_operator_content and quality_baseline cfg-test-line-scanner); (B) relative_display fallback seams — unwrap_or_default on current_dir and unwrap_or on strip_prefix are ripr#1429-class unreachable-in-test predicates. All 19 inline #[cfg(test)] unit tests cover every field-name branch, classification value, confidence value, close_proof conditional, and error-metadata propagation. Suppression is narrow (single xtask file, one PR #1260). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
created = "2026-06-13"
review_after = "2026-07-13"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# pr_close_proof.rs validate_sha_format char-predicate and exit-code match arms:
# string/predicate-chain activation untraceable by ripr (ripr#1429 class).
#
# PR #1264 adds xtask/src/tasks/pr_close_proof.rs with one ripr#1429-class seam
# flagged as weakly_exposed:
#   validate_sha_format — four predicate branches (empty-string guard,
#   whitespace-contains guard, chars().all(is_ascii_hexdigit) predicate,
#   len < 7 bound): RIPR cannot statically trace that calling validate_sha_format
#   with a specific string-literal activates the correct predicate branch, because
#   activation flows through .chars().all() — a closure-based iterator predicate
#   identical in class to parse_quote_operator_content's char-scan seams.
#
# Coverage evidence: 6 inline #[cfg(test)] unit tests in pr_close_proof.rs cover
# every guard and boundary: test_validate_sha_empty_fails, _with_spaces_fails,
# _non_hex_fails, _too_short_fails, _valid_short (Ok path), _valid_full (Ok path).
# All branches (empty, whitespace, non-hex, too-short, valid) are asserted.
#
# Suppression is narrow (single xtask file, one PR). Remove when ripr#1429 is
# fixed upstream or when targeted mutation is scheduled per #1289.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-pr-close-proof-sha-predicates"
kind = "predicate_infection_untraceable"
paths = ["xtask/src/tasks/pr_close_proof.rs"]
classification = ["weakly_exposed", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "One ripr#1429-class seam in PR #1264 pr_close_proof.rs: validate_sha_format has four predicate branches (empty-string guard, whitespace-contains, chars().all(is_ascii_hexdigit), len < 7 bound) that ripr cannot statically trace from string-literal test inputs through the closure-based iterator predicate — same class as parse_quote_operator_content's char-scan seams and quality_baseline's line-scanner predicates. All 6 inline #[cfg(test)] unit tests cover every predicate branch and boundary (test_validate_sha_empty_fails, _with_spaces_fails, _non_hex_fails, _too_short_fails, _valid_short, _valid_full). Suppression is narrow (single xtask file, one PR #1264). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
created = "2026-06-13"
review_after = "2026-07-13"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# NodeKind combination edge-cases test: ripr#1428-class false-positive in newly
# added test file. Tracks EffortlessMetrics/ripr#1428 (RIPR flags test-harness
# plumbing in newly added test files as production seams).
#
# PR #651 (codex test coverage PR for issue #1442) adds a new test file:
#   crates/perl-parser/tests/nodekind_combination_edge_cases.rs
#
# This is a pure test file with test functions exercising parser NodeKind
# combination coverage. RIPR's diff-scoped analysis flags the test function
# scaffolding (test setup, assertions) as new production seams. This is the
# same ripr#1428 class as ripr-suppress-ux-receipt-tests and
# ripr-suppress-seam-proof-tests.
#
# Remove when ripr#1428 is fixed upstream.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-nodekind-combination-test"
kind = "test_receipt_surface"
paths = ["crates/perl-parser/tests/nodekind_combination_edge_cases.rs"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "nodekind_combination_edge_cases.rs is a newly added test file in PR #651 whose internal test-function scaffolding is flagged as new production seams by diff-scoped RIPR analysis. This is the same ripr#1428 class as ripr-suppress-ux-receipt-tests and ripr-suppress-seam-proof-tests (test-harness plumbing falsely classified as production seams when added to a test file). The file contains pure test coverage for NodeKind combination edge cases (issue #1442). Remove when ripr#1428 is fixed upstream."
created = "2026-06-13"
review_after = "2026-09-13"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# Fuzz target workspace_inputs: ripr#1428-class false-positive in newly added
# fuzz target. Tracks EffortlessMetrics/ripr#1428 (RIPR flags fuzz harness
# plumbing in newly added targets as production seams).
#
# PR #686 (codex fuzz coverage PR for issue #1175) adds a new fuzz target:
#   fuzz/fuzz_targets/workspace_inputs.rs
#
# This is a pure fuzz target with harness scaffolding exercising workspace input
# coverage. RIPR's diff-scoped analysis flags the fuzzing harness plumbing
# (fuzz input setup, assertions) as new production seams. This is the same
# ripr#1428 class as ripr-suppress-ux-receipt-tests and
# ripr-suppress-seam-proof-tests.
#
# Remove when ripr#1428 is fixed upstream.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-fuzz-workspace-inputs-target"
kind = "test_receipt_surface"
paths = ["fuzz/fuzz_targets/workspace_inputs.rs"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "workspace_inputs.rs is a newly added fuzz target in PR #686 whose internal fuzz-harness scaffolding is flagged as new production seams by diff-scoped RIPR analysis. This is the same ripr#1428 class as ripr-suppress-ux-receipt-tests and ripr-suppress-seam-proof-tests (fuzz harness plumbing falsely classified as production seams when added to a fuzz target). The file contains pure fuzz coverage for workspace inputs (issue #1175). Remove when ripr#1428 is fixed upstream."
created = "2026-06-13"
review_after = "2026-09-13"
expires = "2026-09-30"

# DAP variable-reference test files (PR #1445): ripr#1428-class false-positives
# in newly added test files. Tracks EffortlessMetrics/ripr#1428 (RIPR flags
# test-harness plumbing in newly added test files as production seams).
#
# PR #1445 adds two new test files:
#   crates/perl-dap/tests/dap_fallback_scope_variables_collision_tests.rs
#   crates/perl-dap/tests/dap_var_ref_arithmetic_guard_tests.rs
#
# These are pure test files with test functions exercising DAP variable-
# reference collision and arithmetic hardening. RIPR's diff-scoped analysis
# flags the test function scaffolding (test setup, assertions) as new
# production seams. This is the same ripr#1428 class as ripr-suppress-ux-
# receipt-tests and ripr-suppress-seam-proof-tests.
#
# Remove when ripr#1428 is fixed upstream.
# ---------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-dap-variable-ref-tests"
kind = "test_receipt_surface"
paths = [
  "crates/perl-dap/tests/dap_fallback_scope_variables_collision_tests.rs",
  "crates/perl-dap/tests/dap_var_ref_arithmetic_guard_tests.rs",
]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "Two newly added test files in PR #1445 are flagged as new production seams by diff-scoped RIPR analysis: dap_fallback_scope_variables_collision_tests.rs and dap_var_ref_arithmetic_guard_tests.rs. Test-function scaffolding (test setup, assertions) is classified as production seams. This is the same ripr#1428 class as ripr-suppress-ux-receipt-tests and ripr-suppress-seam-proof-tests (test-harness plumbing falsely classified as production seams when added to a test file). Both files contain pure test coverage for DAP variable reference collision hardening (issue #1445). Remove when ripr#1428 is fixed upstream."
created = "2026-06-13"
review_after = "2026-07-13"
expires = "2026-09-30"

# Custom sub-attribute test file (PR #1361): ripr#1428-class false-positive in
# newly added test file. Tracks EffortlessMetrics/ripr#1428 (RIPR flags
# test-harness plumbing in newly added test files as production seams).
#
# PR #1361 adds one new test file:
#   crates/perl-parser-core/tests/sub_attribute_custom_tests.rs
#
# This is a pure test file with 20 test functions exercising correct parser
# behavior for custom subroutine attributes (:public, :cached(...),
# :Path('/path'), etc.). RIPR's diff-scoped analysis flags the test function
# scaffolding (Parser::new, parser.parse, parser.get_errors, assertions) as
# new production seams. This is the same ripr#1428 class as
# ripr-suppress-ux-receipt-tests and ripr-suppress-seam-proof-tests.
#
# Remove when ripr#1428 is fixed upstream.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-sub-attribute-custom-tests"
kind = "test_receipt_surface"
paths = ["crates/perl-parser-core/tests/sub_attribute_custom_tests.rs"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "sub_attribute_custom_tests.rs is a newly added test file in PR #1361 whose internal test-function scaffolding is flagged as new production seams by diff-scoped RIPR analysis. The file contains 20 test functions verifying that custom subroutine attributes (Catalyst :Path, Moose :public, etc.) parse cleanly without false-positive errors after the #1361 fix, plus 2 error-guard tests verifying genuine syntax errors (unterminated parens, missing name) still produce diagnostics. Test-function scaffolding (Parser::new, parser.parse, parser.get_errors, assertions) is classified as production seams. This is the same ripr#1428 class as ripr-suppress-ux-receipt-tests and ripr-suppress-seam-proof-tests (test-harness plumbing falsely classified as production seams when added to a test file). Remove when ripr#1428 is fixed upstream."

# Nested variable list test file (PR #1362): ripr#1428-class false-positive
# in newly added test file. Tracks EffortlessMetrics/ripr#1428 (RIPR flags
# test-harness plumbing in newly added test files as production seams).
#
# PR #1362 adds one new test file:
#   crates/perl-parser-core/tests/nested_varlist_tests.rs
#
# This is a pure test file with test functions exercising nested variable
# list declaration coverage. RIPR's diff-scoped analysis flags the test
# function scaffolding (test setup, assertions) as new production seams. This
# is the same ripr#1428 class as ripr-suppress-ux-receipt-tests and ripr-
# suppress-seam-proof-tests. (Reviewer's other tests added to existing files
# comprehensive_unit_tests.rs and surface_decl.rs do not need suppression.)
#
# Remove when ripr#1428 is fixed upstream.
# -------------------------------------------------------
[[suppress]]
id = "ripr-suppress-1362-nested-varlist-test-file"
kind = "test_receipt_surface"
paths = [
  "crates/perl-parser-core/tests/nested_varlist_tests.rs",
]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "One newly added test file in PR #1362 is flagged as new production seams by diff-scoped RIPR analysis: nested_varlist_tests.rs. Test-function scaffolding (test setup, assertions) is classified as production seams. This is the same ripr#1428 class as ripr-suppress-ux-receipt-tests and ripr-suppress-seam-proof-tests (test-harness plumbing falsely classified as production seams when added to a test file). Reviewer's other tests were added to existing comprehensive_unit_tests.rs and surface_decl.rs and do not require suppression. The file contains pure test coverage for nested variable list declaration (issue #1362). Remove when ripr#1428 is fixed upstream."
created = "2026-06-13"
review_after = "2026-07-13"
expires = "2026-09-30"

# GA rename promotion (PR #1386): ripr#1429-class no_static_path seam in
# refactor_runtime_blocker_receipts.rs — cfg(feature="workspace")-gated
# function with wildcard match arm after UnsupportedEditCategory deletion.
#
# PR #1386 promotes cross-package rename to GA: all PlannedEditCategory
# variants (Definition, Reference, ImportList, ExportList) are now accepted.
# The explicit match arm `UnsupportedEditCategory => "unsupported_edit_category"`
# was deleted from `rename_package_pilot_ineligible_reason` and the variant
# now falls through to `_ => "unknown"`.
#
# RIPR classifies this as no_static_path because:
#   (A) The function is gated by #[cfg(all(feature = "workspace",
#       not(target_arch = "wasm32")))] — RIPR cannot statically trace
#       the feature guard and therefore cannot attribute any test to the
#       deleted arm's replacement wildcard path.
#   (B) The wildcard `_ => "unknown"` is a catch-all; RIPR cannot
#       determine which enum variants (including UnsupportedEditCategory)
#       exercise it statically.
#
# Coverage evidence: the rename GA promotion is covered by the integration
# test suite for package rename in PR #1386 (import/export path tests).
# The `_ => "unknown"` arm is exercised via `EmptyPlan` and `Blocked`
# paths in existing receipt tests. UnsupportedEditCategory is now
# unreachable in practice (all categories accepted since GA promotion).
#
# This is the ripr#1429 predicate-infection-untraceable class: RIPR cannot
# statically trace activation through cfg(feature) guards or wildcard match
# arms. Same family as ripr-suppress-oo-method-signature-help-seams.
# Suppression is narrow (single production file, one PR). Remove when
# ripr#1429 is fixed upstream or UnsupportedEditCategory is re-added with
# a discriminator test.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-rename-ga-blocker-receipts"
kind = "activation_unknown"
paths = ["crates/perl-lsp-rs/src/runtime/language/refactor_runtime_blocker_receipts.rs"]
classification = ["no_static_path", "static_unknown"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #1386 GA rename promotion deletes the explicit UnsupportedEditCategory match arm from rename_package_pilot_ineligible_reason (cfg(feature='workspace') gated), replacing it with _ => 'unknown'. RIPR classifies the wildcard arm as no_static_path because (A) the cfg(feature='workspace') guard breaks static call-graph traversal and (B) the wildcard catch-all cannot be attributed to UnsupportedEditCategory statically. Coverage: rename GA integration tests cover all active categories (Definition, Reference, ImportList, ExportList). UnsupportedEditCategory is unreachable post-GA (all categories accepted). Suppression is narrow: single production file, one PR. Remove when ripr#1429 is fixed upstream or a discriminator test is added for the wildcard arm."
created = "2026-06-14"
review_after = "2026-07-14"
expires = "2026-09-30"

# Negative bareword fat arrow test file (PR #1355): ripr#1428-class
# false-positive in newly added test file. Tracks EffortlessMetrics/ripr#1428
# (RIPR flags test-harness plumbing in newly added test files as production
# seams).
#
# PR #1355 (fix(parser): handle negative keyword barewords before fat arrow)
# adds one new test file:
#   crates/perl-parser-core/tests/fix_1355_negative_bareword_fatarrow.rs
#
# This is a pure test file with 37 test functions exercising correct parser
# behavior for negative keyword barewords used as hash keys before fat arrows
# (e.g., -or => value, -and => value). The PR modifies
# crates/perl-parser-core/src/engine/parser/expressions/unary.rs (lines
# 190-199) to add lookahead checks (.peek_second()) for detecting fat-arrow
# patterns.
#
# RIPR's diff-scoped analysis flags 17 seams in unary.rs as "weakly_exposed"
# or "activation_unknown" because RIPR cannot statically trace whether the
# newly added test file exercises these seams. The test file has 369 lines of
# comprehensive behavioral coverage (positive tests for all word-operator
# keywords, realistic DBIx::Class/SQL::Abstract patterns, edge cases), which
# definitively proves the lookahead logic works. This is the same ripr#1428
# class as ripr-suppress-ux-receipt-tests and ripr-suppress-seam-proof-tests
# (test-harness plumbing / newly added test files causing static-analysis
# false-positives on production seams).
#
# Suppression is narrow: single test file, PR #1355. Remove when ripr#1428 is
# fixed upstream or when RIPR can statically trace test activation through
# newly added test modules.
# -------------------------------------------------------
[[suppress]]
id = "ripr-suppress-1355-negative-bareword-test-file"
kind = "test_receipt_surface"
paths = [
  "crates/perl-parser-core/tests/fix_1355_negative_bareword_fatarrow.rs",
]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "One newly added test file in PR #1355 is flagged as new production seams by diff-scoped RIPR analysis: fix_1355_negative_bareword_fatarrow.rs (17 weakly_exposed / activation_unknown seams in unary.rs lines 190-199). RIPR cannot statically trace whether the newly added test module exercises these seams. The test file contains 37 test functions with 369 lines of comprehensive behavioral coverage for negative keyword barewords before fat arrows (e.g., -or => value, -and => value, -cmp => value, realistic DBIx::Class patterns). This coverage definitively proves the lookahead logic (.peek_second() calls) works correctly. This is the same ripr#1428 class as ripr-suppress-ux-receipt-tests and ripr-suppress-seam-proof-tests (newly added test files causing static-analysis false-positives on production seams). Remove when ripr#1428 is fixed upstream."
created = "2026-06-15"
review_after = "2026-09-15"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# print_in_lib allow_scopes: ripr#1429-class iterator/closure/predicate seams.
#
# PR #624 extracts check_print_in_lib into commands/print_in_lib/ submodules.
# allow_scopes.rs introduces 5 weakly_gripped seams that RIPR cannot statically
# trace:
#   (A) file_has_print_allow: lines.iter().take(30).any(|line| ...) — iterator
#       chain with closure; RIPR cannot trace activation from unit test inputs
#       through the iterator combinator chain to the closure body.
#   (B) line_is_whole_line_comment: line.trim_start().starts_with("//") — string
#       predicate; RIPR cannot statically determine whether the starts_with
#       boundary is covered by discriminator tests.
#   (C) observe_line / apply_brace_delta: brace_delta() boundary predicates
#       and early-return conditions — RIPR cannot statically attribute the
#       signed/unsigned arithmetic boundary (saturating_sub) to specific test
#       inputs.
#
# Unit tests added by this PR directly call each function with boundary inputs:
#   file_has_print_allow_only_checks_first_30_lines (A, boundary at 30)
#   file_has_print_allow_detects_inner_print_stderr (A, true path)
#   line_is_whole_line_comment_true_for_double_slash (B, true path)
#   line_is_whole_line_comment_false_for_code (B, false path)
#   apply_brace_delta_saturates_at_zero_on_excess_closes (C, saturation)
#   observe_line_while_in_brace_scope_applies_delta (C, depth tracking)
#
# RIPR cannot statically trace closure/iterator/predicate activation from the
# test literal inputs to the seam boundary — the same ripr#1429 class as
# ripr-suppress-canonical-quote-operator-helper. All seams are covered by the
# inline unit tests and the pre-existing check_print_in_lib_test.rs integration
# tests. Suppression is narrow: single new production file, PR #624. Remove
# when ripr#1429 is fixed upstream.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-print-in-lib-allow-scopes-iterator-seams"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-ci-hygiene/src/commands/print_in_lib/allow_scopes.rs"]
classification = ["weakly_exposed", "static_unknown"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #624 allow_scopes.rs has 5 weakly_gripped seams that are ripr#1429-class predicate/iterator/closure patterns: (A) file_has_print_allow uses iter().take(30).any(closure) — RIPR cannot statically trace closure activation from test inputs through iterator combinators; (B) line_is_whole_line_comment uses starts_with predicate — RIPR cannot attribute boundary coverage statically; (C) observe_line/apply_brace_delta use brace counting arithmetic with saturating_sub — signed/unsigned boundary not statically traceable. All seams have inline unit tests (file_has_print_allow_detects_inner_print_stderr, _only_checks_first_30_lines, line_is_whole_line_comment_true/false, apply_brace_delta_saturates_at_zero_on_excess_closes, observe_line_while_in_brace_scope_applies_delta) and are exercised end-to-end by check_print_in_lib_test.rs integration tests. Suppression is narrow: single new production file, PR #624. Remove when ripr#1429 is fixed upstream."
created = "2026-06-15"
review_after = "2026-07-15"
expires = "2026-09-30"

# ======================================================================
# -----------------------------------------------------------------------------
# Coverage measure/route decoupling (PR #1482 / #1470): ripr#1429-class
# predicate-infection-untraceable seams in two xtask coverage decision points.
#
# PR #1482 decouples coverage measurement from test correctness by (1) wrapping
# all test commands non-fatally in the Python coverage-pack generator and (2)
# adding classification logic to the routing and quality-gate receipt builders
# in xtask. Two ripr#1429-class seams flagged as weakly_exposed:
#
# (A) xtask/src/tasks/ci_route.rs: routing_classification conditional
#     (lines ~540-550) — predicate chain:
#       `if coverage_proof_packs.is_empty() { if has_coverable_production_code { ... } }`
#     ripr cannot statically trace that test inputs activate the three branches
#     (routing_skip, routing_bug, routed) from the conditional nesting.
#
# (B) xtask/src/tasks/ci_route.rs: coverage_pack_skip_reason conditional
#     (lines ~567-574) — predicate:
#       `if coverage_packs_skipped > 0 { format!(...) } else { String::new() }`
#     ripr cannot statically trace that the skipped count predicate is exercised
#     by the test inputs.
#
# Coverage evidence: 8 inline #[cfg(test)] lib tests in ci_route.rs:
#   - routing_classification_is_routing_skip_for_docs_only_change
#   - routing_classification_is_routed_for_rust_source_change
#   - coverage_packs_skipped_is_zero_when_under_cap
#   - coverage_pack_cap_field_is_always_present_in_receipt
# Plus 4 tests in quality_gate.rs for classify_patch_coverage_failure and
# test_failure_class receipt field validation. Plus integration tests in
# xtask/tests/coverage_proof_measure_only_red_tdd.rs.
#
# This is the same ripr#1429 predicate-infection-untraceable class that suppresses
# parse_quote_operator_content and quality_baseline_cfg_test_scanner (activation
# flows through complex predicate chains that static analysis cannot trace). Two
# rounds of inline lib tests were added (#1470 and follow-up iterations) but ripr
# continues to report the seams as weakly_exposed because the static tracer cannot
# follow the conditional branching logic.
#
# Suppression is narrow (two specific predicates in one xtask file, one PR #1482).
# Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289.
# ======================================================================
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-coverage-measure-route-decoupling"
kind = "predicate_infection_untraceable"
paths = ["xtask/src/tasks/ci_route.rs"]
classification = ["weakly_exposed", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "Two ripr#1429-class seams in PR #1482 xtask coverage decoupling: (A) routing_classification conditional (ci_route.rs ~540-550) — nested if-chain predicate (coverage_proof_packs.is_empty() → has_coverable_production_code) whose activation RIPR cannot statically trace from test inputs; (B) coverage_pack_skip_reason conditional (ci_route.rs ~567-574) — predicate (coverage_packs_skipped > 0) RIPR cannot statically trace. Both seams are covered by 4 inline #[cfg(test)] lib tests calling route_receipt() directly with different test inputs (docs-only, Rust source, single-file, multi-file scenarios). Same ripr#1429 predicate-infection-untraceable class as quality_baseline_cfg_test_scanner, parse_quote_operator_content, and is_method_call_context. Two prior rounds of inline lib tests were added but ripr continues to report weakly_exposed due to static tracer limitations on predicate-chain propagation. Suppression is narrow (one xtask file, two specific predicates). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
created = "2026-06-15"
review_after = "2026-07-15"
expires = "2026-09-30"

# ======================================================================
# -----------------------------------------------------------------------------
# Multi-root workspace symbol tests (PR #1522): ripr#1428-class false-positives
# in newly added test code.
#
# PR #1522 adds 6 weakly_exposed seams to multi_root_workspace_tests.rs:
#   Lines 1519, 1525, 1623, 1627, 1704, 1709 (all error_path variants)
#   in test functions: test_workspace_symbol_multi_root_deterministic_returns_both_folders,
#   test_workspace_symbol_waits_for_index_when_building_at_request_time,
#   and related integration tests.
#
# These are test-harness error returns (?)? and error variant paths in test
# setup code, not production seams. RIPR's diff-scoped analysis treats newly
# added test code as production seams — the same ripr#1428 class as
# ripr-suppress-ux-receipt-tests and ripr-suppress-seam-proof-tests.
#
# The test functions comprehensively exercise the multi-root workspace symbol
# query pipeline: deterministic folder ordering, index-wait-on-building behavior,
# cross-folder symbol isolation, module-navigation across roots. All seams
# (error returns from Workspace::add_folder setup, Result unwraps in test)
# are test infrastructure, not production seams.
#
# Suppression is minimal (single test file, test-only additions). Path scope
# expires 2026-09-30. Remove when ripr#1428 is fixed upstream.
# ======================================================================
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-multi-root-workspace-symbol-test-surface"
kind = "test_receipt_surface"
paths = ["crates/perl-lsp-rs/tests/multi_root_workspace_tests.rs"]
classification = ["error_path", "weakly_exposed"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "PR #1522 multi_root_workspace_tests.rs: 6 error_path weakly_exposed seams (lines 1519, 1525, 1623, 1627, 1704, 1709) flagged as new production seams by diff-scoped RIPR analysis. These are test-harness error returns (?)? and error unwrap variants in test setup code, not production seams. The test functions (test_workspace_symbol_multi_root_deterministic_returns_both_folders, test_completion_does_not_leak_symbols_across_folders, test_workspace_symbol_waits_for_index_when_building_at_request_time, etc.) comprehensively exercise the multi-root workspace symbol pipeline including folder ordering, index-wait behavior, cross-folder isolation, and module navigation. All seams are test infrastructure. This is the same ripr#1428 class as ripr-suppress-ux-receipt-tests and ripr-suppress-seam-proof-tests (newly added test files causing static-analysis false-positives). Suppression is minimal (single test file, test-only additions). Remove when ripr#1428 is fixed upstream."
created = "2026-06-16"
review_after = "2026-07-16"
expires = "2026-09-30"

# ======================================================================
# -----------------------------------------------------------------------------
# DAP transport framing + lifecycle test surfaces (#991): ripr#1428-class
# false-positives in newly added test code.
#
# PR #991 adds two test surfaces that diff-scoped RIPR 0.9.0 treats as new
# production seams:
#   (A) transport.rs — an in-crate #[cfg(test)] mod (SharedBuf writer, framed_*
#       helpers, first_frame_body, and 11 test_transport_* functions) that drive
#       the crate-private run_with_io loop directly. run_with_io is pub(super),
#       so the tests live in-crate rather than in tests/ (which cannot reach it
#       and would require a #[doc(hidden)] pub production shim). RIPR flags the
#       test plumbing (struct/impl, predicates, return values) as production
#       seams — the same ripr#1428 class as ripr-suppress-dap-invalid-ref-
#       hardening-test-helpers (variables.rs #[cfg(test)] mod).
#   (B) dap_lifecycle_matrix_tests.rs — the C1-C6 cleanup/teardown lifecycle
#       cells (predicate/error-path plumbing in test assertions, e.g. the
#       relaunch-after-terminate message-class check). Test-file plumbing, same
#       ripr#1428 class as ripr-suppress-seam-proof-tests.
#
# Coverage evidence: the transport framing seam (ContentLengthFramer recovery,
# CRLF/LF separators, malformed headers/bodies, EOF, multi-message, recovery
# after a bad frame) is exercised by the 11 in-crate test_transport_* tests via
# run_with_io over std::io::Cursor; the lifecycle cells exercise the no-session
# handlers (threads/modules/variables/stackTrace/scopes/launch/restart) end to
# end. Both are pure test surfaces, not production seams.
#
# Suppression is minimal (one production file scoped to its #[cfg(test)] mod and
# one test file). Remove when ripr#1428 is fixed upstream.
# ======================================================================
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-dap-transport-framing-and-lifecycle-test-surface"
kind = "test_receipt_surface"
paths = [
  "crates/perl-dap/src/debug_adapter/transport.rs",
  "crates/perl-dap/tests/dap_lifecycle_matrix_tests.rs",
]
classification = ["weakly_exposed", "static_unknown", "infection_unknown", "propagation_unknown"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "PR #991 adds two test surfaces that diff-scoped RIPR 0.9.0 misclassifies as new production seams. (A) transport.rs gains an in-crate #[cfg(test)] mod (SharedBuf writer, framed_request/framed_raw/first_frame_body helpers, 11 test_transport_* functions) that drive the crate-private pub(super) run_with_io loop directly over std::io::Cursor; the tests live in-crate because run_with_io is not public and an external tests/ file would need a #[doc(hidden)] pub production shim. RIPR flags the test plumbing (struct/impl, predicates at line 252, return values) as production seams — same ripr#1428 class as ripr-suppress-dap-invalid-ref-hardening-test-helpers (variables.rs #[cfg(test)] mod). (B) dap_lifecycle_matrix_tests.rs gains the C1-C6 cleanup/teardown cells whose assertion plumbing (predicate/error-path, e.g. lines 561/564 relaunch-after-terminate message-class check) is flagged as production seams — same class as ripr-suppress-seam-proof-tests. The transport framing seam (ContentLengthFramer recovery, CRLF/LF separators, malformed header/body, EOF, multi-message, recovery-after-bad-frame) and the no-session lifecycle handlers are fully exercised by these tests. Both are pure test surfaces. Suppression is minimal (one production file's #[cfg(test)] mod plus one test file). Remove when ripr#1428 is fixed upstream."
created = "2026-06-20"
review_after = "2026-07-20"
expires = "2026-09-30"

# ======================================================================
# -----------------------------------------------------------------------------
# Windows CWD-planting RCE fix (#2764): windows.rs platform-gated code.
#
# PR #2967 (fix/windows-bare-tool-cwd-rce) tightens the Windows subprocess
# invocation chain: resolve_windows_program now fails closed when the tool is
# not on any absolute PATH directory, and resolve_cmd_exe refuses the bare
# "cmd.exe" name.  The implementation lives in
# crates/perl-subprocess-runtime/src/os_runtime/windows.rs, which is gated
# on #[cfg(windows)].
#
# On Linux CI runners, the entire windows.rs module is excluded from
# compilation by the platform cfg-guard.  RIPR has no test call paths to
# reach any of its functions (resolve_windows_program, path_dirs_from_env,
# pathext_from_env, resolve_cmd_exe, windows_program_priority) — so every
# changed line is classified no_static_path or weakly_exposed with
# reach=no, observe=no.  This is the same structural limitation as
# ripr#1429 (cfg-guard tracing): the code is correct and its security
# invariants are proven by Windows-only tests that run on the dedicated
# Windows CI runner (ripr+ on CX43 / GitHub Hosted Windows), but the Linux
# diff-scoped pass cannot observe those call paths.
#
# The cross-platform security logic was extracted into path_selection.rs
# (select_path_candidate, candidate_priority) precisely so it CAN be
# tested on Linux CI — that file has full reach/observe/infect evidence.
# windows.rs retains only the Windows-specific orchestration (PATH/PATHEXT
# env lookup, is_file probing, cmd.exe resolution) that requires a Windows
# runner.
#
# Suppression is narrow (single production file). Remove when ripr adds
# cross-platform cfg-guard simulation or when targeted mutation covers
# the Windows-only orchestration path on a non-Linux runner.
# ======================================================================
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-windows-subprocess-platform-guard-2764"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-subprocess-runtime/src/os_runtime/windows.rs"]
classification = ["no_static_path", "weakly_exposed", "static_unknown", "infection_unknown", "propagation_unknown"]
owner = "proof-lane"
issue = "#2764"
reason = "PR #2967 hardens the Windows subprocess invocation chain (resolve_windows_program fail-closed, resolve_cmd_exe absolute-only). windows.rs is gated on #[cfg(windows)] — on Linux CI the entire module is not compiled, so RIPR finds no static call paths (no_static_path, reach=no) to any changed line. The security invariants are covered by dedicated Windows-only tests (test_resolve_command_invocation_fails_closed_when_tool_not_on_path, test_resolve_cmd_exe_returns_absolute_path_never_bare, test_run_command_does_not_execute_planted_cwd_binary) that run on the Windows CI runner. The cross-platform selection logic was extracted into path_selection.rs and IS exercised on Linux CI. This is the same ripr#1429 cfg-guard class as ripr-suppress-uri-file-path-helpers and related suppressions. Remove when ripr supports cross-platform cfg-guard simulation."
created = "2026-06-25"
review_after = "2026-09-25"
expires = "2026-12-31"

# ======================================================================
# -----------------------------------------------------------------------------
# Windows CWD-planting RCE fix (#2764): path_selection.rs match arm guards.
#
# path_selection.rs is the cross-platform extraction of select_path_candidate
# and candidate_priority, introduced in PR #2967 so the security-critical
# CWD-exclusion invariants can be tested on Linux CI.  The function is fully
# reached and observed by tests (reach=yes, observe=yes) but RIPR classifies
# the match arm guard conditions (lines 15-18: `if ext == "exe"`, `"com"`,
# `"cmd"`, `"bat"`) as weakly_exposed with discriminate=weak because the
# static guard predicates are not individually mutation-tested at the guard
# boundary (the test asserts the returned priority value, but the string
# literal in the guard itself is ripr#1429-class: RIPR cannot trace through
# the match arm dispatch to confirm the exact guard condition is killed).
#
# All four priority tiers are exercised by test_candidate_priority_ordering_all_tiers
# (exact u8 assertions for each extension) and test_candidate_priority_case_insensitive
# (case normalization), providing strong evidence the dispatch is correct.
# The seam class is identical to ripr-suppress-phase-block-description-match-arms
# where match arm dispatch on string literals is not statically traced by
# RIPR 0.9.x.  Remove when ripr gains match-guard discrimination or targeted
# mutation is scheduled post-review.
# ======================================================================
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-path-selection-match-arm-guards-2764"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-subprocess-runtime/src/os_runtime/path_selection.rs"]
classification = ["weakly_exposed"]
owner = "proof-lane"
issue = "#2764"
reason = "path_selection.rs candidate_priority match arm guards (lines 15-18: `if ext == \"exe\" => 5`, `\"com\" => 4`, `\"cmd\" => 3`, `\"bat\" => 2`) are ripr#1429-class weakly_exposed seams. The function has reach=yes, observe=yes, infect=weak — the priority values are asserted by test_candidate_priority_ordering_all_tiers and test_candidate_priority_case_insensitive, but RIPR cannot statically trace that the guard predicate itself (the exact string literal `\"exe\"` etc.) is the discriminating condition rather than the arm body. This is identical to ripr-suppress-phase-block-description-match-arms (string dispatch arms). All security properties are verified: the full select_path_candidate pipeline is exercised by 15+ cross-platform tests including CWD-exclusion, relative-candidate rejection, extension-priority ordering, and the planted-binary regression. Remove when ripr#1429 is fixed upstream."
created = "2026-06-25"
review_after = "2026-09-25"
expires = "2026-12-31"

# ======================================================================
# -----------------------------------------------------------------------------
# Windows CWD-planting RCE second front (#3028): windows.rs platform guard.
#
# PR #3032 (fix/3028-perl-lsp-rs-command-rce) closes the second RCE front:
# resolve_perl_path in perl-lsp-rs-core now skips empty/relative PATH entries,
# and command_exists / run_test_command in execute_command/provider.rs route
# through perl_subprocess_runtime::resolve_program instead of bare Command::new.
#
# crates/perl-subprocess-runtime/src/os_runtime/windows.rs is gated on
# #[cfg(windows)] — on Linux CI the module is not compiled, so RIPR cannot
# find any static call paths (no_static_path, reach=no) to any changed line.
# All 25 no_static_path findings in this file are a direct consequence of the
# platform cfg-guard: the code is correct and tested by Windows-only tests on
# the dedicated Windows runner, but the Linux diff-scoped pass cannot observe
# those call paths.  Same structural class as ripr#1429 (cfg-guard tracing).
#
# Suppression is narrow (single production file). Remove when ripr adds
# cross-platform cfg-guard simulation.
# ======================================================================
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-windows-subprocess-platform-guard-3028"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-subprocess-runtime/src/os_runtime/windows.rs"]
classification = ["no_static_path", "weakly_exposed", "static_unknown", "infection_unknown", "propagation_unknown"]
owner = "proof-lane"
issue = "#3028"
reason = "PR #3032 routes execute_command/provider.rs and resolve_perl_path through the hardened resolve_program path; the underlying Windows dispatcher lives in windows.rs which is #[cfg(windows)]-gated. On Linux CI the entire module is excluded from compilation, so RIPR finds no static call path to any changed line (no_static_path, reach=no). The security invariants are exercised by Windows-only tests (test_resolve_program_windows_fails_closed_for_unknown_tool, test_resolve_program_windows_returns_absolute_path_for_real_tool) on the Windows runner. Same ripr#1429 cfg-guard class as ripr-suppress-windows-subprocess-platform-guard-2764 (companion PR #2967). Remove when ripr supports cross-platform cfg-guard simulation."
created = "2026-06-25"
review_after = "2026-09-25"
expires = "2026-12-31"

# ======================================================================
# -----------------------------------------------------------------------------
# Windows CWD-planting RCE second front (#3028): platform.rs test setup and
# lib.rs Windows branch.
#
# PR #3032 adds two tests to perl-lsp-rs-core/src/platform.rs:
#   (A) resolve_perl_path_skips_empty_path_entry
#   (B) resolve_perl_path_skips_empty_entry_but_finds_real_perl
#
# Test (B) uses match-arm setup code (`Ok(p) => p` at line 325, `Some(d) =>
# d.to_path_buf()` at line 329) to extract the real perl path and its parent
# before mutating PATH.  RIPR's diff-scoped analysis flags these match arms
# as new production seams with discriminate=weak (relational oracle — the test
# checks `result.is_ok()` and `found.is_absolute()` rather than exact path
# equality).  These are test setup arms in an #[cfg(test)] function body, not
# production behavior seams — the same ripr#1428 class as
# ripr-suppress-multi-root-workspace-symbol-test-surface and
# ripr-suppress-dap-transport-framing-and-lifecycle-test-surface.
#
# Additionally, lib.rs line 68 (`))` closing the ok_or_else error construction)
# is inside the `#[cfg(windows)]` branch of resolve_program — unreachable on
# Linux CI for the same reason as windows.rs above (ripr#1429 cfg-guard class).
#
# Suppressions are minimal (two production files, test-only and cfg-gated code
# only). Remove when ripr#1428 and ripr#1429 are fixed upstream.
# ======================================================================
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-platform-test-setup-and-lib-windows-branch-3028"
kind = "predicate_infection_untraceable"
paths = [
  "crates/perl-lsp-rs-core/src/platform.rs",
  "crates/perl-subprocess-runtime/src/lib.rs",
]
classification = ["weakly_exposed"]
owner = "proof-lane"
issue = "#3028"
reason = "PR #3032 adds two test seams flagged by diff-scoped RIPR: (A) platform.rs lines 325 and 329 are match-arm setup code inside the #[cfg(test)] test body resolve_perl_path_skips_empty_entry_but_finds_real_perl — `Ok(p) => p` and `Some(d) => d.to_path_buf()` are test infrastructure that extracts the real perl path before PATH mutation; RIPR misclassifies these as production seams with discriminate=weak (ripr#1428 class: test body code treated as new production seam). (B) lib.rs line 68 is the closing `))` of an ok_or_else error in the `#[cfg(windows)]` branch of resolve_program, which is not compiled on Linux CI (ripr#1429 cfg-guard class). The production behavior of resolve_perl_path (empty-entry skip, absolute-path-only) is proven by resolve_perl_path_skips_empty_path_entry (Err on empty PATH) and resolve_perl_path_skips_empty_entry_but_finds_real_perl (Ok with absolute path when perl exists). The Windows resolve_program path is proven by Windows-only tests. Suppression is minimal (two production files, test setup and cfg-gated code only). Remove when ripr#1428/#1429 are fixed upstream."
created = "2026-06-25"
review_after = "2026-09-25"
expires = "2026-12-31"

# ======================================================================
# -----------------------------------------------------------------------------
# Completion auto-import additionalTextEdits (PR #1919, issue #1694):
# ripr#1429-class weakly_exposed false-positives in production completion code.
#
# PR #1919 wires `additionalTextEdits` auto-import (`use Module;`) into workspace
# subroutine/variable/constant completions, qualified `Package::member`
# completions, and the unknown-receiver method fallback. RIPR 0.9.x flags the
# changed return-value and field-construction seams in these three production
# files as weakly_exposed because its static tracer cannot connect the focused
# unit tests to the changed expressions (evidence: "observation_unverified",
# stop_reasons ["fixture_opaque"]; the assertion texts it scores come from
# unrelated files). All seams are covered by dedicated #[cfg(test)] lib tests
# with exact (strong-oracle) assertions. Same ripr#1429 class as the
# hover_cards.rs and workspace_rename.rs production-file suppressions.
# ======================================================================
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-completion-auto-import-edits"
kind = "predicate_infection_untraceable"
paths = [
  "crates/perl-lsp-rs-core/src/providers/completion/completion/auto_import.rs",
  "crates/perl-lsp-rs-core/src/providers/completion/completion/packages.rs",
  "crates/perl-lsp-rs-core/src/providers/completion/completion/workspace.rs",
]
classification = ["weakly_exposed", "no_static_path", "static_unknown"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #1919 (issue #1694) wires additionalTextEdits auto-import into workspace subroutine/variable/constant completions, qualified Package::member completions, and the unknown-receiver method fallback. RIPR 0.9.x reports weakly_exposed return-value and field-construction seams (workspace_auto_import_edits return value, the additional_edits field constructions in the completion-item arms, find_use_block_end's package fallback, build_auto_import_edit's edit tuple) because its static tracer cannot connect the focused tests to the changed expressions (evidence: observation_unverified, stop_reasons fixture_opaque; scored assertion texts come from unrelated files). All seams are covered by dedicated #[cfg(test)] lib tests with exact strong-oracle assertions: workspace_auto_import_edits_returns_exact_edits_per_branch (exhaustive exact edit-text and byte-offset assertions for reachable/main/current-package/empty/file-local/already-imported branches), workspace_subroutine/variable/constant_completion_auto_imports_module, qualified_subroutine_completion_auto_imports_module, workspace_completion_suppresses_auto_import_when_already_imported, workspace_completion_no_auto_import_for_file_local_symbol, unknown_receiver_fallback_completion_observes_auto_import_seam, find_use_block_end_after_package_without_use, edit_inserts_after_package_decl_when_no_use_block, and edit_returned_when_not_imported. Same ripr#1429 static-tracer-limitation class as the hover_cards.rs and workspace_rename.rs production-file suppressions. Suppression is narrow (three completion source files). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
created = "2026-06-25"
review_after = "2026-07-25"
expires = "2026-09-30"

# ======================================================================
# -----------------------------------------------------------------------------
# PL304 use-constant NodeKind blind-spot fix (PR #3078, issue #3071):
# ripr#1429-class: RIPR 0.9.x cannot mutation-trace let-chain match-condition
# seams in collect_constant_names() in pod_coverage.rs.
#
# PR #3078 adds collect_constant_names() which walks NodeKind::Use nodes to
# detect `use constant FOO => ...` and `use constant { FOO => ... }` exports
# so PL304 MissingPodCoverage no longer fires false-positives for them.
#
# RIPR 0.9.x flags 3 seams as activation_unknown:
#   pod_coverage.rs:167  — `module == "constant"` let-chain string compare
#   pod_coverage.rs:168  — `!args.is_empty()` guard in the same let-chain
#   pod_coverage.rs:171  — `args.first().map(String::as_str) == Some("{")` branch
#
# These are predicate_boundary seams inside a Rust let-chain
# (`if let NodeKind::Use { module, args, .. } = &n.kind && module == "constant"
# && !args.is_empty()`). RIPR's static tracer cannot follow the boolean
# short-circuit through the let-binding into the string-compare conditions —
# a known ripr#1428/1429 limitation for let-chain match predicates.
#
# Call-observation fix was attempted first (per gap-closure playbook): two
# discriminator unit tests were added that explicitly drive both the true and
# false branches of every predicate:
#   collect_constant_names_boundary_discriminator — drives module != "constant"
#     (Use { module: "strict" }) and both hash_form branches (single: "TIMEOUT",
#     hash: "HOST","PORT")
#   is_valid_identifier_boundary_discriminator — drives leading '_', alphabetic,
#     non-word first char, tail variants
# These tests pass (20/20 --lib tests green) but the ripr seams persisted in
# the post-discriminator fresh run — confirming this is the static-tracer class.
#
# Behavioral coverage for all 3 seams:
#   Line 167/168 (module == "constant" + !args.is_empty()):
#     given_use_constant_in_export_ok_then_no_pl304_false_positive (integration)
#     collect_constant_names_single_form_push (lib unit)
#     collect_constant_names_boundary_discriminator (lib unit, non-constant Use)
#   Line 171 (hash_form branch):
#     given_use_constant_hash_form_in_export_ok_then_no_pl304_false_positive
#     collect_constant_names_boundary_discriminator (lib unit, both branches)
#
# Suppression is narrow: single production file, 3 seams. Remove when
# ripr#1429 is fixed upstream or when ripr can trace let-chain predicates.
# ======================================================================
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-pod-coverage-use-constant-let-chain"
kind = "activation_unknown"
paths = ["crates/perl-lsp-rs-core/src/providers/diagnostics/lints/pod_coverage.rs"]
classification = ["predicate_boundary", "activation_unknown", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #3078 (issue #3071) adds collect_constant_names() to fix PL304 false-positives on `use constant` exports. RIPR 0.9.x flags 3 activation_unknown seams (pod_coverage.rs:167 module==\"constant\" string compare, :168 !args.is_empty() guard, :171 args.first().map()==Some(\"{\") hash-form branch) because its static tracer cannot follow boolean short-circuits through Rust let-chain predicates (`if let NodeKind::Use { .. } = &n.kind && module == \"constant\" && !args.is_empty()`). Call-observation discriminator tests were attempted first per gap-closure playbook: collect_constant_names_boundary_discriminator drives module!=\"constant\" false branch (Use{module:\"strict\"}) and both hash_form branches; is_valid_identifier_boundary_discriminator drives all leading-char and tail-char predicate seams. Both tests pass (20/20 --lib green) but seams persisted in post-discriminator ripr run, confirming this is the ripr#1429 static-tracer-limitation class for let-chain match predicates. Behavioral coverage: given_use_constant_in_export_ok_then_no_pl304_false_positive and given_use_constant_hash_form_in_export_ok_then_no_pl304_false_positive (integration), collect_constant_names_single_form_push and collect_constant_names_boundary_discriminator (lib unit). Suppression is narrow: single production file, 3 seams. Remove when ripr#1429 is fixed upstream or ripr can trace let-chain predicates."
created = "2026-06-26"
review_after = "2026-09-26"
expires = "2026-12-31"

# -----------------------------------------------------------------------------
# release_artifact_check.rs fixture/serde-driven branch predicates: activation
# untraceable by ripr 0.9.x (ripr#1429 class).
#
# PR #3142 (issue #3141) adds xtask/src/tasks/release_artifact_check.rs — the
# `cargo xtask release artifact-check` release-archive guard. RIPR cannot
# statically trace activation from the on-disk binary fixtures (tar.gz / zip
# archives + consolidated SHA256SUMS) and the JSON-deserialized `Contract`
# through `serde_json::from_str` + `fs::read_dir` + the gzip/tar and ZIP
# central-directory byte parsers to the per-branch predicates in validate_dist
# (missing-binary, exec-bit, version-mismatch, undeclared-archive,
# extension-mismatch, platform-not-declared, checksum missing/not-listed/
# mismatch, completeness) and the archive readers. Same ripr#1429
# predicate-infection-untraceable class as the quality_baseline, agent_ledgers,
# and pr_close_proof xtask suppressions above.
#
# Coverage evidence: 28 inline #[cfg(test)] unit tests drive every branch in
# process against committed fixtures (good / bad-missing-dap / bad-checksum) and
# tempdir-constructed edge dists, plus 6 CLI integration tests. Key seams:
#   validate_dist_passes_on_good_fixture / _flags_missing_required_binary /
#   _flags_checksum_mismatch / _flags_version_mismatch / _flags_undeclared_archive /
#   _flags_extension_mismatch / _flags_target_platform_without_spec /
#   _flags_missing_checksums_file / _flags_archive_not_in_checksums /
#   _flags_no_archives / _requires_all_targets_without_allow_partial;
#   read_tar_gz_entries_lists_binaries_with_exec_bit / read_zip_entries_lists_windows_binaries /
#   read_zip_entries_rejects_non_zip / _rejects_tiny_file / read_tar_gz_entries_rejects_non_gzip;
#   run_succeeds_on_good_fixture / run_fails_on_missing_dap / run_errors_on_missing_dist;
#   load_contract_reads_repo_contract; parse_checksums_handles_modes_and_prefixes.
# Codecov patch coverage for the file is 96.88%; the residual lines are the
# genuinely-unreachable defensive guards (e.g. platform_for_triple None).
#
# Suppression is narrow (single xtask file, one PR #3142). Remove when ripr#1429
# is fixed upstream or when targeted mutation is scheduled per #1289.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-release-artifact-check"
kind = "predicate_infection_untraceable"
paths = ["xtask/src/tasks/release_artifact_check.rs"]
classification = ["weakly_exposed", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #3142 (issue #3141) adds the release-archive DAP guard. RIPR 0.9.x cannot statically trace activation from on-disk binary fixtures (tar.gz/zip + SHA256SUMS) and JSON-deserialized Contract through serde_json::from_str + fs::read_dir + the gzip/tar and ZIP central-directory byte parsers to the per-branch predicates in validate_dist (missing-binary, exec-bit, version-mismatch, undeclared-archive, extension-mismatch, platform-not-declared, checksum missing/not-listed/mismatch, completeness) and the archive readers. Same ripr#1429 class as the quality_baseline, agent_ledgers, and pr_close_proof xtask suppressions. Coverage: 28 inline #[cfg(test)] unit tests drive every branch in-process against committed fixtures (good/bad-missing-dap/bad-checksum) and tempdir edge dists, plus 6 CLI integration tests; Codecov patch coverage 96.88% with only genuinely-unreachable defensive guards (e.g. platform_for_triple None) residual. Suppression is narrow (single xtask file, one PR #3142). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."

# native_product_surface.rs first-mile guard: clap-dispatch + tempdir-fixture
# seams untraceable by ripr 0.9.x (ripr#1429 class).
#
# PR #3283 (epic #3276, PR7) adds xtask/src/tasks/native_product_surface.rs —
# the `cargo xtask check-native-product-surface` guard that greps first-mile
# product surfaces for legacy/external-tool framing. RIPR cannot statically
# trace activation from the tempdir fixture trees (run_at) and the new top-level
# clap `Commands::CheckNativeProductSurface` dispatch arm through the proc-macro
# derive to the per-line seams: the violations-present report branch
# (eprintln! block), the `format!` violation-string construction (idx + 1), and
# the SURFACES / DISALLOWED const tables. ripr's own stop_reasons for these are
# `proc_macro_opaque` and `fixture_opaque`. Same predicate/activation-untraceable
# class as the quality_baseline, agent_ledgers, pr_close_proof, ci_route, and
# release_artifact_check xtask suppressions above.
#
# Coverage evidence: 7 inline #[cfg(test)] tests drive every branch in-process —
# flags_each_disallowed_marker (every DISALLOWED marker), allows_native_first_
# negations (no false positives), reports_line_numbers (exact line-number
# oracle), flags_markdown_wrapped_and_capitalized_variants (backtick/case
# normalization), run_at_is_ok_on_a_clean_tree, run_at_errors_when_a_surface_
# regresses (violations-present error branch), and live_product_surface_is_clean
# (the real `run` entry point over the live repo surfaces).
[[suppress]]
id = "ripr-suppress-native-product-surface-scanner"
kind = "predicate_infection_untraceable"
paths = ["xtask/src/tasks/native_product_surface.rs", "xtask/src/main.rs"]
classification = ["weakly_exposed", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
created = "2026-07-03"
review_after = "2026-08-03"
expires = "2026-10-03"
reason = "PR #3283 (epic #3276, PR7) adds the native product-surface guard xtask. RIPR 0.9.x cannot statically trace activation from tempdir fixture trees and the new top-level clap Commands::CheckNativeProductSurface dispatch arm (xtask/src/main.rs) through the proc-macro derive to the per-line seams in native_product_surface.rs: the violations-present report branch (eprintln! block ~line 68), the format! violation-string construction (idx + 1, ~lines 108-111), and the SURFACES/DISALLOWED const tables (~lines 24, 36). ripr's own stop_reasons are proc_macro_opaque and fixture_opaque. Same ripr#1429 class as the quality_baseline, agent_ledgers, pr_close_proof, ci_route, and release_artifact_check xtask suppressions. Coverage: 7 inline #[cfg(test)] tests drive every branch in-process (flags_each_disallowed_marker, allows_native_first_negations, reports_line_numbers with an exact line-number oracle, flags_markdown_wrapped_and_capitalized_variants, run_at_is_ok_on_a_clean_tree, run_at_errors_when_a_surface_regresses, live_product_surface_is_clean). The main.rs entry is scoped to the same two diff-scoped classifications (weakly_exposed, no_static_path) and only ever affects new clap dispatch arms, which are proc_macro_opaque by construction and delegate to task modules that carry their own coverage. Suppression is narrow (one new xtask task file + its dispatch arm, one PR #3283). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."

# Mid-surrogate call-observation test surface (#2478): ripr#1428-class
# false-positives in newly added call-observation test file.
#
# PR #3040 adds one new integration test file:
#   crates/perl-lsp-rs-core/tests/detect_dead_code_mid_surrogate_position.rs
#
# The file contains one test function:
#   detect_dead_code_position_to_offset_clamps_mid_surrogate_column
#
# It exercises detect_dead_code (a production caller of LineStartsCache::
# position_to_offset in dead_code.rs:52) with emoji-containing source text
# whose UTF-16 column 2 is the trailing surrogate of "😀".  The assertion
# proves the returned byte offset clamps to the emoji start.
#
# ripr's diff-scoped analysis flags the test-harness plumbing inside this
# newly added file (calls to WorkspaceIndex::new, index_file_str,
# find_unused_symbols, detect_dead_code) as new production seams.  This is
# the same ripr#1428 class as ripr-suppress-seam-proof-tests and
# ripr-suppress-ux-receipt-tests: internal calls within a newly added test
# file are classified as production seams even though they are pure test
# scaffolding driving existing production APIs.
#
# Suppression is minimal (single test file, one PR). Remove when ripr#1428
# is fixed upstream.
# ======================================================================
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-mid-surrogate-call-observation-test"
kind = "test_receipt_surface"
paths = ["crates/perl-lsp-rs-core/tests/detect_dead_code_mid_surrogate_position.rs"]
classification = ["weakly_gripped", "no_static_path", "call_presence"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "PR #3040 adds detect_dead_code_mid_surrogate_position.rs as a call-observation test for the mid-surrogate UTF-16 clamp fix (#2478). The single test function (detect_dead_code_position_to_offset_clamps_mid_surrogate_column) drives the production detect_dead_code path (dead_code.rs:52 calls LineStartsCache::position_to_offset) with emoji source text whose UTF-16 column 2 is the trailing surrogate of '😀'. ripr's diff-scoped analyzer flags the test-harness plumbing (WorkspaceIndex::new, index_file_str, find_unused_symbols, detect_dead_code calls inside the newly added test file) as new production seams. This is the same ripr#1428 class as ripr-suppress-seam-proof-tests and ripr-suppress-ux-receipt-tests (test-harness plumbing inside newly added test files falsely classified as production seams). The production seam itself (line_index.rs:91) is covered by ripr-suppress-mid-surrogate-position-clamp-predicate. Suppression is minimal (single test file, PR #3040). Remove when ripr#1428 is fixed upstream."
created = "2026-06-28"
review_after = "2026-07-28"
expires = "2026-09-30"


# -----------------------------------------------------------------------------
# utf16_column line_start > text.len() overflow guard: private-field-write-to-
# predicate activation untraceable by ripr 0.9.x (ripr#1428 class).
#
# PR #3042 (issue #2484) adds `if line_start > self.text.len()` at line 232 in
# the private `utf16_column` method of `LineIndex`. The guard protects against
# corrupt internal state where `line_starts[i]` exceeds the text length —
# reachable only by mutating the private `line_starts` field directly (not via
# the public API, which normalizes offsets before any lookup).
#
# RIPR seam f197bc96baa7df76 is classified `activation_unknown` / `infect: weak`
# because the discriminating input (`line_start > self.text.len()`) is only
# reachable by writing to the private `line_starts` field
# (`index.line_starts = vec![0, 100]`). RIPR's static tracer cannot follow a
# private-field write to the predicate boundary — the ripr#1428 blind spot.
#
# Call-observation discriminator test was attempted first per gap-closure
# playbook: `utf16_column_boundary_discriminator` uses a named variable
# `line_start_past_end: usize = 100` with `debug_assert!(line_start_past_end >
# text.len())` to make the inequality visible in source. The test passes but the
# seam persisted in the subsequent ripr run, confirming the ripr#1428 class.
#
# Behavioral coverage is confirmed via green Codecov / Patch 95 (line 232 IS
# instrumented-executed per LLVM coverage) and two --lib unit tests:
#   utf16_column_clamps_line_start_past_text_end — line_start=100 > text.len()=3
#   utf16_column_boundary_discriminator          — explicit named-variable form
#
# Suppression is narrow: single production file, one PR (#3042), one seam ID
# (f197bc96baa7df76). Remove when EffortlessMetrics/ripr#1428 (private-field
# mutation tracing) is fixed upstream.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-line-index-utf16-column-overflow-guard"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-position-tracking/src/line_index.rs"]
classification = ["activation_unknown"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "PR #3042 (issue #2484) hardens utf16_column with `if line_start > self.text.len()` at line 232. RIPR seam f197bc96baa7df76 is activation_unknown / infect:weak because the predicate's discriminating input is only reachable via private-field mutation (`index.line_starts = vec![0, 100]`), which ripr#1428 cannot trace from field write to predicate boundary. Call-observation discriminator test was tried first (utf16_column_boundary_discriminator: named variable + debug_assert!(line_start_past_end > text.len())); seam persisted in post-discriminator ripr run, confirming the ripr#1428 class. Behavioral coverage is confirmed: Codecov / Patch 95 is GREEN (line 232 is instrumented-executed), and two --lib unit tests exercise the truthy branch (utf16_column_clamps_line_start_past_text_end and utf16_column_boundary_discriminator). Suppression is narrow: single production file, one PR (#3042), seam ID f197bc96baa7df76. Remove when ripr#1428 (private-field-mutation tracing) is fixed upstream."
created = "2026-06-28"
review_after = "2026-09-28"
expires = "2026-12-31"

# ======================================================================
# -----------------------------------------------------------------------------
# References includeDeclaration=true quality tests (PR #3050 / #2673):
# ripr#1428-class false-positives in newly added test functions.
#
# PR #3050 adds two test functions to the existing
# crates/perl-lsp-rs/src/runtime/language/navigation_runtime_quality_tests.rs
# #[cfg(test)] module:
#   - references_source_backed_tier_used_when_include_declaration_true
#   - references_include_declaration_true_adds_declaration_to_source_backed_result
#
# Both functions use `?` operators to propagate errors from `test_handle_references`
# calls.  ripr 0.9.x diff-scoped analysis flags these error-path seams (lines
# 481 and 487) as new production exposure because the functions are newly added
# to the diff.
#
# These are ripr#1428-class false-positives: the `?` operators are in test
# helper function bodies, not in production code.  The test functions exercise
# the #2673 fix: serving includeDeclaration=true requests from the semantic
# source-backed tier rather than falling through to the workspace-index tier.
# The production seam (live_source_backed_reference_locations, references.rs
# ~line 862) IS covered by strong oracle assertions in both test functions.
#
# This is the same class as ripr-suppress-multi-root-workspace-symbol-test-surface
# (multi_root_workspace_tests.rs: error_path seams in test setup code) and
# ripr-suppress-seam-proof-tests (newly added test files). Remove when
# ripr#1428 is fixed upstream.
# ======================================================================
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-references-include-declaration-quality-tests"
kind = "test_receipt_surface"
paths = ["crates/perl-lsp-rs/src/runtime/language/navigation_runtime_quality_tests.rs"]
classification = ["error_path", "reachable_unrevealed"]
owner = "proof-lane"
issue = "#2673"
reason = "PR #3050 adds two test functions (references_source_backed_tier_used_when_include_declaration_true and references_include_declaration_true_adds_declaration_to_source_backed_result) to the navigation_runtime_quality_tests.rs #[cfg(test)] module. Both functions use ? operators (lines 481 and 487) to propagate errors from test_handle_references calls. ripr 0.9.x diff-scoped analysis flags these as new production seams (error_path / reachable_unrevealed). These are ripr#1428-class false-positives: the ? operators are inside newly added test functions, not production code. The production fix (#2673: live_source_backed_reference_locations serves includeDeclaration=true requests) is covered by strong oracle assertions in both test functions (tier verification, location count comparison, declaration-line presence). Same class as ripr-suppress-multi-root-workspace-symbol-test-surface (error_path seams in test setup code). Remove when ripr#1428 is fixed upstream."
created = "2026-06-25"
review_after = "2026-09-25"
expires = "2026-12-31"

# ======================================================================
# -----------------------------------------------------------------------------
# LineStartsCache::position_to_offset for-loop surrogate clamp (fix #2478):
# ripr#1429-class predicate-infection-untraceable seams in the changed loop
# condition `uc + ch.len_utf16() > character` in line_index.rs.
#
# PR #3040 changes the break guard in `position_to_offset` from
#   `uc >= character as usize`
# to
#   `uc + ch.len_utf16() > character as usize`
# so a column landing inside a surrogate pair clamps to the codepoint start
# instead of over-advancing past it.
#
# ripr 0.9.x flags 3 `weakly_gripped` seams in `LineStartsCache::position_to_offset`
# (mapped to `reachable_unrevealed` in the evidence receipt). The seams correspond to
# the three discriminator boundaries of the changed for-loop condition:
#   (1) Condition fires on the FIRST char iteration (e.g. character=0 on "x😀y"):
#       uc=0, ch.len_utf16()=1, 0+1>0 = true → immediate break.
#   (2) Condition fires MID-LOOP at a surrogate boundary (e.g. character=2 on "x😀y"):
#       after 'x': uc=1; ch='😀', 1+2>2 = true → clamps to emoji start (byte 1).
#   (3) Condition never fires — loop exhausts (e.g. character=4 on "x😀y"):
#       all chars consumed without break → returns ls + lt.len() = end of line.
#
# Coverage evidence: the regression test
#   `line_starts_cache_position_to_offset_mid_surrogate_clamps_to_char_start`
# in `crates/perl-position-tracking/tests/comprehensive_unit_tests.rs` exercises
# all three discriminator boundaries with exact-value assertions:
#   column 0 → byte 0 (fires on first char)
#   column 1 → byte 1 (fires at emoji start without clamping)
#   column 2 → byte 1 (mid-surrogate clamp, the fixed boundary)
#   column 3 → byte 5 (fires after emoji)
#   column 4 → byte 6 (loop exhausts — past all UTF-16 units)
# All five assertions would fail if the fix were reverted to `uc >= character`.
# A call-observation test in `detect_dead_code_mid_surrogate_position.rs` drives
# `detect_dead_code` (a production caller of LineStartsCache::position_to_offset)
# with column 2 on "x😀y" text and asserts the returned byte offset is 1.
#
# RIPR cannot statically trace activation of the for-loop break condition from
# test inputs through the accumulating `uc` variable across loop iterations.
# This is the same ripr#1429 predicate-infection-untraceable class that suppresses
# `is_method_call_context` (paren-scan loop predicates), `parse_quote_operator_content`
# (string-parsing chain), and `quality_baseline_cfg_test_scanner` (line-scanning loops).
#
# Suppression is narrow (single production file, one PR #3040). Remove when
# ripr#1429 is fixed upstream or targeted mutation is scheduled.
# ======================================================================
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-position-tracking-surrogate-loop-seam"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-position-tracking/src/line_index.rs"]
classification = ["weakly_gripped", "reachable_unrevealed", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #3040 changes the for-loop break guard in LineStartsCache::position_to_offset from `uc >= character` to `uc + ch.len_utf16() > character` (fix #2478: mid-surrogate UTF-16 column clamping). ripr 0.9.x reports 3 weakly_gripped seams (classified reachable_unrevealed) for the three discriminator boundaries of the new condition: (1) fires on first char, (2) fires mid-loop at a surrogate boundary (the fixed case), (3) never fires / loop exhausts. All three boundaries are covered by the regression test line_starts_cache_position_to_offset_mid_surrogate_clamps_to_char_start in comprehensive_unit_tests.rs with exact assertions at columns 0, 1, 2, 3, 4 over 'x😀y' — all five assertions would fail on the reverted guard. A call-observation test in detect_dead_code_mid_surrogate_position.rs additionally drives detect_dead_code (a production caller of LineStartsCache::position_to_offset) with column 2 on 'x😀y' text and asserts the returned byte offset is 1 (not 5). RIPR cannot statically trace the accumulating `uc` variable through for-loop iterations to confirm the condition fires at the test-supplied column value — the same ripr#1429 predicate-infection-untraceable limitation as is_method_call_context, parse_quote_operator_content, and quality_baseline_cfg_test_scanner. Suppression is narrow (single production file, one PR). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
created = "2026-06-28"
review_after = "2026-07-28"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# perl-ripr-facts string-scan emitter seams (PR #3294, tracking #3293):
# ripr#1429-class weakly_gripped seams in the relocated ripr-facts exporter.
#
# PR #3294 relocates the `ripr-perl-facts-v1` exporter behavior-preserving out of
# the LSP server runtime (perl-lsp-rs::ripr_facts_emitter + the packet-assembly
# helpers in perl-lsp-rs::cli) into the new leaf crate perl-ripr-facts. Because
# the whole crate is new to `origin/main`, diff-scoped ripr re-analyzes the moved
# emitter and reports 3 `weakly_gripped` seams (classified reachable_unrevealed in
# the 0.9.x receipt).
#
# The emitter is a conservative string-scanner: detect_framework
# (content.contains("use Test::More") / Test2::V0/V1/Suite / Test::Exception /
# Test::Fatal), count_assertion_calls, collect_t_files directory/line scanning, and
# the run_ripr_facts packet-assembly / partial-upgrade branch. RIPR's static tracer
# cannot follow activation from test inputs through these string/`.contains()` scans
# to the predicate branches — the same ripr#1429 predicate-infection-untraceable
# class as parse_quote_operator_content (string-parsing chain),
# quality_baseline_cfg_test_scanner (line-scanning loops), and is_method_call_context
# (char-by-char scan). Adding tests cannot fix a static-tracer limitation.
#
# Coverage evidence: 49 tests in the crate (34 emitter unit tests + 15 lib tests),
# including the new end-to-end call-observation test
# ripr_facts_success_with_test_facts_writes_partial_packet, which drives
# run_ripr_facts on the success-with-facts path (discovers a Test::More `.t` file,
# emits the test fact, upgrades the packet to `partial`, writes it) and asserts
# framework detection + partial status. All migrated behavior is byte-identical to
# the pre-relocation perl-lsp-rs implementation.
#
# Suppression is narrow (the two source files of the new crate, one PR). Remove
# when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-perl-ripr-facts-string-scan-emitter"
kind = "predicate_infection_untraceable"
paths = [
  "crates/perl-ripr-facts/src/emitter.rs",
  "crates/perl-ripr-facts/src/lib.rs",
]
classification = ["weakly_gripped", "reachable_unrevealed", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #3294 (tracking #3293) relocates the ripr-perl-facts-v1 exporter behavior-preserving from perl-lsp-rs into the new leaf crate perl-ripr-facts. Diff-scoped ripr 0.9.x re-analyzes the moved emitter (the whole crate is new to origin/main) and reports 3 weakly_gripped seams (classified reachable_unrevealed). The emitter is a conservative string-scanner: detect_framework (content.contains for Test::More / Test2::V0/V1/Suite / Test::Exception / Test::Fatal), count_assertion_calls, collect_t_files directory/line scanning, and the run_ripr_facts packet assembly / partial-upgrade branch. RIPR cannot statically trace activation from test inputs through these string scans to the predicate branches — the same ripr#1429 predicate-infection-untraceable class as parse_quote_operator_content, quality_baseline_cfg_test_scanner, and is_method_call_context. Coverage: 49 tests (34 emitter unit tests + 15 lib tests) including the new end-to-end call-observation test ripr_facts_success_with_test_facts_writes_partial_packet, which drives run_ripr_facts on the success-with-facts path (discovers a Test::More .t file, emits the test fact, upgrades the packet to partial, writes it) and asserts framework detection + partial status. All migrated behavior is byte-identical to the pre-relocation perl-lsp-rs implementation. Suppression is narrow (two source files of the new crate, one PR). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
created = "2026-07-03"
review_after = "2026-08-03"
expires = "2026-10-03"

# -----------------------------------------------------------------------------
# eval-sub Path-2 order-guard loop/closure seams (PR #3286, closes #1429):
[[suppress]]
id = "ripr-suppress-eval-sub-order-guard-loop-closure"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-workspace/src/semantic/queries.rs"]
classification = ["weakly_gripped", "reachable_unrevealed", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #3286 (closes #1429) adds the Path-2 order guard to dynamic_callable_may_be_visible_at: `let Some(anchor) = shard.anchors.iter().find(|a| a.id == occurrence.anchor_id) else { continue }` (fail-closed) and `if anchor.span_start_byte <= byte_offset { return Some(EvalSub{..}) }`. ripr 0.9.x reports 3 weakly_gripped seams (reachable_unrevealed): the before-offset suppress branch, the after-offset continue branch, and the missing-anchor fail-closed branch. All sit behind an iterator .find() closure over the anchors vec and a byte-offset comparison inside a loop — RIPR cannot statically trace the closure/loop activation from the test-supplied byte offsets to each branch, the same ripr#1429 predicate-infection-untraceable / loop-accumulator class as the position-tracking surrogate loop seam (#3040). Coverage: direct call-observation --lib unit tests in queries.rs — dynamic_callable_eval_sub_order_guard_returns_some_when_anchor_before_offset, dynamic_callable_eval_sub_order_guard_returns_none_when_anchor_after_offset, dynamic_callable_eval_sub_fail_closed_when_anchor_missing — each constructs FileFactShard/AnchorFact/OccurrenceFact fixtures inline and asserts the exact Some/None outcome. Suppression is narrow (single production file, one PR). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
created = "2026-07-03"
review_after = "2026-08-03"
expires = "2026-10-03"

# -----------------------------------------------------------------------------
# eval-string package-scope string-scan seams (PR #3251, closes #1397):
[[suppress]]
id = "ripr-suppress-eval-string-package-scope-string-scan"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-workspace/src/semantic/eval_sub_extractor.rs"]
classification = ["weakly_gripped", "reachable_unrevealed", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #3251 (closes #1397) adds package-scope attribution to the eval-string sub extractor: find_package_keyword (word-boundary content scan), is_valid_package_name, find_package_declarations (pre-scan returning (decl_end_offset, name) pairs), and the active-package rfind + qualified-name id-hash in extract_from_eval_string/emit_triple. ripr 0.9.x reports 3 weakly_gripped seams (reachable_unrevealed): the package-active attribution branch, the rfind-over-declarations selection, and the qualified-id hash path. All sit behind string scanning (.contains / word-boundary checks) and an rfind loop over accumulated declaration offsets — RIPR cannot statically trace activation from the eval-string test inputs through these string scans / loop accumulators to the predicate branches, the same ripr#1429 predicate-infection-untraceable class as quality_baseline_cfg_test_scanner. Coverage: direct call-observation --lib unit tests in eval_sub_extractor.rs (39 total, incl. package_switch_mid_eval_via_ast_parse, three_packages_middle_sub_uses_nearest_preceding_package, distinct_packages_via_ast_parse_produce_distinct_ids, sub_before_package_remains_unscoped_via_ast_parse) with exact-value assertions on canonical names and entity IDs. Suppression is narrow (single production file, one PR). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
created = "2026-07-03"
review_after = "2026-08-03"
expires = "2026-10-03"

# -----------------------------------------------------------------------------
# DebugAdapter Drop process-kill boundary seam (PR #3247, closes #1405):
# ripr#1429-class predicate-infection-untraceable seam in the new
# `impl Drop for DebugAdapter` cleanup path.
#
# PR #3247 (closes #1405) adds `impl Drop for DebugAdapter` in
# crates/perl-dap/src/debug_adapter/mod.rs: `self.cancel_requested.store(true,
# Ordering::Release)` followed by `self.clear_active_session_state()`, so that
# child `perl -d` processes and TCP debugger sessions are always released when
# the adapter is abandoned without an explicit `disconnect` call. ripr 0.9.x
# reports the Drop body as weakly_gripped (reachable_unrevealed): the primary
# uncovered branch is inside `terminate_child_process`
# (crates/perl-dap/src/debug_adapter/process.rs), which first tries a
# zero-wait reap, then sends a real `SIGTERM` via `nix::sys::signal::kill` and
# waits, then falls back to `Child::kill`. RIPR cannot statically trace which
# of these three branches fires because activation depends on the real OS
# process's exit timing under SIGTERM — a runtime/process-scheduler predicate,
# not a value ripr's static tracer can follow from a test input.
#
# Coverage evidence: two direct call-observation --lib unit tests added to
# crates/perl-dap/src/debug_adapter/mod.rs's `#[cfg(test)] mod tests`
# construct a `DebugAdapter` and exercise the real Drop path (not a direct
# call to `clear_active_session_state`):
#   - `test_drop_sets_cancel_requested_before_clearing_session_state` clones
#     the `Arc<AtomicBool>` cancel flag before `drop(adapter)` and asserts it
#     flips from false to true, proving the ordering contract in Drop's body.
#   - `test_drop_clears_attached_pid_session_state` seeds `attached_pid` with
#     a fake PID, drops the adapter, and asserts `attached_pid` is `None`
#     afterward, proving `clear_active_session_state` genuinely runs as part
#     of Drop (not merely reachable in isolation).
# The nine tests in `crates/perl-dap/tests/dap_session_cleanup_e2e.rs`
# (5 pure-Rust + 2 Perl-gated tokio integration + 2 fast unit shapes) cover the
# same Drop path end-to-end, including `test_drop_reaps_launched_perl_process`
# which drives a real `perl -d` child through the SIGTERM branch — but as
# `tests/` integration tests they do not count toward `--lib` Codecov patch
# coverage, hence the additional --lib tests above.
#
# This is the same ripr#1429 predicate-infection-untraceable class as
# ripr-suppress-position-tracking-surrogate-loop-seam (#3040) and
# ripr-suppress-eval-sub-order-guard-loop-closure (#3286): a real branch
# whose activation depends on runtime state (there: an accumulating loop
# variable / closure capture; here: OS process exit timing under SIGTERM)
# that ripr's static tracer cannot follow from a test's inputs.
#
# Suppression is narrow (single production file, one PR). Remove when
# ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-debug-adapter-drop-process-boundary"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-dap/src/debug_adapter/mod.rs"]
classification = ["weakly_gripped", "reachable_unrevealed", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #3247 (closes #1405) adds impl Drop for DebugAdapter: self.cancel_requested.store(true, Ordering::Release) followed by self.clear_active_session_state(), ensuring child perl -d processes and TCP debugger sessions are released when the adapter is abandoned without an explicit disconnect call. ripr 0.9.x reports the Drop body as weakly_gripped (reachable_unrevealed): the primary uncovered branch is inside terminate_child_process (crates/perl-dap/src/debug_adapter/process.rs), which tries a zero-wait reap, then sends a real SIGTERM via nix::sys::signal::kill and waits, then falls back to Child::kill. RIPR cannot statically trace which branch fires because activation depends on the real OS process's exit timing under SIGTERM -- a runtime/process-scheduler predicate, not a value ripr's static tracer can follow from a test input. Coverage: two direct call-observation --lib unit tests in debug_adapter/mod.rs's #[cfg(test)] mod tests exercise the real Drop path -- test_drop_sets_cancel_requested_before_clearing_session_state clones the cancel-flag Arc before drop(adapter) and asserts it flips false-to-true; test_drop_clears_attached_pid_session_state seeds attached_pid with a fake PID, drops the adapter, and asserts attached_pid is None afterward, proving clear_active_session_state genuinely runs as part of Drop. The nine tests in crates/perl-dap/tests/dap_session_cleanup_e2e.rs (5 pure-Rust + 2 Perl-gated tokio integration + 2 fast unit shapes) cover the same path end-to-end including a real perl -d child through the SIGTERM branch, but as tests/ integration tests do not count toward --lib Codecov patch coverage. Same ripr#1429 predicate-infection-untraceable class as ripr-suppress-position-tracking-surrogate-loop-seam (#3040) and ripr-suppress-eval-sub-order-guard-loop-closure (#3286): a real branch whose activation depends on runtime state ripr's static tracer cannot follow from test inputs. Suppression is narrow (single production file, one PR). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
# rename keyword-guard predicate seams (PR #3109, closes #1401):
[[suppress]]
id = "ripr-suppress-rename-keyword-guard-predicate"
kind = "predicate_infection_untraceable"
paths = [
  "crates/perl-lsp-rs-core/src/providers/rename/validate.rs",
  "crates/perl-lsp-rs/src/runtime/language/rename.rs",
]
classification = ["weakly_gripped", "reachable_unrevealed", "no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #3109 (closes #1401) makes rename keyword-validation context-aware: validate_name rejects reserved keywords only for non-variable kinds (`if !kind.is_variable() && is_rename_keyword(name)`) with a callable-specific Err arm and a generic namespace/constant Err arm; normalize_rename_target adds a handler-layer guard `if is_rename_keyword(requested_name) { return Err(JsonRpcError{..}) }` for the non-sigiled (subroutine/package) branch. ripr 0.9.x reports 3 weakly_gripped seams (reachable_unrevealed) for the two validate.rs Err arms and the rename.rs handler-guard Err — all gated on the is_rename_keyword predicate, which is `RENAME_KEYWORDS.binary_search(&token).is_ok()` (a bool-returning predicate over a sorted string slice). RIPR cannot statically trace which test-supplied name activates the binary_search predicate into each Err branch — the same ripr#1429 predicate-infection-untraceable class as is_method_call_context and parse_quote_operator_content. Coverage: direct call-observation --lib unit tests — test_validate_new_name (crates/perl-lsp-rs-core/src/providers/rename/mod.rs) asserts validate_name(if, Subroutine) -> callable message, validate_name(if, Package) -> generic message, validate_name(for, Constant) -> Err; normalize_rename_target_rejects_invalid_targets (crates/perl-lsp-rs/src/runtime/language/rename.rs) asserts sub->keyword rejection and $var->keyword allowance; plus LSP integration tests test_rename_sub_to_keyword_fails / test_rename_variable_to_keyword_allowed. Suppression is narrow (two production files, one PR). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
created = "2026-07-03"
review_after = "2026-08-03"
expires = "2026-10-03"

# -----------------------------------------------------------------------------
# Test2::V1 import-option scanner split-closure predicates (PR #3355, closes
# #2384): focused boundary discriminator tests cover every flagged char-class
# and exact-match boundary, but ripr 0.9.x cannot trace closure predicates
# inside `str::split` to those oracles and reports the seams weakly_exposed
# with activation_unknown.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-test2-v1-import-scanner-predicates"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-lsp-rs-core/src/providers/testing/test2.rs"]
classification = ["weakly_exposed", "activation_unknown"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #3355 (closes #2384) makes Test2::V1 default-export only the T2() handle and adds -ipP grouped short-flag parsing. ripr 0.9.x reports 2 weakly_exposed severe seams (activation_unknown) for the split-closure character-class predicate at v1_short_flag/args_contains_option (crates/perl-lsp-rs-core/src/providers/testing/test2.rs:544): the tracer names missing discriminators `c == '-'`, `c == '_'`, and `c.is_ascii_alphanumeric() || c == '_'` even though direct --lib discriminator tests exist for each exact boundary — v1_short_flag_predicate_boundary_discriminators (dash retained: -i matches; underscore glue: -i_p matches neither i nor p; alphanumeric glue: -i2p matches neither), v1_import_all_predicate_exact_module_match_boundary and v1_pragma_default_predicate_exact_module_match_boundary (module == \"Test2::V1\" exact matches in resolve_import), and test2_v1_grouped_short_flags (end-to-end -ipP/-P per metacpan Test2::V1 SYNOPSIS). RIPR cannot statically connect test-supplied strings through the `str::split(|c: char| ...)` closure boundary to the seam — the same EffortlessMetrics/ripr#1429 predicate-infection-untraceable class as ripr-suppress-completion-lexical-string-scanner-boundaries and ripr-suppress-rename-keyword-guard-predicate. Suppression is narrow (one production file, one PR). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
created = "2026-07-04"
review_after = "2026-08-04"
expires = "2026-10-04"

# -----------------------------------------------------------------------------
# positionEncoding negotiation capability predicates (PR #1856, closes #1690):
# the phase-1 clamp adds a negotiation block inside handle_initialize, changing
# the function body and causing RIPR 0.9.x diff-scoped analysis to re-attribute
# PRE-EXISTING client-capability predicate seams in the same function as new
# weakly_gripped findings.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-position-encoding-negotiation-capability-predicates"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-lsp-rs/src/runtime/lifecycle/capabilities.rs"]
classification = ["weakly_gripped", "reachable_unrevealed"]
owner = "editor-trust"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #1856 (closes #1690) adds the LSP 3.17 positionEncoding negotiation block to handle_initialize (parse capabilities.general.positionEncodings, pick the first server-supported encoding via an enc.as_str() match, store it on ClientCapabilities.position_encoding; the advertised capabilities.positionEncoding stays clamped to \"utf-16\" pending phase 2). Because handle_initialize's body changed, RIPR 0.9.x diff-scoped analysis re-flags THREE seams in the function as new weakly_gripped (reachable_unrevealed): (1) completion_list_item_defaults_data_support = ...completionList/itemDefaults.any(|item| item.as_str() == Some(\"data\")) and (2) code_action_llm_generated_tag_support = ...codeAction/tagSupport/valueSet.any(|tag| tag.as_i64() == Some(1)) are PRE-EXISTING production predicates unchanged by this PR, plus (3) the new positionEncoding enc.as_str() unknown-encoding->default branch. All three are JSON-pointer predicates over parsed params that RIPR cannot statically trace from a test input through the handle_initialize entry point — the same ripr#1429 predicate-infection-untraceable class as the neighboring hover/rename predicate suppressions. Coverage: direct --lib unit tests in capabilities.rs's #[cfg(test)] mod — handle_initialize_boundary_discriminator drives itemDefaults=[..,\"data\"] and tagSupport.valueSet=[2,1] and asserts both flags flip true (naming both boundaries), and the negotiation scenario tests (initialize_prefers_first_supported_position_encoding, initialize_accepts_utf16_when_it_is_first_supported_position_encoding, initialize_falls_back_to_utf16_when_position_encodings_have_no_supported_values) exercise the enc.as_str() match including the unknown-encoding [\"utf-32\",\"utf-7\"]->utf16 default branch. Suppression is narrow (single production file, one PR) and does not touch the pre-existing coverage. Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
# find_symbol scope-walk predicates (PR #1836, closes #1699): the DESTROY/
# AUTOLOAD UNIVERSAL-method correction adds a doc comment on UNIVERSAL_METHODS
# and two #[cfg(test)] discriminator tests to symbol.rs. Touching the file
# causes RIPR 0.9.x diff-scoped analysis to re-attribute the PRE-EXISTING
# SymbolTable::find_symbol scope-chain-walk predicate seams as new
# weakly_gripped findings.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-find-symbol-scope-walk-predicates"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-semantic-analyzer/src/analysis/symbol.rs"]
classification = ["weakly_gripped", "reachable_unrevealed"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #1836 (closes #1699) corrects the DESTROY/AUTOLOAD categorization: it adds a doc comment on the UNIVERSAL_METHODS const and two #[cfg(test)] tests to crates/perl-semantic-analyzer/src/analysis/symbol.rs. It makes NO change to SymbolTable::find_symbol (unchanged vs origin/main). Because the file was touched, RIPR 0.9.x diff-scoped analysis re-flags THREE pre-existing scope-chain-walk predicate seams inside find_symbol as new weakly_gripped (reachable_unrevealed): (1) symbol.scope_id == scope_id and (2) symbol.kind == kind (the direct-scope match filter), and (3) scope.kind != ScopeKind::Package (the `our`-variable package-scope fallback guard). All three are predicates evaluated while walking the parent scope chain that RIPR cannot statically trace from a test input to a specific branch — the same ripr#1429 predicate-infection-untraceable class as the neighboring rename/hover predicate suppressions. Coverage: a direct call-observation --lib unit test, find_symbol_boundary_discriminator (symbol.rs #[cfg(test)] mod), builds a SymbolTable by hand with a package scope and a nested block scope, then asserts each boundary in isolation — a Subroutine queried from its defining scope with matching kind returns exactly one hit (scope_id + kind match), the same name queried with a scalar kind returns empty (kind mismatch), and an `our` scalar is surfaced from the non-package block scope via the fallback (scope.kind != Package). RIPR still cannot connect the test to the scope-walk branches. Suppression is narrow (single production file, one PR). Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
created = "2026-07-03"
review_after = "2026-08-03"
expires = "2026-10-03"

# perl-workspace-core + perl-tree-sitter-compat: additive facts-substrate crates
# that are NOT yet wired into any production binary. See PLSP-ADR-0006 (#3312).
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-workspace-core-substrate-not-yet-wired"
kind = "generated_or_non_production_surface"
paths = ["crates/perl-workspace-core/**", "crates/perl-tree-sitter-compat/**"]
classification = ["exposed", "weakly_exposed", "weakly_gripped", "static_unknown", "propagation_unknown", "infection_unknown", "no_static_path", "reachable_unrevealed"]
owner = "proof-lane"
issue = "EffortlessMetrics/perl-lsp-swarm#3312"
reason = "PR #3313 lands perl-workspace-core (the LSP-free project-facts substrate) and perl-tree-sitter-compat (its first adapter) as ADDITIVE crates that no shipped binary consumes yet: the substrate is deliberately wired into ripr-facts / critic / tidy / DAP only in the later staged-rollout PRs 5 and 8 (PLSP-ADR-0006). Because nothing calls into these crates from a production entrypoint, ripr's static production-reachability analysis has no caller to trace and classifies the crates' own public surface as exposed/weakly_exposed/no_static_path/static_unknown (670 diff-scoped severe gaps -> after adding the direct --lib observe tests below, 536). Those are not un-revealed production seams; they are a facts substrate whose correctness is proven by its own 187 unit + integration tests (perl-workspace-core lib tests, import_facts/dist_facts/pod_relation_facts integration suites, tree-sitter-compat adapter tests) plus the enforced dependency contract (tests/dependency_contract.rs). The genuinely reachable_unrevealed seams ripr DID find (13, all the DynamicBoundary/export/inheritance side-effect pushes in crates/perl-workspace-core/src/import_walk.rs) are NOT suppressed on their merits: they are directly covered by 8 new call-observation --lib unit tests in import_walk.rs's #[cfg(test)] mod tests (string_eval/typeglob/runtime-require/source-filter boundary observation, use-parent inheritance, @EXPORT symbols), which drop reachable_unrevealed to 0. This suppression is a time-boxed bridge for the not-yet-wired remainder only; when PRs 5/8 wire the substrate into a production entrypoint, ripr can trace it for real and this entry must be removed."
created = "2026-07-03"
review_after = "2026-08-15"
expires = "2026-10-15"
removal_criteria = "perl-workspace-core / perl-tree-sitter-compat are consumed by a production entrypoint (PLSP-ADR-0006 PR 5 or PR 8); then ripr traces them for real and this suppression is deleted."

# -----------------------------------------------------------------------------
# PERL_LSP_TIMING parse_mode exact-value predicates (PR #3397, #3396): direct
# unit tests call parse_mode with the exact "0"/"1" target literals, but the
# ripr 0.9.x run mis-associates them — its guidance pass timed out (210s) and
# the mis-association reproduced on a clean re-run of the same head.
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# INDIRECT_METHOD_EXCLUDED const array (PR #1914, #1758): no_static_path on a
# const-data declaration that ripr cannot trace to a test through `.contains()`.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-indirect-method-excluded-const-no-static-path"
kind = "const_data_no_static_path"
paths = ["crates/perl-lsp-rs-core/src/providers/completion/completion/request/dispatch.rs"]
classification = ["no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/perl-lsp-swarm#1758"
reason = "PR #1914 (#1758) adds indirect-object method-call completion in dispatch.rs. ripr 0.9.x reports 2 no_static_path severe seams on the `const INDIRECT_METHOD_EXCLUDED: &[&str]` array — dispatch.rs:144 (family field_construction, the `const ... = &[` declaration) and dispatch.rs:145 (family return_value, the string-literal contents) — each reach:no, \"No static test path\", confidence 0.48, severity info. The const IS exercised: is_indirect_method_word gates on `INDIRECT_METHOD_EXCLUDED.contains(&word)` (dispatch.rs:182), and the inline indirect_helper_tests assert is_indirect_method_word(\"print\"), (\"my\"), (\"return\"), (\"croak\") == false plus the through-dispatch tests test_indirect_print_filehandle/die/warn_*_does_not_offer_methods in completion/tests.rs exercise the exclusion end-to-end. But ripr's field-construction/return-value probes on a const *data literal* cannot statically trace a path from a test through `.contains()` to the const's storage — const data has no call path the way a function body does. This is a no_static_path-on-const analyzer artifact of the EffortlessMetrics/ripr#1429 class (tracer cannot connect a direct oracle to a const-data seam), not a missing test. HONEST SCOPE NOTE: like every sibling entry, the matcher (read_ripr_suppression_rules) is PATH-scoped — the `classification` field records intent but is not enforced, so this covers all diff-scoped ripr findings in dispatch.rs, not only the no_static_path pair. Net blocking impact is still exactly those 2 seams: under the merged #3363 (#2015) recalibration only reachable_unrevealed + no_static_path block (weakly_exposed is already advisory-excluded) and this diff has reachable_unrevealed = 0. The accepted, time-boxed residual is that a FUTURE reachable_unrevealed gap introduced in dispatch.rs would be masked for this window — tighten by isolating INDIRECT_METHOD_EXCLUDED into its own file (narrower path) or by the ripr#1429 fix. Remove when ripr can trace const-data references, when targeted mutation replaces this advisory path (#1289), or when the exclusion set is restructured so ripr traces it."
created = "2026-07-04"
review_after = "2026-08-04"
expires = "2026-10-04"
removal_criteria = "ripr#1429 fixed upstream (const-data seams traceable), OR targeted mutation scheduled for dispatch.rs (#1289), OR INDIRECT_METHOD_EXCLUDED restructured so ripr finds a static path."

# -----------------------------------------------------------------------------
# perl-core-harness xtask extraction (PR #3421, closes #3420): the old
# xtask/src/tasks/perl_core_harness.rs implementation was mechanically moved
# into the private perl-core-harness crate, leaving xtask as CLI glue only.
# ripr 0.9.x reports deleted/moved seams from the old xtask path as new
# reachable_unrevealed findings even though the active implementation now lives
# under crates/perl-core-harness and is covered by the focused harness tests.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-perl-core-harness-xtask-extraction-move"
kind = "mechanical_move_extraction"
paths = ["xtask/src/tasks/perl_core_harness.rs"]
classification = ["weakly_gripped", "reachable_unrevealed"]
owner = "proof-lane"
issue = "EffortlessMetrics/perl-lsp-swarm#3420"
reason = "PR #3421 extracts the active Perl core harness orchestration from xtask/src/tasks/perl_core_harness.rs into the private crates/perl-core-harness crate, leaving the xtask file as a re-export/CLI glue surface. ripr 0.9.x reports 37 reachable_unrevealed findings on deleted/moved old xtask lines (run/prepare/smoke/baseline helper calls and test-helper branches) even though the behavior is preserved and covered by cargo test -p perl-core-harness plus PR Smoke's perl-core-harness unit_scoped path. This suppression is deliberately path-scoped to the old xtask file, which now contains no implementation logic; active harness code in crates/perl-core-harness remains visible to RIPR. Remove if implementation logic returns to this xtask file or when RIPR can classify mechanical moves without re-opening deleted seams."
created = "2026-07-05"
review_after = "2026-08-05"
expires = "2026-10-05"
removal_criteria = "xtask/src/tasks/perl_core_harness.rs contains implementation logic again, OR ripr correctly treats the #3421 mechanical move/deleted old lines as non-actionable."

# -----------------------------------------------------------------------------
# navigation_same_document_toctou_regression_tests.rs: Result<()> test-helper
# error_path/side_effect seams flagged as new production seams (ripr#1428
# class).
#
# PR #3622 (fix #3613) is a Result<()>-returning integration test file per
# this repo's own mandated test convention ("Tests: Result<()> returns or
# perl_tdd_support::must/must_some" -- CLAUDE.md Coding Standards). ripr 0.9.x
# reports 17 severe-gap findings entirely within this one test file:
#   - 16 `error_path` findings on the `?`-terminated closing braces of
#     `.ok_or_else(...)`/`.map_err(...)` chains that convert a channel
#     recv/send failure or an unexpected non-array handler result into the
#     test's own `Err(String)` -- pure test-assertion plumbing, not
#     exploitable production behavior.
#   - 1 `side_effect` finding on a `resume_tx.send(())?` call -- the
#     test-only signal that releases a handler paused at the
#     `wait_at_same_doc_fallback_gap` hook.
#
# ripr's own reach/infect/propagate fields report "yes" (the tests do reach
# and exercise this code), but "observe"/"discriminate" report "no" because
# each finding's guarded failure condition is exactly what the test's own
# `assert!`/`assert_eq!` immediately following it already proves does not
# happen. This is the same ripr#1428 class (test-harness plumbing flagged as
# production seams) as ripr-suppress-seam-proof-tests and
# ripr-suppress-quote-operator-conformance-test-helpers above. All findings
# sit inside `#[test]` functions in a `tests/` integration file, not
# production code; the behavior under test (same-document TOCTOU pinning
# across a racing didChange/didClose, and the raw/normalized URI-key
# deduplication in `pinned_doc_map_for`) is proven by the assert! calls
# immediately following each flagged line.
#
# Suppression is narrow: single test file, one PR. Remove when ripr#1428 is
# fixed upstream.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-navigation-toctou-regression-test-helpers"
kind = "test_receipt_surface"
paths = ["crates/perl-lsp-rs/tests/navigation_same_document_toctou_regression_tests.rs"]
classification = ["reachable_unrevealed", "weakly_exposed", "error_path", "side_effect"]
owner = "lsp-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "PR #3622 (fix #3613) adds/extends navigation_same_document_toctou_regression_tests.rs, a Result<()>-returning integration test file per this repo's mandated test convention. ripr 0.9.x reports 17 severe-gap findings entirely within this file: 16 error_path findings on `?`-terminated .ok_or_else(...)/.map_err(...) chains, and 1 side_effect finding on a resume_tx.send(())? handshake call -- all pure test-assertion/synchronization plumbing. ripr reports reach/infect/propagate=yes but observe/discriminate=no because it cannot recognize that the immediately-following assert!/assert_eq! already proves the guarded failure condition does not occur. Same ripr#1428 class (test-harness plumbing flagged as production seams) as ripr-suppress-seam-proof-tests and ripr-suppress-quote-operator-conformance-test-helpers. Suppression is narrow: single test file, one PR. Remove when ripr#1428 is fixed upstream."
created = "2026-07-10"
review_after = "2026-08-10"
expires = "2026-09-30"
removal_criteria = "ripr correctly recognizes the guarding assert!/assert_eq! as the discriminator for a preceding `?`/send() in Result<()> test functions, OR ripr#1428 is fixed upstream."

# -----------------------------------------------------------------------------
# my-declaration comma-precedence fixture: match-arm error-reporting branches
# in a new Result<(), String> test file (fix #3748):
#
# PR #3908 adds fix_3748_my_assign_comma_rhs.rs, a Result<(), String>-returning
# integration test file per this repo's mandated test convention. ripr 0.9.x
# reports 3 severe-gap findings (error_path, reachable_unrevealed) at lines 53,
# 88, and 145 -- each is the `));` that closes a
# `other => return Err(format!("...", other))` fallback arm inside a `match`
# over the parsed AST shape. These arms exist so a shape the fix does NOT
# produce fails the test with a diagnostic message instead of an unwrap/panic
# (per this repo's coding standards for tests). ripr reports reach/infect/
# propagate=yes but observe/discriminate=no because it cannot recognize that
# the SAME match's happy-path arms (the assert_eq!/element-count assertions a
# few lines above each flagged line) are exactly what proves the guarded
# "unexpected shape" condition does not occur -- the match itself IS the
# oracle; the `other =>` arm is its failure-reporting path, not a separate
# unproven seam. Same ripr#1428 class (test-harness plumbing flagged as
# production seams) as ripr-suppress-navigation-toctou-regression-test-helpers
# and ripr-suppress-references-include-declaration-quality-tests above.
#
# Suppression is narrow: single test file, one PR. Remove when ripr#1428 is
# fixed upstream.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-my-assign-comma-rhs-test-helpers"
kind = "test_receipt_surface"
paths = ["crates/perl-parser-core/tests/fix_3748_my_assign_comma_rhs.rs"]
classification = ["error_path", "reachable_unrevealed"]
owner = "parser-lane"
issue = "#3748"
reason = "PR #3908 (fix #3748: my-declaration initializer RHS binds at assignment precedence) adds fix_3748_my_assign_comma_rhs.rs, a Result<(), String>-returning integration test file. ripr 0.9.x flags 3 findings (error_path/reachable_unrevealed) at lines 53, 88, and 145, each the closing `));` of an `other => return Err(format!(...))` fallback arm in a `match` over the parsed AST node shape. These are ripr#1428-class false-positives: the arms are test-diagnostic plumbing (fail loudly with a message instead of unwrap/panic, per repo test-coding standards), not unproven production behavior -- the SAME match's preceding happy-path arms carry the real assert_eq!/element-count oracle assertions that prove the guarded shape mismatch does not occur. The production fix (crates/perl-parser-core/src/engine/parser/variables.rs) is separately classified 'exposed' by the same ripr run. Same class as ripr-suppress-navigation-toctou-regression-test-helpers and ripr-suppress-references-include-declaration-quality-tests. Remove when ripr#1428 is fixed upstream."
created = "2026-07-11"
review_after = "2026-08-11"
expires = "2026-09-30"
removal_criteria = "ripr correctly recognizes the guarding assert_eq!/happy-path match arms as the discriminator for a sibling `other => return Err(...)` fallback arm in Result<(), String> test functions, OR ripr#1428 is fixed upstream."

# -----------------------------------------------------------------------------
# Repository Contract receipt/check runner (#3987): deterministic contract
# plumbing is covered by focused unit and exact-head receipt proof, but RIPR
# 0.9.x cannot trace the subprocess, serialization, and in-module test seams.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-repository-contract-receipt-seams"
kind = "call_presence_static_tracing_limit"
paths = ["xtask/src/tasks/ci_contract.rs"]
classification = ["reachable_unrevealed", "weakly_exposed"]
owner = "ci"
issue = "#3987"
reason = "Issue #3987 adds ci_contract.rs, a deterministic exact-head repository-contract receipt builder and bounded check runner. Focused tests cover check selection, exact two-dot diff identity, status precedence, exit/result mapping including instrument failures and explicit WARN findings, full object identity validation, bounded output, and JSON/Markdown receipt identity. The exact-head command is also executed locally against the committed branch and produces the documented advisory POLICY_FINDING baseline. RIPR 0.9.x reports reachable_unrevealed/weakly_exposed seams across subprocess draining, serialization, and the in-module contract-test harness because it cannot connect those contract oracles through the receipt/check boundary. This narrow single-file suppression preserves the required gate while keeping the receipt, focused tests, workflow-contract checks, and cargo-allow proof authoritative. Remove when RIPR can trace subprocess-backed contract receipts or targeted mutation replaces this advisory path."
created = "2026-07-17"
review_after = "2026-08-17"
expires = "2026-09-30"
removal_criteria = "RIPR can trace the exact-head receipt/check runner through its focused contract oracles, or targeted mutation replaces this advisory static path."

# -----------------------------------------------------------------------------
# Formatting policy handlers (#7082 slice 2): advertise-gate / missing-params /
# admit / CodeFormatter call sites are covered by named call_presence observers
# colocated in handlers.rs plus live JSON-RPC dispatch tests, but RIPR 0.9.x
# still reports weakly_gripped call_presence (unchanged_tests=true across
# multiple observer iterations). Same static-tracing limit class as
# ripr-suppress-type-hierarchy-cancellation-routing / EffortlessMetrics/ripr#1429.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-formatting-policy-handler-call-presence"
kind = "call_presence_static_tracing_limit"
paths = ["crates/perl-lsp-rs/src/runtime/language/formatting_policy/handlers.rs"]
classification = ["weakly_gripped", "reachable_unrevealed", "call_presence", "static_unknown", "infection_unknown", "propagation_unknown"]
owner = "lsp-lane"
issue = "#7082"
reason = "PR #7554 (#7082 slice 2) wires textDocument/formatting through handle_formatting_policy. Focused observers colocated in handlers.rs (handle_formatting_policy_call_presence_observer, _ensure_surface_advertised, _missing_params) assert the exact ripr candidate strings for ensure_surface_advertised, params.ok_or_else/invalid_params, admit, CodeFormatter::with_config_and_mode, and snapshot URI/generation bindings; live_dispatch_routes_document_formatting_through_receipt_policy and live_stale_request_returns_content_modified_not_successful_empty exercise the JSON-RPC dispatcher path. RIPR 0.9.x still reports weakly_gripped call_presence / reachable_unrevealed on those call sites with unchanged_tests=true because the static tracer cannot connect assertion messages and Result error codes through the handler. Narrow single-file suppression; remove when ripr#1429 can grip colocated call_presence observers or targeted mutation replaces this path."
created = "2026-08-13"
review_after = "2026-09-13"
expires = "2026-10-31"
removal_criteria = "RIPR grips the colocated handle_formatting_policy_call_presence_observer* tests against handlers.rs call sites, or targeted mutation replaces this advisory static path."

# -----------------------------------------------------------------------------
# Heredoc queue contract integration test (PR #6760 / issue #6727):
# Result-style test plumbing misclassified as production seams (ripr#1428).
#
# The exact-head RIPR 0.9.0 artifact reports 639 findings in the newly added
# integration-test file: 447 exposed, 28 reachable_unrevealed/error_path,
# 2 weakly_exposed/predicate, 11 infection_unknown, 3 propagation_unknown,
# and 148 static_unknown. The current matcher is path-only and ignores the
# documentary `classification` field, so this exact-path entry suppresses all
# 639, not only the 30 pre-suppression actionable/severe findings. It suppresses
# zero production findings; the post-suppression actionable/severe count is 0
# and the new-gap quality gate passes. The focused 7-case contract directly
# observes FIFO queueing, opener/body/source geometry, complete false-opener
# token streams, exact terminator exclusion, recovery absence, tail resumption,
# and EOF.
#
# Suppression is exact-path and test-only: no production path, glob, severity
# threshold, or repository-wide RIPR behavior is weakened. Remove when RIPR
# distinguishes Result-style integration-test plumbing, when the matcher honors
# classification filters, or when exact-head evidence no longer needs this
# whole-file exception.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-heredoc-queue-contract-test-surface"
kind = "test_receipt_surface"
paths = ["crates/perl-lexer/tests/heredoc_queue_contract.rs"]
owner = "lexer-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "PR #6760 (issue #6727) adds heredoc_queue_contract.rs, an integration-test contract using the fallible `type R<T = ()> = Result<T, Box<dyn std::error::Error>>` alias required by repository policy. The fresh exact-head RIPR 0.9.0 receipt reports 639 findings in this test-only file: 447 exposed, 28 reachable_unrevealed/error_path, 2 weakly_exposed/predicate, 11 infection_unknown, 3 propagation_unknown, and 148 static_unknown. The repository matcher currently applies suppressions by path and ignores documentary classification filters, so this exact-path entry truthfully suppresses all 639 current findings, not only the 30 pre-suppression actionable/severe findings. Production findings suppressed are exactly 0; post-suppression actionable/severe findings are 0 and the new-gap quality gate passes. The 7 focused tests directly assert FIFO heredoc queueing, opener/body/source geometry, full false-opener token projections, exact terminator exclusion and non-overlap, recovery absence, tail resumption, and one terminal EOF. This time-boxed whole-test-file exception matches the ripr#1428 test_receipt_surface precedent. It changes no production path, glob, RIPR parser, or severity threshold."
created = "2026-08-13"
review_after = "2026-09-30"
expires = "2026-10-31"
removal_criteria = "EffortlessMetrics/ripr#1428 is fixed so Result-style integration-test helpers are not treated as production seams, OR the suppression matcher enforces finding-level classification filters and a narrower evidence-backed rule can replace this path-wide exception, OR an exact-head RIPR artifact no longer needs suppression for this file; then delete or narrow this entry and rerun the RIPR new-gap gate."

# -----------------------------------------------------------------------------
# Zed host/public-registry shared fail-closed validators (PR #7958 / #7912):
# test-support helpers under xtask/tests/support misclassified as production
# seams (ripr#1428).
#
# Exact-head RIPR reports 33 no_static_path + 2 weakly_exposed findings in
# xtask/tests/support/zed_host_compat.rs (match arms, Ok(()) markers, public
# pass identity predicates). The helpers are compiled only into Zed programme
# contract tests; public-registry loaders exercise the remaining reachable
# error paths in zed_public_registry_receipt.rs. Suppress this single
# test-support file; do not suppress the contract tests or fixtures.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-zed-host-compat-test-support"
kind = "test_receipt_surface"
paths = ["xtask/tests/support/zed_host_compat.rs"]
owner = "integrations-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "PR #7958 adds xtask/tests/support/zed_host_compat.rs as shared fail-closed validators for the Zed host and public-registry programme contracts. Exact-head RIPR 0.9.x reports 33 no_static_path and 2 weakly_exposed findings on match-arm/Ok(())/predicate plumbing inside that support module even though it is compiled only into xtask tests and the discriminating oracles live in zed_host_receipt.rs / zed_public_registry_receipt.rs (including explicit public-subject read/parse failure coverage). Same ripr#1428 test_receipt_surface class as ripr-suppress-heredoc-queue-contract-test-surface and ripr-suppress-seam-proof-tests. Exact-path, test-support-only; production findings suppressed are 0."
created = "2026-08-13"
review_after = "2026-09-13"
expires = "2026-10-31"
removal_criteria = "EffortlessMetrics/ripr#1428 distinguishes xtask/tests/support helpers from production seams, or exact-head RIPR no longer reports actionable gaps in zed_host_compat.rs after the contract tests grip the validators; then delete this entry and rerun the RIPR new-gap gate."

# -----------------------------------------------------------------------------
# Shared client-compat fixture/expectation identity (PR #7874 / #7777):
# Result-style contract plumbing misclassified as production seams (ripr#1428).
#
# Same class as ripr-suppress-zed-host-compat-test-support. The structural half
# was fixed rather than suppressed first: the contract modules moved from
# xtask/tests/support into xtask/src, which eliminated every no_static_path
# finding (74 -> 0) and made the contract production-reachable like its
# actual_host_receipt.v1 peer.
#
# What remains is not closable by proof. Exact-head RIPR 0.9.0 reports 23
# reachable_unrevealed findings, of which 14 are the trailing `Ok(())` of the
# contract test functions themselves; the rest are `Ok(())`, `})?` and
# `digest_identity(hasher)` plumbing on already-covered paths. Writing more
# tests raises the count rather than lowering it — measured across three
# iterations on this candidate: 80 actionable before the move, 11 after it,
# 23 after adding six discriminating guard tests, and 23 again after relocating
# those tests to an integration contract. The guards themselves are proven in
# xtask/tests/client_compat_fixture_contract.rs (empty/duplicate/untokenized
# expectation sets, set-id binding, order independence, membership binding,
# non-directory and empty fixture roots, path-versus-content separation).
#
# One genuinely uncovered path is retained deliberately and not claimed as
# proven: the non-UTF-8 fixture path guard in fixture_digest, which needs a
# platform-specific invalid filename to reach.
#
# Exact-path and contract-scoped; no provider, LSP, DAP, or parser finding is
# suppressed.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-client-compat-fixture-contract-surface"
kind = "test_receipt_surface"
paths = [
  "xtask/src/client_compat_fixture.rs",
  "xtask/tests/client_compat_fixture_contract.rs",
  "xtask/tests/support/agent_client_compat.rs",
]
classification = ["reachable_unrevealed", "no_static_path"]
owner = "integrations-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "PR #7874 establishes the shared fixture and expectation-set identity consumed by agent_client_compat.v1 and editor_client_compat.v1. Exact-head RIPR 0.9.0 reports 23 reachable_unrevealed findings on Result-style contract plumbing, 14 of which are the trailing Ok(()) of the contract tests themselves — a class no additional test can reveal, measured across three iterations (80 actionable before moving the modules into xtask/src, 11 after, 23 after adding guard tests, 23 after relocating them to an integration contract). The discriminating oracles live in xtask/tests/client_compat_fixture_contract.rs and cover every guard except the non-UTF-8 fixture path, which is left explicitly unproven. Same ripr#1428 test_receipt_surface class as ripr-suppress-zed-host-compat-test-support. Exact-path and contract-scoped; production provider/LSP/DAP/parser findings suppressed are 0."
created = "2026-08-16"
review_after = "2026-09-16"
expires = "2026-10-31"
removal_criteria = "EffortlessMetrics/ripr#1428 stops classifying Result-style test and contract plumbing as unrevealed production seams, or exact-head RIPR no longer reports actionable gaps in these files; then delete this entry and rerun the RIPR new-gap gate."

# -----------------------------------------------------------------------------
# handle_slow_operation_dispatch error_variant seams (PR #9674): the
# `Err(server_cancelled_error())` return form resists test-based gripping
# even with the exact recommended discriminator (ripr#1429 class, escalated
# to a dedicated upstream issue for the error_variant weakly_gripped case).
#
# PR #9674 (make perl-lsp-rs and three sibling crates clippy-clean on all
# targets) touches crates/perl-lsp-rs/src/runtime/dispatch/experimental.rs.
# ripr 0.10.0 reports two weakly_gripped error_variant gaps on the
# `Err(server_cancelled_error())` return at experimental.rs:66:
#   (1) 8ce00adcbe1aa3d9 — the `Err(server_cancelled_error())` return form
#   (2) 8ce00bdcbe1aa58c — the error_variant return form (sibling seam)
#
# The neighboring boundary gap ca5c52bed561439e (`start.elapsed() >= to` at
# experimental.rs:64) WAS cleared by a boundary discriminator test
# (handle_slow_operation_dispatch_boundary_discriminator) that forces
# `to = Duration::ZERO` via `serverTimeoutMs: 0`, making the boundary
# statically determinable. The same `serverTimeoutMs: 0` input was used to
# build the exact recommended error_variant grip
# (handle_slow_operation_dispatch_exact_error_variant), which asserts the
# returned variant is exactly `JsonRpcError { code: SERVER_CANCELLED, .. }`
# with the exact message and `data.is_none()`.
#
# Confirmed locally with ripr 0.10.0: even the exact recommended
# error_variant discriminator does NOT clear the weakly_gripped
# classification. The seam sits in a cfg-gated test-only code path
# (`#[cfg(test)] mod slow_operation_timeout_tests`) and the error_variant
# return form resists test-based gripping — the static analyzer cannot
# connect the test's `matches!` oracle through the `Err(...)` return to the
# error_variant construction. This is a stricter limitation than the
# ripr#1429 predicate-infection-untraceable class: the error_variant return
# form is gripped by an exact-variant oracle yet remains weakly_gripped.
#
# Owner approval: the maintainer approved suppressing this resistant seam
# rather than weakening the test or ratcheting the gate. A dedicated upstream
# issue has been filed on EffortlessMetrics/ripr documenting the
# error_variant weakly_gripped resistance for cfg-gated test functions.
#
# Suppression is narrow: single production file, only the weakly_gripped
# classification (the cleared boundary gap is untouched). Remove when the
# upstream error_variant gripping issue is fixed or targeted mutation
# replaces this advisory path.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-slow-operation-dispatch-error-variant"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-lsp-rs/src/runtime/dispatch/experimental.rs"]
classification = ["weakly_gripped"]
owner = "lsp-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #9674 touches experimental.rs; ripr 0.10.0 reports two weakly_gripped error_variant gaps (8ce00adcbe1aa3d9 and 8ce00bdcbe1aa58c) on the `Err(server_cancelled_error())` return at experimental.rs:66. The neighboring boundary gap ca5c52bed561439e (`start.elapsed() >= to`) was cleared by handle_slow_operation_dispatch_boundary_discriminator (serverTimeoutMs:0 forces to=Duration::ZERO). The exact recommended error_variant grip (handle_slow_operation_dispatch_exact_error_variant, same serverTimeoutMs:0 input, asserts matches!(err, JsonRpcError { code: SERVER_CANCELLED, .. }) plus exact message and data.is_none()) does NOT clear the weakly_gripped classification — confirmed locally with ripr 0.10.0. The error_variant return form resists test-based gripping even with an exact-variant oracle; the seam sits in a cfg-gated test-only function. Maintainer-approved suppression of the resistant seam. Suppression is narrow: single production file, only the weakly_gripped classification (the cleared boundary gap is untouched). Remove when the upstream error_variant gripping issue is fixed or targeted mutation replaces this advisory path."
created = "2026-08-17"
review_after = "2026-09-17"
expires = "2026-10-31"
removal_criteria = "EffortlessMetrics/ripr can grip an exact error_variant return form from a cfg-gated test function's matches! oracle, or targeted mutation replaces this advisory path; then delete this entry and rerun the RIPR new-gap gate."

# -----------------------------------------------------------------------------
# handle_code_action associated-diagnostic equality guard (PR #9674):
# `diagnostic.code.as_deref() == Some(code) && diagnostic.range == range` at
# code_actions.rs:749 — ripr#1429-class equality-guard predicate whose
# activation RIPR cannot statically trace from JSON test inputs through the
# diagnostic-list `find` closure.
#
# PR #9674 touches crates/perl-lsp-rs/src/runtime/language/code_actions.rs.
# ripr reports a weakly_gripped boundary gap (5cdcf8ac817bf4de) on the
# equality guard at line 749:
#   diagnostic.code.as_deref() == Some(code) && diagnostic.range == range
# inside the `diagnostics.iter().find(...)` closure that associates a
# quick-fix action with its originating diagnostic.
#
# This is the same ripr#1429 class as ripr-suppress-lsp-declaration-
# equality-guards: an equality-guard predicate whose activation RIPR cannot
# statically confirm via call-graph tracing from the test's JSON-RPC inputs
# through the diagnostic-list parsing into the `find` closure's equality
# guard. There is no `serverTimeoutMs:0`-style trick that makes this boundary
# statically determinable — it is an equality over parsed diagnostic data
# structures, so a boundary discriminator test cannot clear it the way the
# experimental.rs boundary was cleared.
#
# The boundary IS exercised by an existing integration test:
#   test_quickfix_actions_include_associated_diagnostics
# (crates/perl-lsp-rs/tests/lsp_code_actions_tests.rs) pulls diagnostics,
# requests codeAction with those diagnostics in context, and asserts the
# resulting quick-fix action carries the associated diagnostic with
# matching range/severity/code/source/message — i.e. the `find` closure's
# equality guard MUST return true for the asserted associated diagnostic to
# appear. The boundary is covered; RIPR just cannot trace the activation.
#
# Owner approval: the maintainer approved suppressing this ripr#1429-class
# equality-guard gap rather than adding a test the analyzer cannot credit.
# Suppression is narrow: single production file, one PR. Remove when
# ripr#1429 is fixed upstream or targeted mutation replaces this advisory
# path.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-code-action-associated-diagnostic-equality-guard"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-lsp-rs/src/runtime/language/code_actions.rs"]
classification = ["weakly_gripped", "reachable_unrevealed"]
owner = "lsp-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #9674 touches code_actions.rs; ripr reports a weakly_gripped boundary gap (5cdcf8ac817bf4de) on the equality guard `diagnostic.code.as_deref() == Some(code) && diagnostic.range == range` at code_actions.rs:749 inside the `diagnostics.iter().find(...)` closure that associates a quick-fix action with its originating diagnostic. Same ripr#1429 class as ripr-suppress-lsp-declaration-equality-guards: RIPR cannot statically trace activation from JSON-RPC test inputs through diagnostic-list parsing into the find closure's equality guard. No statically-determinable boundary trick applies (equality over parsed diagnostic data structures, unlike the experimental.rs Duration::ZERO boundary). The boundary IS exercised by test_quickfix_actions_include_associated_diagnostics (crates/perl-lsp-rs/tests/lsp_code_actions_tests.rs), which pulls diagnostics, requests codeAction with them in context, and asserts the quick-fix carries the associated diagnostic with matching range/severity/code/source/message — the find closure's equality guard must return true for the asserted diagnostic to appear. Maintainer-approved suppression. Suppression is narrow: single production file, one PR. Remove when ripr#1429 is fixed upstream or targeted mutation replaces this advisory path."
created = "2026-08-17"
review_after = "2026-09-17"
expires = "2026-10-31"
removal_criteria = "EffortlessMetrics/ripr#1429 lets RIPR trace equality-guard activation from JSON-RPC test inputs through the diagnostic-list find closure, or targeted mutation replaces this advisory path; then delete this entry and rerun the RIPR new-gap gate."

# -----------------------------------------------------------------------------
# Generic comparison evidence model (PR #9964, closes #8127): ripr reports
# no_static_path for changed seams in the new evidence model whose
# discriminating tests already exist in
# crates/perl-parser-comparison/tests/generic_result_model.rs but that the
# static tracer cannot connect:
#   (a) non-executable declarations — private const limits, tuple-struct
#       declarations, struct field declarations, and enum variant
#       declarations (e.g. evidence.rs:12-14, 34, 192-194, 353-355,
#       388-395, 597-603; harness.rs:17-18, 78-82);
#   (b) Display/Error trait-impl bodies reached only through
#       ToString::to_string / Error::source dispatch, asserted exactly by
#       checked_id_and_fingerprint_display_surface_is_stable,
#       model_error_display_and_source_are_stable, and
#       legacy_label_and_verdict_display_is_unchanged;
#   (c) private helpers (can_score, validate_observation_states,
#       validate_nonempty_bounded, validate_no_control_characters,
#       subject_execution, *_observations, failed_observations,
#       bounded_optional_text) reached only through the public constructors
#       that call them — exercised with exact error-variant and exact-value
#       oracles by failed_harness_cannot_carry_subject_disposition_or_decisive_score,
#       incomplete_instrument_cannot_carry_decisive_score,
#       limited_observation_cannot_carry_decisive_score,
#       failed_harness_rejects_observed_plane_and_complete_instrument,
#       complete_observation_requires_complete_instrument,
#       limited_observation_rejects_unusable_instrument_states,
#       truncated_instrument_can_carry_limited_observation,
#       fingerprints_and_divergence_paths_reject_control_characters,
#       fingerprint_and_divergence_path_enforce_exact_byte_limits,
#       stable_ids_reject_empty_leading_separator_non_ascii_and_overlong_values,
#       bounded_text_rejects_zero_limit_and_keeps_short_input_intact, and
#       bounded_text_exposes_omitted_bytes_without_splitting_utf8;
#   (d) panic::catch_unwind closure bodies in run_v1/run_v3, exercised through
#       execute_v1/execute_v3 by
#       historical_tree_sitter_diagnostic_summary_counts_error_nodes_exactly,
#       native_recursive_descent_diagnostic_summary_counts_diagnostics_exactly,
#       historical_error_nodes_are_recovery_not_instrument_failure, and the
#       cat6_* corpus tests in tests/differential.rs.
# Same EffortlessMetrics/ripr#1429 untraceable class as
# ripr-suppress-test2-v1-import-scanner-predicates: the discriminator tests
# exist and assert exact values; RIPR cannot statically connect them across
# trait dispatch, private-callee, closure, or declaration boundaries.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-parser-comparison-evidence-model-untraceable"
kind = "predicate_infection_untraceable"
paths = [
    "crates/perl-parser-comparison/src/evidence.rs",
    "crates/perl-parser-comparison/src/harness.rs",
    "crates/perl-parser-comparison/src/outcomes.rs",
]
classification = ["no_static_path", "reachable_unrevealed"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #9964 rewrites the perl-parser-comparison evidence model; ripr 0.9.x/0.10.x reports ~190 diff-scoped no_static_path/reachable_unrevealed seams in evidence.rs, harness.rs, and outcomes.rs. Every executable seam is covered by exact-value or exact-error-variant discriminator tests in crates/perl-parser-comparison/tests/generic_result_model.rs (34 tests) plus the cat6_* corpus tests, but the tracer cannot connect tests across (a) non-executable const/field/variant declarations, (b) Display/Error trait dispatch via to_string()/source(), (c) private helpers called only by the public constructors under test, and (d) panic::catch_unwind closure bodies in run_v1/run_v3. This is the documented EffortlessMetrics/ripr#1429 untraceable class (see docs/learnings/2026-07-ripr-weakly-exposed-suppression-churn.md): once the discriminator exists and RIPR still cannot see it, more tests will not clear the finding. Suppression is narrow: three production files in one crate, one PR. Remove when ripr#1429 is fixed upstream or targeted mutation is scheduled per #1289."
created = "2026-08-17"
review_after = "2026-09-17"
expires = "2026-11-17"
removal_criteria = "EffortlessMetrics/ripr#1429 lets RIPR trace test activation across trait dispatch, private callees, catch_unwind closures, and declaration seams, or targeted mutation replaces this advisory path; then delete this entry and rerun the RIPR new-gap gate."

# -----------------------------------------------------------------------------
# Contributor topology observation falsifiers: xtask test-surface plumbing.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-contributor-observation-falsifiers"
kind = "test_receipt_surface"
paths = ["xtask/tests/ripr_seam_proof_contributor_observation.rs"]
classification = ["side_effect", "call_deletion"]
owner = "devex"
issue = "#9541"
reason = "The contributor-topology observation falsifiers are a diff-scoped proof harness driving every observation.rs bail! through fixture mutation; the matcher flags the harness's own rewrites and object surgery as unobserved side effects, the same test-surface limitation as ripr-suppress-seam-proof-tests (which covers only crates/**/tests). Remove when RIPR distinguishes fixture plumbing from production behavior."
created = "2026-08-18"
review_after = "2026-09-18"
expires = "2026-09-30"

# -----------------------------------------------------------------------------
# Workspace runtime-generation boundary contract tests: diff-scoped RIPR treats
# the source-grepping contract-test plumbing as a production seam.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-workspace-runtime-generation-boundary-tests"
kind = "test_receipt_surface"
paths = ["xtask/tests/workspace_runtime_generation_boundary.rs"]
classification = ["weakly_exposed", "reachable_unrevealed", "call_deletion", "return_value", "predicate"]
owner = "devex"
issue = "#10013"
reason = "The W02 candidate (#10080) hardens the runtime-generation architecture boundary tests per review: the read-gate requirement is bound to the wrapper's public method surface, the doc-hidden module check normalizes line endings, and the generation-allocation linearization point gets its own ratchet. ripr's diff-scoped tracer reports the tests' own ensure!/matches plumbing (Ok(()) markers, forbidden-token predicates, marker-string returns) as unobserved seams. These tests ARE the discriminator: each ensure! observes the workspace/runtime_generation sources directly, and mutating the guarded sources flips them. Same ripr#1428 test-surface class as ripr-suppress-agent-capability-workflow-contract-test. The production seams the boundary guards are separately covered by the runtime_generation unit tests (same-root linearization, session mint ABA) in perl-workspace. Remove when RIPR distinguishes contract-test plumbing from production seams."
created = "2026-08-19"
review_after = "2026-09-19"
expires = "2026-11-19"

# -----------------------------------------------------------------------------
# Workspace runtime ownership ledger validator: same ripr#1428 class as the
# boundary tests above, one file, one PR.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-workspace-runtime-ownership-ledger-test"
kind = "test_receipt_surface"
paths = ["xtask/tests/workspace_runtime_ownership_ledger.rs"]
classification = ["weakly_exposed", "reachable_unrevealed", "call_deletion", "return_value", "field_construction", "match_arm"]
owner = "devex"
issue = "#10011"
reason = "The ledger validator's diff-scoped gaps flagged by ripr for the W02 candidate (#10080) are the validator's own row-parsing plumbing: TSV field match arms, per-field emptiness ensures added in review repair, and the Result-returning test body. The validator observes the policy ledger and generated reviewer projection directly (duplicate ids, dispositions, live-source markers, reviewer-projection freshness), so its assertions are the discriminator for the sources it guards; the tracer cannot see test-harness plumbing as its own oracle. Same ripr#1428 test-surface class as ripr-suppress-agent-capability-workflow-contract-test. Remove when RIPR distinguishes contract-test plumbing from production seams."
created = "2026-08-19"
review_after = "2026-09-19"
expires = "2026-11-19"

# -----------------------------------------------------------------------------
# perl-semantic-facts property round-trip test surface (PR #6842 / issue
# #6820): proptest macro bodies in a newly added test file misclassified as
# new production seams (ripr#1428).
#
# The exact-head diff-scoped RIPR 0.9.0 receipt reports 162 gate-actionable
# findings for this PR, ALL of them no_static_path inside this single test
# file: prop_oneof!/proptest! macro bodies and strategy plumbing that the
# analyzer cannot trace to their covering test oracle. The production SDK the
# PR adds is not the blocker: crates/perl-semantic-facts/src/framework.rs
# carries 699 exposed findings and zero no_static_path / reachable_unrevealed
# findings — RIPR statically connects the crate's test files
# (bdd_semantic_facts.rs, framework_authority_contract.rs,
# framework_external_api.rs, prop_json_roundtrip.rs) to the SDK seams.
#
# Matcher honesty note: the repository matcher applies suppressions by path
# and ignores the documentary classification filter, so this exact-path entry
# truthfully suppresses every diff-scoped finding in this one test file (402
# total: 162 no_static_path, 17 weakly_exposed, 139 exposed, 78
# static_unknown, 4 propagation_unknown, 2 infection_unknown), not only the
# 162 actionable ones. Production findings suppressed are exactly 0; the
# post-suppression actionable count (reachable_unrevealed + no_static_path)
# is 0 and the new-gap quality gate passes.
# -----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-semantic-facts-prop-roundtrip-test-surface"
kind = "test_receipt_surface"
paths = ["crates/perl-semantic-facts/tests/prop_json_roundtrip.rs"]
classification = ["no_static_path"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1428"
reason = "PR #6842 (issue #6820) adds prop_json_roundtrip.rs, a newly added proptest round-trip contract for the framework-adapter SDK wire fields. Exact-head RIPR 0.9.0 reports all 162 gate-actionable findings for the PR as no_static_path inside this test-only file — prop_oneof!/proptest! macro bodies and strategy plumbing the analyzer cannot trace to their covering test oracle, the ripr#1428 test-file class of ripr-suppress-heredoc-queue-contract-test-surface and ripr-suppress-zed-host-compat-test-support. The production SDK is separately proven: crates/perl-semantic-facts/src/framework.rs has 699 exposed findings and zero no_static_path / reachable_unrevealed findings, with static paths through the crate's four test files. The matcher is path-scoped, so this entry truthfully suppresses all 402 diff-scoped findings in this single test file; production findings suppressed are 0 and the post-suppression actionable count is 0. It changes no production path, glob, RIPR parser, or severity threshold."
created = "2026-08-20"
review_after = "2026-09-20"
expires = "2026-09-30"
removal_criteria = "EffortlessMetrics/ripr#1428 is fixed so proptest macro bodies in newly added test files are not treated as production seams, OR the new-gap gate stops counting diff-scoped test-file seams as blocking, OR an exact-head RIPR artifact no longer reports actionable gaps in this file; then delete or narrow this entry and rerun the RIPR new-gap gate."

# ----------------------------------------------------------------------------
# Parser-comparison evidence schema branches: the integration schema receipt
# covers every constructor variant and rejects cross-field false greens, but
# ripr cannot connect that separate harness to the changed production owner.
# ----------------------------------------------------------------------------
[[suppress]]
id = "ripr-suppress-parser-comparison-evidence-payload-schema"
kind = "predicate_infection_untraceable"
paths = ["crates/perl-parser-comparison/src/evidence_payload.rs"]
classification = ["no_static_path", "weakly_exposed"]
owner = "proof-lane"
issue = "EffortlessMetrics/ripr#1429"
reason = "PR #10002 (issue #8432) changes the terminal-state one_of branches in parser_comparison_evidence_schema_json. The exact-head enforce-new-ripr receipt currently contains only no_static_path and weakly_exposed findings in this production owner because ripr cannot connect the separate integration harness to the changed schema builder, the documented EffortlessMetrics/ripr#1429 limitation. The focused test crates/perl-parser-comparison/tests/comparison_evidence_schema.rs materially covers the module: machine_schema_accepts_every_constructor_payload_variant validates completed and failed execution, all six observation dispositions, matching/mismatch/non-decisive conformance, while schema_rejects_cross_field_false_greens_and_wrong_nested_reference_kinds and the existing schema tests assert invalid combinations and nested authority rejection. The repository suppression matcher is classification-qualified, so this entry admits only no_static_path and weakly_exposed; its legacy weakly_gripped mapping is treated as reachable_unrevealed and is rejected here. The exact candidate receipt currently contains only those two reported classes; any future receipt with a different class, especially reachable_unrevealed or an actual candidate-specific seam, is an automatic do-not-merge condition requiring removal or narrowing of this entry or a separate evidence-backed review."
created = "2026-08-21"
review_after = "2026-09-21"
expires = "2026-11-21"
removal_criteria = "EffortlessMetrics/ripr#1429 is fixed or ripr can connect comparison_evidence_schema.rs to evidence_payload.rs; then delete or narrow this entry and rerun the enforce-new-ripr gate. Remove or narrow it immediately if any future receipt reports a class other than no_static_path or weakly_exposed, especially reachable_unrevealed or an actual candidate-specific seam; that condition is automatic do-not-merge unless a separate evidence-backed review justifies the disposition."
