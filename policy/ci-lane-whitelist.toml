# CI Lane Whitelist
#
# Governed map of every CI lane to its proof obligation, owner, trigger,
# cost estimate, and evidence artifact. This is the missing inventory layer
# between docs and the planner: not just "CI runs," but "CI has a reason,
# an owner, a cost, a proof obligation, and a review date."
#
# See docs/ci/perl-lsp-rollout-plan.md (PR 02 entry) for the rationale.
#
# Lifecycle:
#   unknown → whitelisted → measured → learned-estimate → routed → enforced
#
# Do not route or enforce a lane until it has been whitelisted and measured.
#
# Source-of-truth boundaries:
#   - LEM bands and runner multipliers live in policy/ci-budget.toml. They are
#     NOT duplicated here; the policy linter resolves them from ci-budget.toml.
#   - Lane IDs use underscores to match policy/ci-lanes.toml lane keys (so the
#     planner and linter can cross-reference by lane.id == lane.<id>).

schema_version = 1
policy = "ci-lane-whitelist"
owner = "EffortlessMetrics"
status = "advisory"
updated = "2026-05-20"

# =============================================================================
# Swarm routed Rust small lanes
# =============================================================================

[[lane]]
id = "swarm_rust_small_route"
workflow = ".github/workflows/em-ci-routed-rust.yml"
job = "route-rust-small"
tier = "frontdoor"
kind = "control"
blocking = true
default_pr = true
runner = "ubuntu_latest"
base_lem = 1
owner = "ci"
intent = "Route trusted same-repo Rust Small PRs to CX53, CX43, or GitHub-hosted fallback without exposing self-hosted runners to fork PRs."
failure_mode = "Swarm PRs either do not route to available trusted capacity or expose self-hosted capacity to untrusted forks."
proof_obligation = "Emit router_target, router_reason, and router_error; fork PRs must select GitHub-hosted fallback."
evidence = ["job output", "step-summary"]
allowed_triggers = ["pull_request", "merge_group", "workflow_dispatch"]
expensive = false
duplicate_of = []
review_after = "2026-06-20"
expires = "2026-11-20"

[[lane]]
id = "swarm_rust_small_cx53"
workflow = ".github/workflows/em-ci-routed-rust.yml"
job = "rust-small-cx53"
tier = "merge_gate"
kind = "rust"
blocking = false
default_pr = true
runner = "self_hosted_cx53"
base_lem = 12
owner = "ci"
intent = "Run the Perl LSP Rust Small lane on the primary trusted CX53 runner when available."
failure_mode = "Primary same-repo PR runner path does not prove the small Rust lane."
proof_obligation = "Run cargo check --workspace --locked, parser smoke, and LSP smoke in the routed self-hosted lane."
evidence = ["job output", "Perl LSP Rust Small Result"]
allowed_triggers = ["pull_request", "merge_group", "workflow_dispatch"]
expensive = true
expensive_reason = "Workspace check compiles the full seeded source tree."
duplicate_of = []
review_after = "2026-06-20"
expires = "2026-11-20"

[[lane]]
id = "swarm_rust_small_cx43"
workflow = ".github/workflows/em-ci-routed-rust.yml"
job = "rust-small-cx43"
tier = "merge_gate"
kind = "rust"
blocking = false
default_pr = true
runner = "self_hosted_cx43"
base_lem = 18
owner = "ci"
intent = "Run the Perl LSP Rust Small lane on the trusted CX43 backfill runner when CX53 is unavailable or forced."
failure_mode = "Backfill same-repo PR runner path does not prove the small Rust lane."
proof_obligation = "Run cargo check --workspace --locked, parser smoke, and LSP smoke in the routed self-hosted lane."
evidence = ["job output", "Perl LSP Rust Small Result"]
allowed_triggers = ["pull_request", "merge_group", "workflow_dispatch"]
expensive = true
expensive_reason = "Workspace check compiles the full seeded source tree on a smaller runner."
duplicate_of = []
review_after = "2026-06-20"
expires = "2026-11-20"

[[lane]]
id = "swarm_rust_small_github"
workflow = ".github/workflows/em-ci-routed-rust.yml"
job = "rust-small-github"
tier = "merge_gate"
kind = "rust"
blocking = false
default_pr = true
runner = "ubuntu_latest"
base_lem = 24
owner = "ci"
intent = "Run the Perl LSP Rust Small lane on GitHub-hosted fallback for forks, missing runner token, or no idle trusted runner."
failure_mode = "Forks or runner outages lack a safe small Rust proof path."
proof_obligation = "Run cargo check --workspace --locked, parser smoke, and LSP smoke on GitHub-hosted Linux."
evidence = ["job output", "Perl LSP Rust Small Result"]
allowed_triggers = ["pull_request", "merge_group", "workflow_dispatch"]
expensive = true
expensive_reason = "GitHub-hosted fallback compiles the full seeded source tree without self-hosted cache guarantees."
duplicate_of = []
review_after = "2026-06-20"
expires = "2026-11-20"

[[lane]]
id = "swarm_rust_small_result"
workflow = ".github/workflows/em-ci-routed-rust.yml"
job = "rust-small-result"
tier = "merge_gate"
kind = "control"
blocking = true
default_pr = true
runner = "ubuntu_latest"
base_lem = 1
owner = "ci"
intent = "Normalize conditional runner implementation jobs into the single branch-protection status."
failure_mode = "Branch protection accidentally requires skipped implementation jobs instead of a stable result check."
proof_obligation = "Require the selected target to pass and all unselected implementation jobs to skip."
evidence = ["job output", "step-summary"]
allowed_triggers = ["pull_request", "merge_group", "workflow_dispatch"]
expensive = false
duplicate_of = []
review_after = "2026-06-20"
expires = "2026-11-20"

# =============================================================================
# Frontdoor / control lanes
# =============================================================================

[[lane]]
id = "pr_plan"
workflow = ".github/workflows/pr-plan.yml"
job = "plan"
tier = "frontdoor"
kind = "visibility"
blocking = false
default_pr = true
runner = "self_hosted_workflow_nano"
base_lem = 1
owner = "ci"
intent = "Forecast PR CI cost and selected verification lanes."
failure_mode = "Spending CI without a cost forecast or lane rationale."
proof_obligation = "Compute changed files, classify risk packs, sum lane LEM, emit ci-plan.json."
evidence = ["target/ci/ci-plan.json", "step-summary"]
allowed_triggers = ["pull_request", "workflow_dispatch"]
expensive = false
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "draft_guard"
workflow = ".github/workflows/ci.yml"
job = "draft-pr-check"
tier = "frontdoor"
kind = "control"
blocking = true
default_pr = true
runner = "ubuntu_24_04"
base_lem = 1
owner = "ci"
intent = "Avoid running full CI on draft PRs."
failure_mode = "Draft or non-credible candidates consuming CI."
proof_obligation = "Classify draft status and skip downstream CI when appropriate."
evidence = ["job output"]
allowed_triggers = ["pull_request", "push", "merge_group", "workflow_dispatch"]
expensive = false
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "preflight_latest"
workflow = ".github/workflows/ci.yml"
job = "preflight-latest-check"
tier = "frontdoor"
kind = "control"
blocking = true
default_pr = true
runner = "ubuntu_24_04"
base_lem = 1
owner = "ci"
intent = "Skip superseded PR SHAs."
failure_mode = "Spending CI on commits that are no longer current."
proof_obligation = "Compare current SHA to latest branch SHA."
evidence = ["job output"]
allowed_triggers = ["pull_request", "push", "merge_group"]
expensive = false
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "conflict_markers"
workflow = ".github/workflows/ci.yml"
job = "conflict-markers"
tier = "frontdoor"
kind = "hygiene"
blocking = true
default_pr = true
runner = "ubuntu_24_04"
base_lem = 1
owner = "ci"
intent = "Prevent merge conflict markers from reaching master."
failure_mode = "Committed conflict markers break builds or user workflows."
proof_obligation = "Search tracked files for conflict marker patterns."
evidence = ["job log"]
allowed_triggers = ["pull_request", "push", "merge_group"]
expensive = false
duplicate_of = ["gate:check_conflict_markers"]
review_after = "2026-06-07"
expires = "2026-11-07"

# =============================================================================
# Default Rust gates
# =============================================================================

[[lane]]
id = "pr_smoke"
workflow = ".github/workflows/ci.yml"
job = "pr-smoke"
tier = "frontdoor"
kind = "rust"
blocking = true
default_pr = true
runner = "ubuntu_24_04"
base_lem = 4
owner = "ci"
intent = "Run fast scoped Rust proof through xtask gates."
failure_mode = "Obvious compile, format, lint, or scoped unit-test failure."
proof_obligation = "Run pr-fast gates selected by ci-scope."
evidence = [
  "target/receipts/receipt.json",
  "target/test-results/pr-fast-junit.xml",
]
allowed_triggers = ["pull_request", "push", "merge_group", "workflow_dispatch"]
expensive = false
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "merge_gate_shards"
workflow = ".github/workflows/ci.yml"
job = "merge-gate-shards"
tier = "merge_gate"
kind = "rust"
blocking = true
default_pr = true
runner = "ubuntu_24_04"
base_lem = 24
owner = "ci"
intent = "Run bounded required merge-gate shards."
failure_mode = "Defect not caught by scoped PR smoke but required before merge."
proof_obligation = "Run required gate groups from .ci/gate-policy.yaml and emit receipts."
evidence = [
  "target/receipts/shards/*.json",
  "target/test-results/gate-shard-*-junit.xml",
]
allowed_triggers = ["pull_request", "push", "merge_group", "workflow_dispatch"]
expensive = true
expensive_reason = "Multiple shard jobs compile and test distinct workspace surfaces."
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "merge_gate_aggregate"
workflow = ".github/workflows/ci.yml"
job = "merge-gate"
tier = "merge_gate"
kind = "control"
blocking = true
default_pr = true
runner = "ubuntu_24_04"
base_lem = 1
owner = "ci"
intent = "Aggregate shard results into the required merge-blocking status."
failure_mode = "Branch protection cannot rely on a stable summary check."
proof_obligation = "Reduce shard outcomes to a single required status."
evidence = ["job output"]
allowed_triggers = ["pull_request", "push", "merge_group"]
expensive = false
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "check_all_targets"
workflow = ".github/workflows/ci.yml"
job = "check-all-targets"
tier = "merge_gate"
kind = "rust"
blocking = true
default_pr = true
runner = "ubuntu_24_04"
base_lem = 6
owner = "ci"
intent = "Compile all targets to catch integration-test and bench bit-rot."
failure_mode = "Default gates pass while integration tests or benches fail to compile."
proof_obligation = "cargo check --workspace --all-targets."
evidence = ["job output"]
allowed_triggers = ["pull_request", "push", "merge_group"]
expensive = false
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "docs_gate"
workflow = ".github/workflows/ci.yml"
job = "docs-gate"
tier = "frontdoor"
kind = "docs"
blocking = true
default_pr = true
runner = "ubuntu_24_04"
base_lem = 3
owner = "docs"
intent = "Validate docs-only changes without compiling Rust."
failure_mode = "Docs build break lands without compile."
proof_obligation = "Markdown / mkdocs validation on docs-only diffs."
evidence = ["job output"]
allowed_triggers = ["pull_request"]
expensive = false
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

# =============================================================================
# UX / memory / platform default lanes
# =============================================================================

[[lane]]
id = "ux_tests"
workflow = ".github/workflows/ci.yml"
job = "ux-tests"
tier = "merge_gate"
kind = "ux"
blocking = true
default_pr = true
runner = "ubuntu_24_04"
base_lem = 8
owner = "lsp"
intent = "Prevent first-five-minutes LSP UX regressions."
failure_mode = "User-visible LSP behavior regresses while compile/unit tests stay green."
proof_obligation = "Run UX scenario suite against live binary and classify failures."
evidence = [
  "target/receipts/ux-regression.json",
  "target/receipts/ux-regression.log",
  "target/test-results/ux-regression-junit.xml",
]
allowed_triggers = ["pull_request", "push", "merge_group"]
expensive = true
expensive_reason = "Builds binary and runs integration UX harness."
default_pr_exception = "ci-exception-ux-regression-default-pr"
duplicate_of = ["workflow:ux-regression-gate"]
review_after = "2026-06-07"
expires = "2026-08-07"

[[lane]]
id = "lsp_memory_smoke"
workflow = ".github/workflows/ci.yml"
job = "lsp-memory-smoke"
tier = "merge_gate"
kind = "memory"
blocking = true
default_pr = true
runner = "ubuntu_24_04"
base_lem = 8
owner = "runtime"
intent = "Catch retained-state regressions in LSP document churn."
failure_mode = "Open/change/close churn retains document/session/cache state."
proof_obligation = "Run bounded document-churn workload and validate RSS plateau receipt."
evidence = [
  "target/memory/receipts/*.json",
  "target/memory/*.plateau.json",
  "target/memory/*.csv",
]
allowed_triggers = ["pull_request", "push", "merge_group"]
expensive = true
expensive_reason = "Builds release binary and runs process-level memory workload."
default_pr_exception = "ci-exception-lsp-memory-smoke-default-pr"
duplicate_of = ["nightly:memory_plateau"]
review_after = "2026-06-07"
expires = "2026-08-07"

[[lane]]
id = "windows_guardrails"
workflow = ".github/workflows/ci.yml"
job = "windows-guardrails"
tier = "merge_gate"
kind = "platform"
blocking = true
default_pr = true
runner = "windows_latest"
# 10 wall-clock minutes × 2.0 windows multiplier = 20 LEM
base_lem = 20
owner = "platform"
intent = "Catch Windows path, module separator, and sandbox regressions."
failure_mode = "Windows-specific behavior fails while Linux CI passes."
proof_obligation = "Compile key crates and run path/sandbox regression lanes on Windows."
evidence = ["job logs"]
allowed_triggers = ["pull_request", "push", "merge_group"]
expensive = true
expensive_reason = "Windows runner multiplier and separate compile surface."
default_pr_exception = "ci-exception-windows-guardrails-default-pr"
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-08-07"

# =============================================================================
# Oracle-gap lane (advisory)
# =============================================================================

[[lane]]
id = "ripr_advisory"
workflow = ".github/workflows/ripr.yml"
job = "ripr"
tier = "frontdoor"
kind = "oracle_gap"
blocking = false
default_pr = true
runner = "ubuntu_24_04"
base_lem = 4
owner = "testing"
intent = "Find static oracle-gap evidence for changed Rust behavior."
failure_mode = "Changed behavior is reachable but tests do not appear to discriminate it."
proof_obligation = "Run static RIPR exposure analysis and advisory PR evidence routing on production Rust diffs."
evidence = [
  "target/ripr/pr/repo-exposure.json",
  "target/ripr/pr/summary.md",
  "target/ripr/review/comments.json",
  "target/ripr/review/annotations.txt",
  "target/xtask/impacted-evidence/latest.json",
]
allowed_triggers = ["pull_request", "merge_group", "workflow_dispatch"]
labels = ["ripr", "full-ci"]
expensive = false
blocking_promotion_allowed = false
duplicate_of = ["nightly:mutation"]
review_after = "2026-06-07"
expires = "2026-08-07"

# =============================================================================
# Nightly / branch-gated deep lanes
# =============================================================================

[[lane]]
id = "mutation"
workflow = ".github/workflows/ci-nightly.yml"
job = "mutation"
tier = "nightly"
kind = "mutation"
blocking = false
default_pr = false
runner = "ubuntu_24_04"
base_lem = 60
owner = "testing"
intent = "Execution-backed test adequacy calibration."
failure_mode = "Tests execute code but fail to reject concrete behavioral mutants."
proof_obligation = "Run cargo-mutants on bounded package subset."
evidence = ["mutants.out/mutants.json"]
allowed_triggers = ["schedule", "workflow_dispatch", "pull_request:labeled"]
labels = ["ci:mutation", "mutation", "full-ci"]
expensive = true
expensive_reason = "Runs concrete mutants and executes tests repeatedly."
duplicate_of = ["ripr_advisory"]
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "coverage"
workflow = ".github/workflows/ci-nightly.yml"
job = "test-coverage"
tier = "nightly"
kind = "coverage"
blocking = false
default_pr = false
runner = "ubuntu_24_04"
base_lem = 45
owner = "testing"
intent = "Measure execution surface; also provides the required Codecov / Patch 95 check on merge_group runs."
failure_mode = "Coverage drops or important code becomes unexecuted."
proof_obligation = "Run cargo-llvm-cov and upload report."
evidence = ["lcov.info", "Codecov"]
allowed_triggers = ["schedule", "workflow_dispatch", "pull_request:labeled", "merge_group", "pull_request"]
labels = ["ci:coverage", "coverage", "full-ci"]
expensive = true
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "fuzz"
workflow = ".github/workflows/ci-nightly.yml"
job = "fuzz"
tier = "nightly"
kind = "fuzz"
blocking = false
default_pr = false
runner = "ubuntu_24_04"
base_lem = 60
owner = "testing"
intent = "Bounded nightly fuzz sweep across registered targets."
failure_mode = "Crashing inputs or timeouts not caught by deterministic tests."
proof_obligation = "Run cargo-fuzz with bounded budget per target."
evidence = ["fuzz/artifacts/**"]
allowed_triggers = ["schedule", "workflow_dispatch"]
labels = ["ci:fuzz", "full-ci"]
expensive = true
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "real_repo_latency"
workflow = ".github/workflows/ci-nightly.yml"
job = "real-repo-latency"
tier = "nightly"
kind = "performance"
blocking = false
default_pr = false
runner = "ubuntu_24_04"
base_lem = 30
owner = "lsp"
intent = "Real project latency and performance scenarios."
failure_mode = "User-visible LSP latency regression on real repos."
proof_obligation = "Run real-repo latency harness and emit metrics."
evidence = ["target/receipts/real-repo-latency.json"]
allowed_triggers = ["schedule", "workflow_dispatch", "pull_request:labeled"]
labels = ["ci:bench", "ci:real-repo-latency", "full-ci"]
expensive = true
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "memory_plateau"
workflow = ".github/workflows/ci-nightly.yml"
job = "memory-plateau"
tier = "nightly"
kind = "memory"
blocking = false
default_pr = false
runner = "ubuntu_24_04"
base_lem = 35
owner = "runtime"
intent = "Full LSP memory plateau workloads (deeper than memory smoke)."
failure_mode = "Retained-state regressions only visible at longer plateau workloads."
proof_obligation = "Run plateau workload, validate RSS trend over time."
evidence = ["target/memory/plateau/**"]
allowed_triggers = ["schedule", "workflow_dispatch", "pull_request:labeled"]
labels = ["ci:memory", "full-ci"]
expensive = true
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "release_check"
workflow = ".github/workflows/publish-dry-run.yml"
job = "publish-dry-run"
tier = "release"
kind = "release"
blocking = false
default_pr = false
runner = "ubuntu_24_04"
base_lem = 15
owner = "release"
intent = "Package, publish dry-run, semver/API checks."
failure_mode = "Release publishability regressions land before release tag."
proof_obligation = "Run cargo package + publish dry-run + semver checks."
evidence = ["target/package/**"]
allowed_triggers = ["pull_request", "workflow_dispatch"]
labels = ["release-check", "ci:inline-completion", "full-ci"]
expensive = true
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "perl_version_matrix"
workflow = ".github/workflows/perl-version-matrix.yml"
job = "perl-matrix"
tier = "nightly"
kind = "platform"
blocking = false
default_pr = false
runner = "ubuntu_24_04"
base_lem = 40
owner = "platform"
intent = "Perl runtime compatibility from 5.8 through 5.40."
failure_mode = "Behavior regressions on older or newer Perl versions."
proof_obligation = "Run Perl-version matrix smoke."
evidence = ["job logs"]
allowed_triggers = ["schedule", "workflow_dispatch", "pull_request:labeled"]
labels = ["ci:perl-matrix", "full-ci"]
expensive = true
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "vscode_smoke_matrix"
workflow = ".github/workflows/vscode-managed-binary-smoke.yml"
job = "smoke"
tier = "nightly"
kind = "platform"
blocking = false
default_pr = false
runner = "mixed"
# Pre-summed across Linux + Windows + macOS legs.
base_lem = 35
owner = "vscode"
intent = "VS Code extension-host smoke across OSes."
failure_mode = "Extension regressions on Windows or macOS while Linux passes."
proof_obligation = "Run VS Code managed-binary smoke matrix."
evidence = ["job logs"]
allowed_triggers = ["schedule", "workflow_dispatch", "pull_request:labeled"]
labels = ["ci:vscode-matrix", "full-ci"]
expensive = true
expensive_reason = "Three-OS matrix with extension-host startup."
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "security_audit"
workflow = ".github/workflows/ci-security.yml"
job = "security"
tier = "nightly"
kind = "security"
blocking = false
default_pr = false
runner = "ubuntu_24_04"
base_lem = 15
owner = "security"
intent = "Cargo audit, cargo deny, and Trivy scan."
failure_mode = "Dependency vulnerabilities or supply-chain regressions."
proof_obligation = "Run cargo-audit, cargo-deny, Trivy."
evidence = ["audit reports", "deny reports", "Trivy SARIF"]
allowed_triggers = ["schedule", "workflow_dispatch", "pull_request"]
labels = ["security-audit", "ci:security", "full-ci"]
expensive = true
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

# =============================================================================
# Automation / external review
# =============================================================================

[[lane]]
id = "droid_auto_review"
workflow = ".github/workflows/droid-review.yml"
job = "droid-review"
tier = "frontdoor"
kind = "automation"
blocking = false
default_pr = true
runner = "self_hosted_droid_review"
base_lem = 4
owner = "ci"
intent = "External AI review on credible PRs."
failure_mode = "Review feedback gap on agent-generated PRs."
proof_obligation = "Run external AI reviewer and post comments."
evidence = ["PR comments"]
allowed_triggers = ["pull_request"]
labels = ["ai-review", "full-ci"]
expensive = false
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-08-07"

[[lane]]
id = "droid_manual_review"
workflow = ".github/workflows/droid.yml"
job = "droid"
tier = "on-demand"
kind = "automation"
blocking = false
default_pr = false
runner = "self_hosted_droid_review"
base_lem = 2
owner = "ci"
intent = "Manual @droid review triggered by trusted contributors."
failure_mode = "On-demand review not available for trusted actors."
proof_obligation = "Execute @droid commands from OWNER/MEMBER/COLLABORATOR."
evidence = ["PR comments"]
allowed_triggers = ["issue_comment", "pull_request_review_comment", "pull_request_review"]
expensive = false
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-08-07"

[[lane]]
id = "droid_security_scan"
workflow = ".github/workflows/droid-security-scan.yml"
job = "paused"
tier = "on-demand"
kind = "control"
blocking = false
default_pr = false
runner = "ubuntu_24_04"
base_lem = 1
owner = "ci"
intent = "Record the explicit pause of automatic secret-bearing Droid security execution."
failure_mode = "The paused security boundary is accidentally restored without the #6098 execution and publisher proof."
proof_obligation = "Run the read-only boundary contract and emit the truthful pause summary."
evidence = ["step-summary"]
allowed_triggers = ["workflow_dispatch"]
expensive = false
duplicate_of = []
review_after = "2026-08-08"
expires = "2026-11-08"

[[lane]]
id = "workflow_security_ratchet"
workflow = ".github/workflows/workflow-security-ratchet.yml"
job = "trusted-base-ratchet"
tier = "frontdoor"
kind = "hygiene"
blocking = false
default_pr = false
runner = "ubuntu_24_04"
base_lem = 1
owner = "ci"
intent = "Reject new workflow/composite-action security findings against the trusted-base baseline."
failure_mode = "Workflow and composite-action security debt regrows unnoticed between remediations."
proof_obligation = "Run the base-branch evaluator and baseline against candidate files without executing candidate code."
evidence = ["target/receipts/workflow-security-ratchet.json"]
allowed_triggers = ["pull_request_target", "push", "workflow_dispatch"]
expensive = false
duplicate_of = []
review_after = "2026-11-08"
expires = "2027-02-08"

[[lane]]
id = "workflow_security_ratchet_selftest"
workflow = ".github/workflows/workflow-security-ratchet-selftest.yml"
job = "candidate-selftest"
tier = "frontdoor"
kind = "hygiene"
blocking = false
default_pr = false
runner = "ubuntu_24_04"
base_lem = 1
owner = "ci"
intent = "Give a scanner-touching PR fast advisory feedback from its own scanner and baseline."
failure_mode = "A candidate scanner or baseline change is only discovered by the authoritative lane after merge."
proof_obligation = "Run the candidate unit tests and exact-validate the candidate baseline in the unprivileged pull_request context."
evidence = ["target/receipts/workflow-security-candidate.json"]
allowed_triggers = ["pull_request"]
expensive = false
duplicate_of = []
review_after = "2026-11-08"
expires = "2027-02-08"

[[lane]]
id = "methodology_gate"
workflow = ".github/workflows/methodology-gate.yml"
job = "methodology"
tier = "frontdoor"
kind = "hygiene"
blocking = true
default_pr = true
runner = "ubuntu_24_04"
base_lem = 2
owner = "ci"
intent = "Enforce methodology and PR-shape rules."
failure_mode = "PRs land without required methodology evidence."
proof_obligation = "Lint PR title, body, and metadata."
evidence = ["job output"]
allowed_triggers = ["pull_request"]
expensive = false
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "agent_capability_gate"
workflow = ".github/workflows/agent-capability-gate.yml"
job = "route-agent-capability-gate"
tier = "frontdoor"
kind = "hygiene"
blocking = false
default_pr = false
runner = "mixed"
base_lem = 2
owner = "ci"
intent = "Enforce M4b: review/audit agents are mechanically read-only (#3763), routed to trusted workflow-nano capacity with hosted fallback."
failure_mode = "A review/audit agent regains write tools and can mutate source/git/GitHub."
proof_obligation = "Emit an explicit group-aware runner route, then parse .claude/agents/*.md; fail on any write tool in a review/audit allowlist."
evidence = ["router job output", "job output"]
allowed_triggers = ["pull_request", "merge_group", "push"]
expensive = false
duplicate_of = []
review_after = "2026-07-10"
expires = "2026-12-31"

[[lane]]
id = "pr_title_check"
workflow = ".github/workflows/pr-title-check.yml"
job = "validate-title"
tier = "frontdoor"
kind = "hygiene"
blocking = true
default_pr = true
runner = "self_hosted_workflow_nano"
base_lem = 1
owner = "ci"
intent = "Enforce PR title convention with real issue ref."
failure_mode = "PRs with placeholder issue refs land in master."
proof_obligation = "Validate title format ends with (#NNNN) where NNNN is a real issue."
evidence = ["job output"]
allowed_triggers = ["pull_request"]
expensive = false
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

# =============================================================================
# Lanes added by follow-up B (closing PR 11 LANE_WHITELIST_MISSING warnings)
# =============================================================================

[[lane]]
id = "droid_command"
workflow = ".github/workflows/droid.yml"
job = "droid"
tier = "frontdoor"
kind = "automation"
blocking = false
default_pr = false
runner = "self_hosted_droid_review"
base_lem = 4
owner = "ci"
intent = "Manual / dispatch Droid command (separate from droid_auto_review)."
failure_mode = "External AI command path unavailable."
proof_obligation = "Run external AI action on workflow_dispatch."
evidence = ["job logs"]
allowed_triggers = ["workflow_dispatch"]
labels = ["ai-review"]
expensive = false
duplicate_of = ["droid_auto_review"]
review_after = "2026-06-07"
expires = "2026-08-07"

[[lane]]
id = "flake_detection"
workflow = ".github/workflows/flake-detection.yml"
job = "flake-detect"
tier = "nightly"
kind = "automation"
blocking = false
default_pr = false
runner = "ubuntu_24_04"
base_lem = 4
owner = "ci"
intent = "Detect flaky tests by re-running test history."
failure_mode = "Flaky tests not surfaced; CI reliability erodes."
proof_obligation = "Run flake detection sweep and emit report."
evidence = ["flake-report.json"]
allowed_triggers = ["pull_request", "schedule", "workflow_dispatch"]
labels = ["ci:flake"]
expensive = false
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "aggregate_ci_lane_history"
workflow = ".github/workflows/aggregate-ci-lane-history.yml"
job = "aggregate"
tier = "nightly"
kind = "visibility"
blocking = false
default_pr = false
runner = "ubuntu_24_04"
base_lem = 2
owner = "ci"
intent = "Aggregate recent ci-actuals artifacts into learned lane-history estimates."
failure_mode = "Planner keeps using static LEM floors or stale learned lane estimates."
proof_obligation = "Download recent ci-actuals artifacts and run scripts/ci/aggregate_lane_history.py."
evidence = [".ci/metrics/ci-lane-history.json", "ci-lane-history-* artifact"]
allowed_triggers = ["schedule", "workflow_dispatch"]
expensive = false
duplicate_of = []
review_after = "2026-06-11"
expires = "2026-11-11"

[[lane]]
id = "badge_endpoints"
workflow = ".github/workflows/badge-endpoints.yml"
job = "refresh"
tier = "frontdoor"
kind = "automation"
blocking = false
default_pr = false
runner = "ubuntu_latest"
base_lem = 3
owner = "ci"
intent = "Regenerate repository-scoped public Shields endpoint JSON for README badges."
failure_mode = "Public badge endpoint JSON drifts from the checked badge generator."
proof_obligation = "Run cargo xtask badges and open a refresh PR only when badge JSON changes."
evidence = ["badges/*.json", "refresh PR diff"]
allowed_triggers = ["push", "workflow_dispatch"]
expensive = false
duplicate_of = []
review_after = "2026-06-13"
expires = "2026-11-13"

[[lane]]
id = "ux_regression_gate_path_filtered"
workflow = ".github/workflows/ux-regression-gate.yml"
job = "ux-regression-gate"
tier = "merge_gate"
kind = "ux"
blocking = true
default_pr = false
runner = "ubuntu_24_04"
base_lem = 8
owner = "lsp"
intent = "Path-filtered UX regression gate for LSP/DAP/extension changes."
failure_mode = "UX regression on LSP/DAP/extension paths slips through."
proof_obligation = "Run UX regression suite when paths match."
evidence = ["target/receipts/ux-regression.json"]
allowed_triggers = ["pull_request", "workflow_dispatch"]
expensive = true
expensive_reason = "Builds binary and runs UX harness."
duplicate_of = ["ux_tests"]
review_after = "2026-06-07"
# Decision deadline aligned with PR 17 — see docs/ci/ux-ownership.md.
expires = "2026-08-07"

[[lane]]
id = "workflow_policy_lint"
workflow = ".github/workflows/workflow-policy.yml"
job = "workflow-policy-lint"
tier = "frontdoor"
kind = "hygiene"
blocking = true
default_pr = true
runner = "ubuntu_24_04"
base_lem = 2
owner = "ci"
intent = "Lint workflows against policy (PR_TARGET_CHECKOUT_HEAD, WRITE_ALL_PERMISSIONS, BLANKET_CANCEL_IN_PROGRESS, LANE_WHITELIST_MISSING, etc)."
failure_mode = "Unsafe workflow patterns land in .github/workflows/."
proof_obligation = "Run cargo xtask workflow-policy-lint and emit receipt."
evidence = ["target/receipts/workflow-policy.json"]
allowed_triggers = ["pull_request", "workflow_dispatch"]
expensive = false
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "lsp_318_claim_guard"
workflow = ".github/workflows/lsp-318-claim-guard.yml"
job = "claim-guard"
tier = "frontdoor"
kind = "lsp"
blocking = true
default_pr = false
runner = "ubuntu_24_04"
base_lem = 3
owner = "lsp"
intent = "Run the selected LSP 3.18 claim guard and generated matrix check when protocol claim surfaces change."
failure_mode = "Unsupported or undocumented LSP 3.18 claims land without the mechanical guard or stale matrix being checked."
proof_obligation = "Run cargo xtask check-lsp-318-claims and cargo xtask generate-lsp-318-matrix --check."
evidence = ["job output"]
allowed_triggers = ["pull_request", "push", "workflow_dispatch"]
expensive = false
duplicate_of = []
review_after = "2026-06-28"
expires = "2026-11-28"

[[lane]]
id = "policy_validators"
workflow = ".github/workflows/policy-validators.yml"
job = "validate"
tier = "frontdoor"
kind = "hygiene"
blocking = true
default_pr = false
runner = "self_hosted_workflow_nano"
base_lem = 2
owner = "ci"
intent = "Validate CI policy ledgers when policy/, gate-policy, or validator scripts change."
failure_mode = "Risk packs reference unknown lanes, gates have no lane mapping, bounded-result overflow invariants break, or TOML files break."
proof_obligation = "Run validate_risk_packs.py, validate_gate_lane_mapping.py, validate_trust_lanes.py, check_bounded_result_overflow.py, and policy/*.toml parse-checks in --strict mode."
evidence = ["job logs"]
allowed_triggers = ["pull_request", "workflow_dispatch"]
expensive = false
duplicate_of = []
review_after = "2026-06-07"
expires = "2026-11-07"

[[lane]]
id = "commit_checks"
workflow = "cargo xtask gates --tier commit"
job = "(local)"
tier = "commit"
kind = "hygiene"
blocking = false
default_pr = false
runner = "local"
base_lem = 0
owner = "ci"
intent = "Local staged-tree hygiene before git commit."
failure_mode = "Conflict markers, oversized staged blobs, or policy violations reach remote CI."
proof_obligation = "Run cargo xtask gates --tier commit --staged; each gate in .ci/gate-policy.yaml commit tier must pass or advisory-report."
evidence = ["AgentReceipt JSON from xtask gates", "local hook logs"]
allowed_triggers = ["pre_commit", "manual"]
expensive = false
duplicate_of = []
review_after = "2026-07-24"
expires = "2026-12-24"

# =============================================================================
# UB / native-boundary advisory review lanes (PR 1 advisory scaffold)
# =============================================================================

[[lane]]
id = "ub_review_router"
workflow = ".github/workflows/ub-review.yml"
job = "route-ub-review"
tier = "frontdoor"
kind = "control"
blocking = false
default_pr = true
runner = "ubuntu_latest"
base_lem = 1
owner = "ci"
intent = "Route same-repo UB review PRs to CX53, CX43, or GitHub-hosted fallback without exposing self-hosted runners to fork PRs."
failure_mode = "PRs either do not route to available trusted capacity or expose self-hosted capacity to untrusted forks."
proof_obligation = "Emit target, reason, and fallback_allowed; fork PRs must be gated by the fork-policy job."
evidence = ["job output", "step-summary"]
allowed_triggers = ["pull_request"]
expensive = false
duplicate_of = []
review_after = "2026-09-06"
expires = "2026-12-06"

[[lane]]
id = "ub_review_advisory"
workflow = ".github/workflows/ub-review.yml"
job = "ub-review-cx53"
tier = "frontdoor"
kind = "ub_review"
blocking = false
default_pr = true
runner = "self_hosted_cx53"
base_lem = 4
owner = "ci"
intent = "Build UB/native-boundary evidence packet for changed Rust unsafe code and post one advisory grouped PR review."
failure_mode = "Unsafe contract gaps or aliasing/lifetime issues in changed unsafe code go without review."
proof_obligation = "Run ub-review sensors and model lanes; emit evidence packet to target/ub-review/ artifact."
evidence = [
  "target/ub-review/running-summary.md",
  "target/ub-review/review/review.json",
  "target/ub-review/review/github-review.json",
]
allowed_triggers = ["pull_request"]
labels = ["ub-review", "full-ci"]
expensive = false
blocking_promotion_allowed = false
duplicate_of = ["ripr_advisory"]
review_after = "2026-09-06"
expires = "2026-12-06"

[[lane]]
id = "ub_review_advisory_cx43"
workflow = ".github/workflows/ub-review.yml"
job = "ub-review-cx43"
tier = "frontdoor"
kind = "ub_review"
blocking = false
default_pr = true
runner = "self_hosted_cx43"
base_lem = 4
owner = "ci"
intent = "CX43 fallback for UB review advisory when CX53 is unavailable."
failure_mode = "UB evidence packet unavailable on secondary runner path."
proof_obligation = "Same as ub_review_advisory; runs on CX43."
evidence = ["target/ub-review/running-summary.md"]
allowed_triggers = ["pull_request"]
expensive = false
duplicate_of = ["ub_review_advisory"]
review_after = "2026-09-06"
expires = "2026-12-06"

[[lane]]
id = "ub_review_advisory_github"
workflow = ".github/workflows/ub-review.yml"
job = "ub-review-github"
tier = "frontdoor"
kind = "ub_review"
blocking = false
default_pr = true
runner = "ubuntu_24_04"
base_lem = 4
owner = "ci"
intent = "GitHub-hosted fallback for UB review advisory when no self-hosted runner is available."
failure_mode = "UB evidence packet unavailable on GitHub-hosted fallback path."
proof_obligation = "Same as ub_review_advisory; runs on ubuntu-24.04."
evidence = ["target/ub-review/running-summary.md"]
allowed_triggers = ["pull_request"]
expensive = false
duplicate_of = ["ub_review_advisory"]
review_after = "2026-09-06"
expires = "2026-12-06"

[[lane]]
id = "ub_review_fork_policy"
workflow = ".github/workflows/ub-review.yml"
job = "fork-policy"
tier = "frontdoor"
kind = "policy"
blocking = false
default_pr = false
runner = "ubuntu_24_04"
base_lem = 1
owner = "ci"
intent = "Emit a clear policy record for fork PRs that cannot access org secrets."
failure_mode = "Fork PRs silently skip advisory review without a policy record."
proof_obligation = "Emit policy message; job succeeds (advisory skipped, not failed)."
evidence = ["job output"]
allowed_triggers = ["pull_request"]
expensive = false
duplicate_of = []
review_after = "2026-09-06"
expires = "2026-12-06"

[[lane]]
id = "changelog_advisory"
workflow = ".github/workflows/changelog-advisory.yml"
job = "changelog-disposition"
tier = "frontdoor"
kind = "automation"
blocking = false
default_pr = true
runner = "ubuntu_latest"
base_lem = 2
owner = "ci"
intent = "PR-time changelog disposition validation (Changie fragments or explicit exemptions). FOUNDATION / ADVISORY — blocking_enforced_from unset, so the exit-1 path is unreachable. Tracking issue #3784."
failure_mode = "PR changelog disposition is unknown or an added fragment fails validation, but the advisory status does not block merge (no blocking_enforced_from armed)."
proof_obligation = "Run cargo xtask changelog check, validate Changie fragments, and emit receipt."
evidence = ["job output"]
allowed_triggers = ["pull_request", "workflow_dispatch"]
expensive = false
duplicate_of = []
review_after = "2026-08-10"
expires = "2026-12-10"
