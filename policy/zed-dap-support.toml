# Generated by `python scripts/zed_dap_asset_receipts.py project-dap-support`.
# Do not edit: regenerate and commit. Owner: #9489 (Zed DAP support projection).
# Every public cell is quoted from the committed #9487 journey receipt;
# the D05 validator runs before this file is rendered, so gate staleness
# fails the projection closed instead of drifting this surface.

schema = "zed-dap-support.v1"
projection_issue = 9489
product = "Zed"
integration_mode = "native_editor_extension_debugger_adapter_surface"
source_receipt = ".ci/fixtures/zed-perl-upstream/receipts/dap-public-registry.v1.json"
source_receipt_sha256 = "sha256:cefaab2a5827117c9e81bdb1f7e30ab5e71bfa0450761251d867cf3a2dc94499"
source_receipt_result = "blocked_external"
source_receipt_observed_at = "2026-08-24T12:00:00Z"

[adapter]
adapter_id = "perl-dap"
executable = "perl-dap"
dap_config_schema = "debug_adapter_schemas/perl-dap.json"
session_kinds_supported = ["launch"]
separate_language_server_ids = ["perlnavigator-server", "perl-lsp", "perllsp"]
launch_required_keys = ["request", "program"]

[[binary_route]]
id = "managed_download"
state = "not_proven"
boundary = "requires the official-registry public journey (#9487) to pass"

[[binary_route]]
id = "path"
state = "not_proven"
boundary = "distinct route; managed-public evidence can never promote it"

[lsp_surface]
policy = "policy/lsp-client-support.toml"
row_id = "zed"
support_rows = "unchanged_by_this_projection"

[[stage]]
id = "configuration_static_adapter_authority"
state = "registered_static_authority"
source = ".ci/fixtures/zed-perl-upstream/zed-perl/extension.toml"

[[stage]]
id = "exact_source_dev_extension"
state = "not_proven"
source = ".ci/fixtures/zed-perl-upstream/receipts"

[[stage]]
id = "public_registry_install"
state = "blocked_external"
source = ".ci/fixtures/zed-perl-upstream/receipts/dap-public-registry.v1.json"

[[cell]]
id = "host_and_adapter_selection"
receipts_consumed = ["zed_host_identity", "fixture_open", "adapter_selection"]
static_authority = "configuration_only"
exact_source_dev_extension = "not_proven"
public_registry_install = "not_proven"

[[cell]]
id = "managed_resolution"
receipts_consumed = ["managed_resolution", "adapter_process_identity"]
static_authority = "configuration_only"
exact_source_dev_extension = "not_proven"
public_registry_install = "not_proven"

[[cell]]
id = "session_initialize_launch"
session_kind = "launch"
receipts_consumed = ["initialize_launch"]
static_authority = "configuration_only"
exact_source_dev_extension = "not_proven"
public_registry_install = "not_proven"

[[cell]]
id = "breakpoint_stop"
receipts_consumed = ["breakpoint_verified", "stopped_event"]
static_authority = "configuration_only"
exact_source_dev_extension = "not_proven"
public_registry_install = "not_proven"

[[cell]]
id = "stack_source_identity"
receipts_consumed = ["frame_source_identity"]
static_authority = "configuration_only"
exact_source_dev_extension = "not_proven"
public_registry_install = "not_proven"

[[cell]]
id = "scopes_variables"
receipts_consumed = ["scopes_variables"]
static_authority = "configuration_only"
exact_source_dev_extension = "not_proven"
public_registry_install = "not_proven"

[[cell]]
id = "continue_step"
receipts_consumed = ["continue_step"]
static_authority = "configuration_only"
exact_source_dev_extension = "not_proven"
public_registry_install = "not_proven"

[[cell]]
id = "termination_cleanup"
receipts_consumed = ["terminate_disconnect", "cleanup_bounded"]
static_authority = "configuration_only"
exact_source_dev_extension = "not_proven"
public_registry_install = "not_proven"

[[cell]]
id = "restart_reuse"
receipts_consumed = ["restart_reuse"]
static_authority = "configuration_only"
exact_source_dev_extension = "not_proven"
public_registry_install = "not_proven"

[platform]
os = "windows"
architecture = "x86_64"
target = "x86_64-pc-windows-msvc"
asset_binding = "quoted-from-receipt"
public_journey = "not_proven"
cross_platform_promotion = "denied"

[platform.other_os_architecture_rows]
state = "not_observed"
boundary = "one OS/architecture cannot promote another; linux/macOS asset rows stay managed_extracted_not_executed per the receipt limitations"

[claim_boundary]
support_tier = "not_proven"
public_support_requires_issue = 9487
exact_source_alone_cannot_promote = true
all_platforms = "not_proven"
lsp_support_rows = "unchanged"

[currentness]
invalidators = [
  "#9516 contract or aggregate receipt digest change",
  "DU01 registry acceptance manifest recording a merged-and-released subject",
  "a committed exact-source receipt recording a pass",
]
blockers = [
  "#9486 has no current exact-source real Zed DAP receipt",
  "the official zed-industries/extensions registry does not yet serve the DAP-capable existing perl entry (DM02 pending; the DU01 acceptance manifest records no merged-and-released subject)",
  "#9483 routing/final-check authority is not current",
]
limitations = [
  "The official-registry journey has not run: no released official registry entry serves the DAP-capable perl extension yet.",
  "The linux/macOS rows of the bound #9516 aggregate receipt stay managed_extracted_not_executed; those matching-host executions are owned by the #9516 closeout residual, not by this stage.",
  "This receipt consumes the checked #9516 contract and aggregate receipt; it does not re-derive public asset selection, digests, or the recorded Windows member divergence (#9485 and #7980 own those).",
  "The exact-source stage promotes individual cells only from a genuine committed #9486 receipt binding the perl-dap adapter identity; no such receipt is committed, so every exact-source cell stays not_proven.",
  "The exact-source stage boolean counts only receipts that bind the exact perl-dap adapter identity; a Zed LSP exact-source receipt sharing the zed_host_compat.v1 family never promotes the DAP stage.",
]

[gates]
exact_source_zed_dap_receipt = "absent"
extension_upstream_release = "absent"
matching_host_asset_receipt = "current"
official_registry_entry = "absent"
released_zed_build = "absent"
routing_final_check_authority = "absent"
