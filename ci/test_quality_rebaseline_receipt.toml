# Machine-readable receipt for the complete test panic identity gate.
# Historical #3021 scalar counts remain in issue/PR receipts; this file records
# the maintained #2332 authority.

schema = 1
issue = 2332
sha = "07e7b051215988f6e566f3af0678b20b569d8997"
generated_at = "2026-08-03"

[gate]
command = "cargo clippy --workspace --all-targets -- -D warnings -A missing_docs"
note = "All-target Clippy is the binding test-code seam per scripts/gate-local.sh and xtask CI runner."

[policy]
# Integration-test panic scan skips the same legacy/support crates as CI report walkers.
panic_test_integration_exclude_crates = [
    "tree-sitter-perl-c",
    "perl-parser-pest",
    "perl-tdd-support",
    "perl-test-must",
    "perl-ci-hygiene",
]
allow_unwrap_in_tests = true
allow_unwrap_in_tests_owner = "lint-policy"
allow_unwrap_in_tests_ledger = "policy/clippy-debt.toml"
allow_unwrap_in_tests_expires = "2026-07-01"

[baselines]
panic_test_identities = { file = "ci/panic_test_identities.json", consumer = "cargo xtask ci-hygiene check-panic-test" }
todo_test = { file = "ci/todo_test_baseline.txt", value = 0 }

[counts.test_code]
panic_macro_identities = 119
println_macro_files = 61
eprintln_macro_files = 179

[counts.spinoffs_reconciled]
"#3016" = { title = "println/eprintln triage", status = "closed", note = "Delivered debug-output cleanup; no blunt print baseline justified." }
"#3018" = { title = "hardcoded absolute paths", status = "closed", note = "Finite sink review found zero confirmed host-path filesystem defects." }
"#3019" = { title = "no-assertion tests", status = "closed", note = "Lexical candidates were not a defect population; proven gaps remain ordinary focused work." }
"#3020" = { title = "match-arm panic cleanup", status = "closed", note = "Accidental governed debt closed; retained scheduler panic injection is accepted by its own receipt." }

[coordination.invalid_shorthand]
"#3258" = "unrelated July DAP doc PR in this repo"
"#3259" = "unrelated work in this repo"
"#3260" = "unrelated work in this repo"
"#3263" = "unrelated work in this repo"

[coordination.completed_cross_repo]
"EffortlessMetrics/perl-lsp#3261" = "closed"
"EffortlessMetrics/perl-lsp#3262" = "closed"
