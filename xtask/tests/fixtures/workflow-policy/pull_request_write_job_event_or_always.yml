name: fixture-pr-write-job-event-or-always
on:
  pull_request:
  schedule:
    - cron: '0 8 * * *'
permissions:
  contents: read
jobs:
  publish:
    if: github.event_name == 'schedule' || always()
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - run: git push
