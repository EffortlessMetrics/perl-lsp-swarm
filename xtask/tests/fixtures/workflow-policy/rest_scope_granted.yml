name: REST scope granted
on: pull_request
permissions:
  pull-requests: read
jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - name: List open pulls
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh api --paginate "repos/org/repo/pulls?state=open"
