# Context — qualified references parser corpus

Issue: #6454

The qualified_refs fixture already checks symbol-oriented local and package-qualified references, but the parser-accuracy manifest had no concrete AST anchors. This slice measures both call projections alongside the package, subroutine, and return structures that navigation consumes.
