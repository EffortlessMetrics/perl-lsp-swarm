# Checklist: shared declaration VERSION syntax (#10716)

## Authority transition

- [x] Before: `package` and `class` each recognize a version in the header and
      discard it, on two independently written recognizers, losing spelling,
      range, and exactness.
- [x] After: one owner-neutral `DeclarationVersionSyntax` in `perl-ast` can
      carry that reading. Nothing populates it yet — population is #11089.

## Allowed write surface

- [x] `crates/perl-ast/src/ast/declaration_version.rs` (new module)
- [x] `crates/perl-ast/src/ast.rs` (module declaration and re-export only)
- [x] `crates/perl-ast/src/lib.rs` (crate-root re-export only)
- [x] `crates/perl-ast/tests/declaration_version_syntax.rs` (new proof)
- [x] `.spec/10716-declaration-version-syntax/` (this packet)
- [x] `docs/policy/NON_RUST_INVENTORY.md` (regenerated by
      `cargo xtask non-rust inventory --write` for the three `.spec` files;
      all auto-classified `documentation` under the existing
      `non-rust-root-governance-docs` entry, unclassified count unchanged)

## Forbidden write surface — confirmed untouched

- [x] No `NodeKind` variant added, removed, or changed
- [x] No `kind_schema` registry, visit table, or `FieldId` change
- [x] No `classification.rs` / `invariant_policy.rs` change
- [x] No parser production change (`perl-parser-core` unmodified)
- [x] No traversal, `to_sexp`, Debug-render, or Clone/Eq path change
- [x] No semantic, HIR, provider, or LSP change
- [x] No new crate dependency

## Contract checks

- [x] Decimal and v-string remain distinct source forms
- [x] Raw spelling and exact range both retained; the spelling is *sliced from
      the source* by the only constructor, so it cannot disagree with the range
- [x] Absence is owner-level `Option`, not a sentinel value
- [x] Unknown/recovered is representable and is not absence
- [x] Malformed/recovered can never report exact
- [x] Checked construction returns `Result`; no constructor panics or indexes
- [x] Public enums are `#[non_exhaustive]`, matching crate convention
- [x] An exact form admits exactly the spellings Perl admits; recovery is the
      only escape
- [x] The one-line `Display` contract holds for recovered text containing
      control characters
- [x] No normalized value, ordering, comparison, or activation semantics
- [x] No second raw/normalized or compatibility-string authority
- [x] Equality, `Debug`, and the `Display` projection are deterministic

## Verification

- [x] `cargo test -p perl-ast --test declaration_version_syntax --locked` (15 rows)
- [x] Twelve mutation controls applied, caught, and reverted (see `acceptance.md`)
- [x] Grammar differentialled against Perl 5.38.2 over 44 spellings: 44 agree,
      0 disagreements
- [x] `cargo xtask non-rust inventory --check`
- [x] `cargo test -p perl-ast --all-targets --locked`
- [x] `cargo clippy -p perl-ast --all-targets --locked -- -D warnings`
- [x] `cargo fmt -p perl-ast -- --check`
- [x] `cargo check --workspace --all-targets --locked`
- [x] `cargo doc -p perl-ast --no-deps` (rustdoc example compiles)
- [x] `cargo public-api --simplified` compared against all five facade
      baselines (`perl-lsp-rs`, `perl-parser`, `perl-uri`, `perl-dap`,
      `perllsp`) on nightly `1.100.0` — all unchanged, so no baseline is
      regenerated by this claim

## Stop conditions honoured

- [x] No `NodeKind` edit was required
- [x] No parser production change was required
- [x] No source rescanning or flattened-string reparse introduced
- [x] No new range/disposition authority beyond the declaration-VERSION scope
