Route
- Goal / parent: #11869 (honest-main drain; retained-state lineage #7931/#9155)
- Claim: bound the three unbounded session warning-dedup sets and remove raw value/path/error retention (#9769)
- Entry flow: `$deliver-pr`
- Current useful transition: PR #12289 published + auto-merge armed; substantive review and live checks in flight
- Why: implementation, falsifiers, and all ledger/inventory registration landed in one candidate; integration is now remote-owned
- Durable subject: PR #12289 (`reliability(lsp): bound session warning dedup state`)
- Resume when: required checks conclude, review threads need disposition, or auto-merge fires

Bound semantics decision (reviewed saturation, per the issue's allowed options): per-family hard cap of 32 fixed-size `(code, subject tag, FNV-1a fingerprint)` identities; a saturated family still emits new actionable warnings without retaining them, so the bound is enforced, not documented, and no warning is ever dropped because the table is full. No eviction — within-cap suppression stays session-stable, preserving the existing warn-once policy. Lifecycle clears stay family-local (critic config transition, configuration notification for AI backend) and the hard cap remains load-bearing without them.

Also repaired in the same PR (pre-existing main red, unrelated to this claim's behavior): the #7385 and #8398 ledgers were missing the `parse_effect_contract` module added by #11989; both suites failed on current main. One row each, no behavior change.
