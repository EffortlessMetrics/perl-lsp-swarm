my $sum = 1 + 2 * 3;
my $cmp = $a <=> $b;
my $str_cmp = $x cmp $y;
my $and = $p && $q || $r;
my $match = $s =~ /pattern/;
my $concat = 'a' . 'b';
my $range = 1 .. 5;
my $ternary = $cond ? 'yes' : 'no';
