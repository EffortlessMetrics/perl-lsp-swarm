print "hello\n";
my @sorted = sort { $a <=> $b } @list;
my @mapped = map { $_ * 2 } @nums;
my @kept = grep { $_ > 0 } @vals;
my $len = length($string);
push @stack, $item;
my $joined = join(',', @parts);
