my $length = length($value);
my $lengthened = $length + 1;
my $read = <STDIN>;
my $other_read = <STDERR>;
my $ordered = $left <=> $right;
my $numeric = $left < $right && $right > $left;
my $string = $left lt $right;
my $arrow = $object->method;
my %options = (answer => $object->{answer});
